- [x] `useForumSummary.js` 导出 14 个论坛总结纯函数，函数签名和返回值与原内联实现一致
- [x] `useIntentDetection.js` 导出 13 个意图判断纯函数，函数签名和返回值与原内联实现一致
- [x] `useUserPrivateRetrieval.js` 导出 8 个私域检索纯函数，函数签名和返回值与原内联实现一致
- [x] `useChatEngine.js` 在 Task 1-3 完成后，通过 import 调用提取出的纯函数，`npm run build` 通过
- [x] `useActionDraft.js` 通过依赖注入接收共享状态，导出 14 个 Action 草稿函数
- [x] `useChatEngine.js` 在 Task 4 完成后，通过 `useActionDraft()` 代理导出同名函数，BOHAI/index.vue 调用 `useChatEngine()` 返回值接口不变
- [x] `useMemoryCapture.js` 通过依赖注入接收共享状态，导出 20+ 个记忆捕获函数
- [x] `useChatEngine.js` 在 Task 5 完成后，通过 `useMemoryCapture()` 代理导出同名函数，BOHAI/index.vue 调用 `useChatEngine()` 返回值接口不变
- [x] `useKnowledgeRetrieval.js` 通过依赖注入接收共享状态，导出 18 个知识检索函数
- [x] `useChatEngine.js` 在 Task 6 完成后，通过 `useKnowledgeRetrieval()` 代理导出同名函数，BOHAI/index.vue 调用 `useChatEngine()` 返回值接口不变
- [x] `PostCard.vue` 作为 Forum/index.vue 的子组件，帖子卡片渲染结果与原内联模板一致
- [x] `CommentThread.vue` + `ImageViewer.vue` 作为 PostDetail/index.vue 的子组件，渲染结果与原内联模板一致
- [x] `treehole-api.js` 保留为 re-export 入口，所有 `import from '@/utils/api/treehole-api.js'` 无需改动
- [x] `forum-api.js` 保留为 re-export 入口，所有 `import from '@/utils/api/forum-api.js'` 无需改动
- [x] 全部 Task 完成后 `npm run build` 通过
