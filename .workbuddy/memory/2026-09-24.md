# 2026-09-24

## 首屏开场画打磨（承接 09-23 的返工）

用户反馈四条，全部落地：

### 1. 「往下逛逛」太贴底
`StreetSceneHero` 的 `.street-hero-content` 底部 padding 从 `clamp(28px, 6svh, 64px)`（≈50px）
提到 `clamp(72px, 17svh, 152px)`（844px 高视口下 ≈143px），落在视觉下三分之一。
贴底既像被裁掉、也压住底栏安全区。

### 2. 横屏适配（两个真问题）
- **开场画被撑出视口**：`.street-hero { min-height: 480px }` 在横屏手机（视口 390~430px 高）下
  比视口还高 → 底部提示直接被裁。加 `@media (orientation: landscape), (max-height: 560px)`
  把 min-height 放开。
- **顶部被导航栏压住**：横屏导航是**全宽横条**（竖屏是居中胶囊），原来顶部 padding 写
  `max(84px, 12svh)` 不够。改为吃 `--userspace-nav-h`（导航实测高度）：
  `max(84px, calc(var(--userspace-nav-h, 78px) + 16px))`，横屏单独 `max(64px, calc(... + 12px))`。
  横屏问候语另收窄到 `min(52%, 24ch)`、字号降到 `clamp(19px, 2.4vw, 26px)`。

### 3. 每次发布新版本都要重播开场画
一次性标记的 key 带上构建指纹：`boh-home-gate-passed:${<meta name="boh-build-id">}`。
`boh-build-id` 只由生产构建注入（`vite.config.js` 的插件 `apply: 'build'`），
**dev 下拿不到 → 退回不带后缀的 key**，所以开发与探针行为不变。
⇒ 每次发版用户再访问都会看到一次开场画；同一次部署内不会重复打扰。

### 4. 动画：不追快，追精致
- 时长 1080ms → **1200ms**（开场层），论坛层 **1320ms**（比开场层晚收 120ms，形成余韵）
- 缓动从纯 `linear` 换成两条：开场层 `cubic-bezier(0.32, 0.06, 0.36, 1)`（起步柔、收尾缓），
  论坛层 `cubic-bezier(0.16, 1, 0.3, 1)`（easeOutExpo 类，落得柔）
- 开场层退场加 `scale(1 → 1.04)`：纯平移像「贴纸被抽走」，一点推近才有镜头让开的空间感
- **三层错开**是精致感来源：① 开场画内文字按 55% 时长先退（前后景分离，用 `:deep()` 选中
  `.street-hero-content`）；② 底图 1200ms 走完；③ 论坛层 1320ms 缓落
- 底栏入场 560ms linear → **760ms `cubic-bezier(0.16, 1, 0.3, 1)`**，位移 132px → 150px

### 5. 顺带修：开场画被论坛浮层「冒头」
竖屏截图里右下角出现论坛的发帖按钮。根因：`.mobile-compose-fab` 是
**Teleport 到 body** 的（父级就是 body），z-index 1200，而 gate 当时只有 60。
修法：gate 的 `z-index` 提到 **1300** —— 高于 1200（发帖按钮）、低于 9999（导航栏）
与 10000（登录模态框），既压住论坛浮层又不挡用户点登录；同时保留
`.home:not(.home-forum-open) .home-forum-stage { visibility: hidden }` 管 stage 内那类浮层。

### 6. 底栏浮现时序（用户：封面离开时导航栏自然出现，慢一点、晚一点）
底栏的挂载从 `forumOpen` 拆出独立开关 `bottomNavReady`，加一条延迟：

| 时点 | 事件 |
|---|---|
| 0ms | 手势触发，封面开始退场（缓动起步柔，前 100ms 几乎不动） |
| 780ms | 封面走了 ~82%，底栏从下方开始浮现（挂载即播入场） |
| 1200ms | 封面卸载离场，此刻底栏浮到约一半 |
| 1800ms | 底栏完全就位 |

- 延迟 `BOTTOM_NAV_REVEAL_DELAY_MS = 780`，时长 `BOTTOM_NAV_ENTER_MS = 1100`（默认仍是 760，
  只有首页传更慢的值 → `UserSpaceBottomNav` 新增 `enterDuration` prop + CSS 变量
  `--bottom-nav-enter-duration`）
- ⚠️ **曲线踩过一次坑**：初版用 `cubic-bezier(0.16, 1, 0.3, 1)`（easeOutExpo），
  前 30% 就走完 80% 位移 —— 实测观感仍是「弹上来」而非「自然浮现」。
  换成与封面同族的 `cubic-bezier(0.32, 0.06, 0.36, 1)` 后位移才均匀铺开
  （实测 opacity 0.01→0.07→0.20→0.37→0.58→0.72→0.83→0.91→0.96→1.00）。
- 已通过过开场画的会话（sessionStorage 命中）`bottomNavReady` 直接为 true，不补浮现动画。

### 验证
- `probe-home-revamp.mjs` **46 断言全绿**，本轮新增：B0 手势刚触发时底栏还没出现、
  B8b 底栏浮现时长按首页传入。B1/B2 的 transform 采样改成「轮询等位移真的开始」
  （固定 sleep 在 HMR 抖动时会假红）。
- 构建 `vite build --outDir dist-check` ✓；门禁全过

---

## 数据管理面板「列错误」一键查错（用户报「点到有些内容总是报错」）

**结论**：45 个表格页签里 **14 个点开即 400**（`获取数据失败: column xxx does not exist`），
另有 **20 处**用搜索/日期筛选/状态筛选/表头排序才报。根因单一：`query-config.js` 声明的列名
与 DB 实际列对不上，DB 侧零改动（全程只读）。详见
`docs/2026-09-24-数据管理面板列错误查错报告.md`。

**最大一块**：抽奖 6 个页签（lotteryFulfillments/Entries/DrawLogs/NotificationJobs/JoinAttempts/AuditLogs）
内嵌 `profile:user_id(username,email)` 要 `email` → `column profiles_1.email does not exist`。
**`profiles` 表根本没有 email 列**（email 在 `auth.users`，前端靠 RPC `admin_list_users_with_sensitive` 补）。
—— 这是 H-1「敏感字段列级收窄」那次的漏网之鱼：`users` tab 改了，6 处内嵌没改。

**其余点开即报**：anniversaryClaims(`plan_code`→`granted_plan_code`)、moderationLogs(`moderator_name`，
应内嵌 `moderator:moderator_id(username)`)、cloudinaryUploads(表语义已整体过时，需产品决策)、
apiKeyAuditLogs(`operator_id`→`actor_id`，name 不可内嵌)、birthdayEvents(`target_username`→内嵌)、
birthdayWishes(`updated_at` 不存在)、labUsageRecords(裸 `username` 冗余，已有内嵌)、
aiWebSearchLog(内嵌 `profile:user_id` 报 PGRST200 —— FK 指向 auth.users，跨 schema 关联不上)。

**新增工具（package.json 已接）**：
`audit:dm-schema-refresh` / `audit:dm-columns` / `audit:dm-columns:live` / `probe:dm-errors`。
静态 7 类引用全覆盖 + PostgREST 真打三轮（扁平列 / 筛选排序搜索列 / 内嵌关联）+ UI 逐页签点击。

**避坑（都是本轮踩的）**：
- 真打验证**不能剥掉内嵌**：剥掉后只能证明扁平列存在，抓不到 PGRST200「关系不存在」。
  6 个抽奖页签的 email 问题正是第 3 轮补上才发现的。
- `TAB_SORT_COLUMNS` 的值是 **Set**，`Object.keys()` 得 `[]` → 会整类漏检。已修。
- 剥离内嵌若按括号深度收集字符，会得到 `alias:target`（括号被吃掉）→ PostgREST 当成
  `alias:column` 报假列错误。必须用正则整段删除 `[A-Za-z_]\w*:[A-Za-z_]\w*\([^()]*\)`。
- 本机到 `*.supabase.co` 间歇 `fetch failed` → 必须重试（6 次+退避），
  否则失败会被误判成「✅ 通过」。重试仍失败一律标「未验证」，不能算通过。
- **pinia 伪造 admin 登录会被会话/路由检查覆盖**（表现为侧栏模块先能点、几百毫秒后变置灰）。
  单次注入不可靠，要在页内装 `setInterval` 常驻守护 + 点击前实时复核 DOM 的 `is-denied`。
- 探针里 `permission denied for function admin_*` / `user_follows` 401 **不是 bug**：
  anon 无权、`authenticated` 有（已用 pg 侧 `has_table_privilege` + `pg_policy` 核实）。
  报告必须把这类「探针环境噪声」与真问题分开，否则会被误修。

### 追加：cloudinaryUploads（上传队列）判定 —— 结论「摘页签、保表」

**表不是"上传队列"，是「上传归属台账」**（删图 EF 的第一道归属校验就查它：
`supabase/functions/cloudinary-delete/index.ts:167` `filterOwnedCloudPublicIds`）。表**不能删**。

**页签建议摘掉**，4 条依据：
1. 列配置是上一版语义 —— 表里根本没有 `status`/`error_message`/`retry_count`
2. `fields: []`，本来就不能编辑
3. 唯一动作 delete 是 `.from(table).delete().eq('id',..)` **硬删台账行、不删 Cloudinary 文件**
   （data-management 目录下 grep 不到任何 `cloudinary-delete` 调用）→ 删完用户合法删图会 403，**反向作用**
4. 数据没法判断该不该清（见下）

⚠️ **「孤儿」≠ 垃圾**（本轮最重要的反直觉发现）：63 行 `claimed_at is null and deleted_at is null` 里，
逐行查引用后 —— `forum` 42 个孤儿**0 个被引用**（真垃圾）；但 `hero-console` 13 个里 **4 个是首页 hero 在用的图**、
`shop-console` 2 个**全部在用**、`avatar-frames` 5 个里 2 个在用。共 **8 张线上素材被标成"孤儿"**。
根因：`markCloudinaryUploadsClaimed` 全仓**只有 3 个调用点**（`post-api.js:924`、`block-wall-api.js:104`、
`UserSpaceMain.vue:2301`），**装修台（HeroConsole/ShopConsole/AvatarConsole）没接** → 上传即永久孤儿。
⇒ 任何按 `claimed_at is null` 的批量清理都会误伤线上素材，必须先补装修台的 claim。

**清理缺口**：全库 6 个 cron 没有一条碰这张表，孤儿持续累积（5月1/6月23/7月0/8月15/**9月24**）。
该清的是 `source='forum' and claimed_at is null and created_at < now()-7d`，且必须走 EF 删文件 + 标 `deleted_at`。

---

## 数据管理面板列错误：修复落地 + 复验（承接上一节）

**复验结果全绿**（详见 `docs/2026-09-24-数据管理面板列错误查错报告.md` 第九节）：
- 静态 `audit:dm-columns`：P0 26→**0**、P1 3→**0**（exit 0）
- 真打 `audit:dm-columns:live`：select 列错误 7→**0**、筛选/排序/搜索 15→**0**、内嵌 6→**0**、未验证 0
- 写入白名单 `audit:dm-writable`（新增脚本）：列不存在 2→**0**
- 页签 45→44（摘掉上传队列）

**最大一条教训：写入白名单才是「保存」的真判据。** `saveStrategies.js#pickWritableFields()` 只放行
`config/fields.js` 的 `TAB_WRITABLE_FIELDS`，`dataConfig.fields` 只是编辑器 UI，里面的多余字段**不会进 payload**。
首查时我按 fields 判据报了 10 处「编辑即 400」，全是误报；真问题是白名单里的
`users.email`（profiles 无此列）和 `birthdayWishes.updated_at` —— **编辑用户/编辑生日祝福保存必 400**。
→ 静态审计脚本第 7 类判据已改成以 `TAB_WRITABLE_FIELDS` 为准，fields/columns 的缺失列降级为 INFO（多为派生字段）。

**新增工具**：`npm run audit:dm-writable` —— 用 `update <t> set <col> = <col> where false` 逐条判定
（`where false` 永不匹配、**零数据改动**；语法错误在执行前抛出，能精确区分「列不存在」与「其它错误」）。
193 个写入字段全量验证约 1 分钟；Management API 有**限流**（ThrottlerException），跑太快会漏判。

**装修台补认领**：`uploadImageToCloudinary(file, { ..., claimPendingUpload: true })` 透传到
`registerCloudinaryPendingUpload` 的 `options.claimed` → 直接写 `claimed_at`。
3 个 console 共 6 处（Hero 2 / Shop 2 / Avatar 2）。默认不传时行为与改动前**逐字一致**（claimed_at: null）。
⚠️ 坑：HeroConsole 两处上传的**缩进不同**（8 空格 vs 6 空格），`replace_all` 只命中一处，
靠 `grep -c claimPendingUpload` 才发现漏了一处 —— 多点改写必须数落点个数。

**新增单测** `tests/unit/cloudinary-claim-on-upload.test.js`（5 例）：
register 默认未认领 / claimed:true 写时间 / 三个 console 每处 upload 调用都带 claimPendingUpload（源码守卫）。
**已做反证**：移除 AvatarConsole 一处标注 → 守卫测试变红 → 恢复全绿。

**既有红灯（与本轮无关，勿混淆）**：`tests/unit/home-heroes-partial-save-regression.test.js` 有 1 例失败 ——
断言指向 `Home/index.vue` 的 `:key="(hero.template === 'builtin' ...)"`，但该文件已被 09-23 首页改版
重构成「开场层 + 论坛层 + 底栏」三段式，hero 行列表渲染整体移除（HEAD 版本第 18 行还有该表达式，
工作区已无），属**过期断言**，需按新结构更新或删除。

---

## 首屏 / 英雄区 / 装修台：功能实跑验证（用户追问「他们的功能都正常吗」）

**结论：全部正常。**

| 范围 | 探针 | 结果 |
|---|---|---|
| 首屏开场画 A1–A11、下滑进论坛 B0–B9、官方分区渲染**英雄区流** C1–C2、分区切换不炸 C4、底栏 D/E/F、横屏 H、无 pageerror G | `probe-home-revamp.mjs` | **46/46 PASS** |
| 数据管理面板 → 三个装修台入口（商城/首页/头像框）：入口存在可用、路由正确、根节点渲染、标题匹配、按钮齐、无运行时错误 | `probe-console-entries.mjs`（本轮新增） | **22/22 PASS** |
| 装修台相关单测 | vitest | 103/103 |

新增探针 `scripts/probes/probe-console-entries.mjs`，断言矩阵：`.shop-console` / `.hero-console` / `.afc`
三个根节点 + `h1` 文案 + 按钮数 + `hash` 路由 + pageerror/5xx。
⚠️ **探针坑**：装修台是**独立页面**（`/admin/<slug>`），进入后会把 admin shell 整个替换掉、侧栏消失 ——
所以遍历多个入口时**每个都必须先回到 `#/admin/data-management` 再点**，否则第二个之后全点不到
（第一版就这样误报成「首页装修/头像框被禁用」，实际是探针的顺序依赖）。

**两个过期探针/断言（是旧结构，不是功能坏）**：
- `probe-beta6-hero.mjs`：等 `.beta6-hero` 出现在**首屏**并断言「首屏第一位 = beta6-renewal」——
  改版后首屏是开场画，hero 渲染在**官方分区**（`OfficialHeroStage` → `HomeHeroFlow`，两层异步 chunk）。
  组件链本身完好（`BuiltinHeroRenderer` 按 `builtinKey === 'beta6-renewal'` 渲染 `Beta6RenewalHero.vue`）。
  该探针需要把进入方式改成「进官方分区」，或直接废弃（home-revamp C2 已覆盖英雄区流渲染）。
- `home-heroes-partial-save-regression.test.js` 的 `Home/index.vue` `:key` 断言（同上节）。

**本轮改动对首屏/装修台的影响：零。** 只给装修台的上传调用加了 `claimPendingUpload`（纯新增参数），
未触碰任何首屏渲染、开场画、hero 组件或装修台的保存/发布逻辑。

---

## 首屏内容可控性（用户问「我可以控制首屏的内容吗」）

**数据链路**：首屏开场画 = `home_heroes` 表里 `template='street-scene'` 且 `status='published'`
且 `is_archived=false` 的那一行（`src/views/Home/index.vue:366` 用 `publishedHeroes.find(...)` 取）。
**DB 单例约束保证至多一条生效**，冲突时装修台会拦并提示。

| 首屏元素 | 可否后台控制 | 落点 |
|---|---|---|
| 竖屏背景图 `image_portrait`（1170×2532） | ✅ 可配 | HeroConsole「首屏双构图」 |
| 横屏背景图 `image_landscape`（2560×1440） | ✅ 可配 | 同上（WebP ≤350KB） |
| 问候语「早上好，欢迎回到方块街」 | ❌ **代码硬编码** | `StreetSceneHero.vue:104-111`（按小时取时段词） |
| 「往下逛逛 ↓」提示 | ❌ **代码硬编码** | `StreetSceneHero.vue:19-20` |

**降级链**：本方向专属构图图 → 品牌图 `main1-{768,1280,1920}.webp` → 纯暖色底 + 问候（永不白屏）。

⚠️ **现状：DB 里一条 `street-scene` 都没有**（全部 16 条 hero 是 builtin 11 / overlay 3 / showcase 1 / standard 1）。
即首屏当前显示的是**品牌兜底图**，这条控制链路尚未被启用 —— 想接管只需在装修台建一条并发布。

**新增探针** `scripts/probes/probe-hero-console-street-scene.mjs`（13/13 通过）：
验证「新建 hero → 模板下拉选首屏街景 → 出现双构图编辑区（2 个 url 输入 + 规格提示 + 单例说明）
→ 实时预览渲染问候语与提示 → 未配图时显示回落提示」，**不做真实上传、不写库**。

---

## 首屏文案可配落地（用户要求把问候语 / 「往下逛逛」改为可选自定义）

**迁移**：`supabase/migrations/2026092401_home_heroes_greeting_hint.sql` —— `home_heroes` 加
`greeting_text`（问候语模板，支持 `{greeting}` 占位符）/ `hint_text`（底部提示）两列，均可空。
已通过 Management API 应用并补写 `schema_migrations`（version=2026092401）✓；
`docs/db-schema-snapshot.json` 已刷新（967 列）。

**单一真相源**：`src/utils/street-scene-copy.js` —— 默认模板（`{greeting}，欢迎回到方块街`）、
默认提示（`往下逛逛`）、`resolveGreetingWord(date)` / `resolveGreetingText(template, wordOrDate)` /
`resolveHintText(hint)`。**线上 `StreetSceneHero.vue` 与装修台预览共用**（两处必须同口径，
不得各自维护副本 —— 有源码守卫测试钉住）。

**改动点**（6 处）：types/index.ts、stores/homeHeroes.ts（SELECT 列表 / normalizeHero / insert / editableFields）、
HeroConsole（createDraft 默认值 / saveCurrent payload 仅 street-scene 模板生效 / 「首屏文案」编辑区 2 输入 / 预览）、
StreetSceneHero（读 hero 字段 + 回落）。

**关键不变量**：两字段留空时，渲染结果与可配上线前**逐字一致** —— 单测 13 例（含「默认模板逐字断言」）
+ 端到端 9/9（A 自定义含占位符替换 / B 留空回落 / C 无行回落，全部 mock home_heroes 不写库）。
**已反证**：改掉 utils 默认模板 → 3 例变红 → 恢复全绿。
门禁：check:views / structure / first-paint / SFC parse 全过。

---

## 默认首屏改版：白底 + 红苹果 logo + 黑字（用户需求「纯白背景、红 logo 居中、文字黑色、横竖屏适配」）

**改造 `StreetSceneHero.vue` 的兜底形态**：没配街景图（或街景图加载失败）时，不再退位图品牌图
`main1-*.webp`，改为 **CSS 绘制的品牌兜底层** —— 纯白底（#fff）+ 居中红苹果 logo
（`/icons/icon-512.png`，像素风，`image-rendering: pixelated`）+ 暖黑文字。

- **形态切换**：`hasStreetImage = 有本方向构图图 && 未加载失败`。true → 照片形态（img + scrim + 白字阴影）；
  false → `on-brand` 兜底形态（白底 + logo + 黑字）。二者互斥渲染，scrim 仅照片态存在（白底上黑渐晕会成灰带）。
- **横竖屏**：logo 尺寸用 `svh` 约束 —— 竖屏 `clamp(132px, 24svh, 240px)`、横屏 `clamp(96px, 32svh, 176px)`，
  `object-fit: contain`，中心与视口中心偏差实测 0.0px。
- **文字**：问候语 `#17130e`、提示 `#52483c`，均去阴影（对比度达标）；配图态仍是白字+阴影，不变。
- **动画**：logo 640ms 淡入+scale(0.94→1)，文案 520ms 延迟 140ms 淡入；`prefers-reduced-motion` 全关。
- **降级链更新**：街景图 → 白底+logo（DOM+CSS，无外部依赖、不会挂）→ logo 挂了才退纯暖色底。
  原「品牌位图二级兜底」删除；`main1-*.webp` 文件保留（其它处仍引用）。
- `getImageUrl` import 已随 brandFallback 一起移除；探针 `probe-home-revamp` 的 A5/A6 判定放宽为
  「街景图或品牌 logo 二者之一」（默认态无 `.street-hero-img` 节点）。

**验证**：`probe-home-revamp` **46/46**；新增 `probe-street-scene-brand.mjs` **20/20**
（竖屏 A9 / 横屏 B5 / 照片形态 C6 三场景：白底、logo 中心偏差 0、黑字无阴影、无 scrim、
配图后 scrim 回归文字回白）。⚠️ 探针 C 场景的街景图必须用 **data URI**（可真实加载）——
用假 URL 会触发「图挂→回落兜底」的设计行为，测不到照片形态。
单测 29/29、check-first-paint / SFC parse 全过。

---

## 首屏两修：底栏浮现位置偏移（真 bug）+ 退场动态感增强

### 1. 底栏浮现「位置偏移」—— keyframes 与定位方案打架（真 bug）

**实测定位**（`scripts/probes/probe-gate-bottomnav.mjs`，新增）：动画期间 `left=-175, offX=-195`
（偏出左屏半个身位），摘类瞬间跳回 `offX=0` —— 用户看到的偏移就是这 195px 猛跳。

**根因**：底栏有两套水平定位 —— 桌面（≥768px）`left:50% + translateX(-50%)`；
**移动（<768px）`left:16px/right:16px` 拉伸式，transform 只有 `scaleX()`**（bottom-nav.css 923 行媒体查询）。
而入场 keyframes `bottomNavSlideIn` 在 transform 里**写死 `translateX(-50%)`** ——
移动端上等于多平移半个身位，动画结束摘类 → 跳回。⚠️ 该 bug 在 UserSpace 首次进入同样存在（非首页特有）。

**修法（通用解）**：keyframes 改用**独立 `translate` 属性**（`translate: 0 150px → 0 0`）——
它与 `transform` 是两个独立属性、叠加生效互不覆盖，两套定位方案通吃，摘类后归零无跳变。
**教训：凡 keyframes 动 transform，必须先确认元素所有定位形态下 transform 的静态值**；
需要「只动一个分量」时用独立 translate/rotate/scale 属性。

### 2. 首屏退场动态感增强（style.scoped.css + index.vue 计时器同步）

动态感来自「更快 + 加速曲线 + 论坛层更低处追赶」：

| 参数 | 改前 | 改后 |
|---|---|---|
| 开场画退场 | 1200ms 缓出（匀速感） | **880ms 加速**（easeInQuad `cubic-bezier(0.55,.085,.68,.53)`，越走越快冲出） |
| 位移/scale/尾态透明度 | -100% / 1.04 / 0.45 | **-112% / 1.06 / 0.25**（出屏更彻底） |
| 论坛层追赶 | 40px / op 0.78 / 1320ms | **88px / op 0.35 / 980ms**（从更低处追上） |
| 底栏起步/滑入 | 780ms / 1100ms | **560ms / 900ms** |

⚠️ index.vue 的 `GATE_TRANSITION_MS` / `FORUM_ENTER_MS` 与 CSS 变量必须同步改（卸载与摘类按它计时）；
`probe-home-revamp` B8b 断言写死了时长值（1100→900），改节奏记得同步探针。
**验证**：gate-bottomnav 时间线（动画全程 offX=0.0、560ms 起步 1460ms 落定无跳变）、
home-revamp **46/46**（B1/B2 的 matrix2D 解析兼容——故退场保持 2D 不加 rotateX/perspective，
否则 matrix3d 会令探针失配）、first-paint / SFC / important-budget 全过。

---

## 控制台报错排查（用户贴生产/dev 控制台日志问「哪些能修」）

| 日志 | 判定 | 处理 |
|---|---|---|
| GoTrueClient `lock` deprecated ×2 | ✅ 已修 | `supabase-client.js` 删 `lock/lockAcquireTimeout`：v2.116 内置无锁会话协调，官方建议弃用；旧自定义锁只是**同页内存队列**（无跨 tab 能力），单实例场景纯冗余 |
| `apple-mobile-web-app-capable` deprecated | ✅ 已修 | index.html 补标准名 `mobile-web-app-capable`；**apple-* 必须保留**（旧 iOS Safari 只认它），两个都放是标准做法 |
| `main.7ee886d8.js:1 v[w] is not a function` | ⚠️ 待定位 | 生产压缩码、无堆栈无法对应源码；本地核心路径扫描零此类错误（`probe-core-pages-errors.mjs`）。需用户给触发操作或展开堆栈。注意报错夹在第三方脚本日志之间，不排除被注入脚本污染环境诱发 |
| `BRUSH_CX_API_RESPONSE_STATUS` / `operateTypeoperateType` / `...ang.com/api/log/` / `stauts:204` | ❌ 非项目代码 | 第三方注入（浏览器插件/劫持脚本）特征：拼写错误、重复拼接、`VM<id>` eval 脚本、陌生上报域名。让用户查插件/无痕窗口对比 |
| `<Suspense>` experimental | ❌ 无需修 | App.vue:344 有意使用（注释说明 fallback 永不进入），dev-only 警告，生产剥离 |

**新增探针** `probe-core-pages-errors.mjs`：admin 登录态扫 5 条核心路由（/、user-space、login、settings、admin/data-management），收集 pageerror 并专项盯「is not a function」。

⚠️ **自我纠错记录**：删 lock 时 old_string 圈了「函数体+下一行调用」，把无关的
`normalizeSupabaseImplicitHashCallback()` 调用一并删掉 —— 立即发现恢复。
**教训：Edit 删除代码时一次只圈要删的最小片段，绝不顺手带上下文行。**

**欠着的事**：用户要的「生产环境重播首屏的按钮」尚未做（被本话题打断）——
方案已定：清 `boh-home-gate-passed*` 前缀的 sessionStorage（key 带构建指纹须前缀匹配）→ 回 `#/` 并 reload。

---

## Vue 警告「Failed to resolve component: User」修复（用户贴截图）

`UserSpaceMain.vue` 三个 tab（方块 41 / 我的 81 / 设置 148）的未登录占位都写了 `<User class="login-prompt-icon">`（lucide 图标），**但全文件没有任何 lucide-vue-next 导入** → Vue 解析失败退化为原生 `<user>` 元素：
**不只是控制台警告，未登录用户根本看不到占位图标**（真 UI bug）。警告重复 N 次是因为三个 tab 各一份 + KeepAlive 重渲染。

修复：`import { User } from 'lucide-vue-next'`（一行）。验证：未登录访问 `#/user-space?tab=settings`，
`.login-prompt-icon` 的 tagName 从 `user`（坏）变 `svg`（好），警告归零。
⚠️ 启示：`Failed to resolve component` 类警告**永远不只是警告** —— 组件没渲染出来，要用「DOM 里实际渲染成什么标签」验证修复，
而不是只看警告消失。

---

## 「重播首屏开场画」入口（用户要的「生产环境方便看首屏的按钮」）

**位置**：`ProfileSettingsPanel.vue`（UserSpace 底栏「设置」→ 外观与浏览分组，主题设置下面一行，
RotateCcw 图标，desc「清除已看标记并回到首屏再看一次」）—— 所有登录用户可达，不限于 admin。

**实现要点**（`replayHomeGate()`，自包含不经过父级）：
- 清标记必须**前缀匹配**：key 是 `boh-home-gate-passed:<build-id>`（生产构建注入指纹），删某个固定 key 没用；
- 必须用 `location.hash='#/'` + **`location.reload()`**，路由跳转不会重挂 Home → 不会重播。

**端到端闭环全通**（登录态）：入口出现 → 点击 → 整页重载且开场画重播（白底+logo 兜底形态）
→ 下滑进论坛 → 开场画卸载 + `boh-home-gate-passed` 标记由 `markGatePassed()`（在 `enterForum` 里）重新写入。
⚠️ 验证注意两点：① 设置面板只在**登录态**渲染（未登录是 login-prompt 占位），探针必须注 pinia；
② `markGatePassed` 在**进论坛时**才写、不是挂载时 —— reload 后标记暂时为空是正确行为，别写成失败断言。

---

## 过渡期滚动锁 + 论坛层模糊渐显（用户要「被带到下面」的固定动画）

**滚动锁**（`Home/index.vue`）：`enterForum` 里 `lockDocumentScroll()` ——
html+body 双锁 `setProperty('overflow','hidden','important')`（⚠️ 必须 important：style.global.css 的
html/body overflow 带 !important 棘轮，普通 inline 会被压掉）；论坛落定计时器（FORUM_ENTER_MS）里解锁；
**onBeforeUnmount 兜底解锁**（防路由跳走时计时器被清 → 文档永久锁死）。
效果：触发后无论手势滑多长、连滚几下、触控板惯性，880ms 动画完整播完，gate 卸载时论坛必从顶部开始。

**模糊渐显**（`homeForumStageIn` keyframes）：论坛层 `blur(8px)→60%时2px→0`，从失焦到对焦。
⚠️ filter 只挂入场动画 —— 摘 forum-entering 类后 filter 自然归零，不给落定后的论坛层留常驻合成层。

**验证**：新增 `probe-gate-scroll-lock.mjs` **8/8**（锁定期猛滚 scrollY 恒 0 / 动画不被打断 /
blur 在渐变 / gate 卸载后 scrollY=0 且 stage 顶对齐 / filter 归零 / 滚动权交还）；
home-revamp 46/46 无回归。

**「看不到模糊渐显」的排查与增强**（用户反馈看不到）：
- 原因 ①：过渡动画只在**会话首次进论坛**播 —— 标记已写回时刷新直接落论坛、零动画（用设置里的重播入口）；
- 原因 ②：旧版模糊只在论坛侧、起步 opacity 0.35 且被 gate 覆盖，视觉窗口太短。
- 增强：**双侧模糊叙事** —— gate 退场加 `blur(0→12px)`（失焦退场）+ 论坛层 `blur(12px→75%时4px→0)`、
  起步 opacity 0.35→0.5（对焦入场）。实测帧采样：gate 0.58→6.59px 递增、论坛层 12→4px 对焦中。
- ⚠️ 若用户系统开了「减弱动态效果」（prefers-reduced-motion），动画整体关闭 —— 也属于「看不到」的合法原因。