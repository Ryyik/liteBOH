# 2026-09-13

## Lab 澄清升级为「汇总确认」三段状态机（0913 上午，未提交 Git）

**用户拍板交互**：AI 自动判断意图 → 主动问细节 → **汇总「已记录你的要求」并主动询问是否可以开始** → 用户确认后才生成；用户的提示词要求全程牢记并拼入生成输入。

**实现**（Lab/index.vue）：pendingClarify 布尔升级为 `clarifyStage` 三态（none/asked/confirm）+ `clarifyFlow`（可换意图）+ `clarifyIntentText` + `clarifyDetails[]`（牢记列表）。
- asked：命中任务意图（detectIntent 正则，不用 LLM 分类器——P2-12 误判教训）→ CLARIFY_PROMPTS 提问，零扣费零调用
- asked 收到补充 → **buildClarifySummary()**（意图原文+逐条要求列表）+「确认无误请回复开始生成（消耗 1 次配额）」→ confirm 态
- confirm 收到消息：**isAffirmative（≤10 字 + 确认词正则）**→ [意图原文, ...细节列表].join('\n') 作为 userText 进生成（此时才预扣）；否则当补充 → 更新列表再次确认（长消息绝不误判为确认）
- 换意图（confirm 态说「改成写 Word」）跟随更新 clarifyFlow；resetConversation 清状态；send 的 userText 改 let（确认时重写）

**验证**：build PASS；probe-lab-island-fixes.mjs 扩到 **16/16 PASS**（①澄清零扣费 ②汇总牢记细节+主动询问 ③补充更新再确认 ④「开始生成」才预扣+大纲请求 userPrompt 含全部细节 ⑤IME 守卫 ⑥岛卡仲裁）。回归 40/40、14/14、9/9 全保持。

**设计取舍**：意图判断保持正则（LLM 分类器 P2-12 误判率高）；「牢记」用原文列表而非结构化解析（零误判、可追溯）；确认词守严（≤10 字），长消息永远当补充——宁可多问一句不误扣费。
