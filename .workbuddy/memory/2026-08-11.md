# 2026-08-11

## 商城多支付方式改造
- SQL迁移 `2026081101_multi_payment_support.sql`：products 表加 payment_mode + rmb_price，shop_points_orders 加 points_used + rmb_total + payment_mode，RPC 重写为 create_shop_order 支持三种支付模式
- 类型层：Product/BagItem 加 payment_mode + rmb_price，新增 PaymentMode 类型
- products store：normalizeProduct 按 payment_mode 分路验证 is_purchasable
- bag store：addToBag / normalizeBagItems 按 mode 分路验证
- Shop UI：formatPriceDisplay 统一价格显示（积分/RMB/组合），bag summary 分列积分与现金，checkout 支持混合支付
- ShopConsole：表单加支付方式下拉 + 现金价格输入，validateDraft/buildPayload 按 mode 分路
- 数据文件：所有商品加 payment_mode:"points_only"，吉祥物公仔改为 combined 模式 (45积分 + ¥29)
- 横屏：点击头像弹出毛玻璃卡片 overlay，不离开商城页面
- 竖屏：跳转到新路由 `/shop/account`（`ShopAccountPage.vue`）
- 修改文件：`src/views/Shop/index.vue`（orientation 检测 + 逻辑分支）、`src/views/Shop/style.scoped.css`（overlay 样式）、`src/router/routes/public.ts`（新增路由）
- 新增文件：`src/views/Shop/ShopAccountPanel.vue`、`src/views/Shop/ShopAccountPage.vue`
