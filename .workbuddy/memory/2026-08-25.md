# 2026-08-25

## 订阅体系勘察与优化方案（已出方案，未动手）

**背景问题**：用户已是 Ultra 再买一个月 Plus 时，系统行为是「不同 plan_code 不顺延、立即并行生效」，而等级计算取最高档 → Plus 被 Ultra 掩盖白白浪费；且 UI 无续费按钮（同档按钮"正在生效"禁用），FAQ 承诺的"升级折算"未实现。

**关键代码位置**：
- `subscribe_with_points`：`supabase/migrations/2026033003_subscription_points_rework.sql`（只对相同 plan_code 顺延，L119-133）
- 等级计算：`supabase/migrations/2026063004_get_user_tier_rpc.sql` + `2026070103_get_user_subscription_tiers_batch.sql`（取 active 订阅最高档 free<plus<pro<max<ultra）
- 订阅页：`src/components/SubscriptionPlans.vue`（价格硬编码：plus8/pro20/max40/ultra70 月付，年付×10；同档按钮禁用）
- API：`src/utils/api/subscription-api.js`（subscribeWithPoints → RPC）
- 档位顺序前端源：`src/utils/subscription-benefits.js` TIER_ORDER
- 测试：`tests/integration/subscription-api.integration.test.js`

**用户决策**：做优化 = 支持续订（同档顺延）+ 升级（立即生效 + 按剩余天数折算积分抵扣）+ **不做降档**（低于当前档直接禁止）。先出方案评审，不写代码。

**方案要点**（已给用户，待评审）：
- RPC 签名不变，内部三态判定（new/renew/upgrade）；折算 credit = 最高档订阅 points_cost × remaining_days / (duration_months×30)，应付 = max(0, 全价-credit)，旧订阅置 expired + metadata 记录；降档返回 LOWER_TIER_NOT_SUPPORTED
- 前端按钮四态：立即订阅 / 续费 / 升级 / 已是更高级会员(禁用)；升级弹窗显示折算预览（以 RPC 返回为准）
- 新迁移文件建议名：`2026082604_subscription_upgrade_proration.sql`（最新编号 2026082603）
- FAQ 折算文案保留（这次真实现）

## 已落地（用户批准方案后实施完成）

- 新增 `supabase/migrations/2026082604_subscription_upgrade_proration.sql`：重写 subscribe_with_points 三态（new 新建 / renew 同档顺延 / upgrade 立即生效+折算），降档守卫 LOWER_TIER_NOT_SUPPORTED；折算公式 credit=round(旧points_cost/(duration_months×30)×remaining_days_ceil)，应付=max(0,全价-credit)；返回 action/previous_plan_code/credit_applied/remaining_days，points_deducted=实际扣费；升级时作废全部 active 订阅（含历史并行低档 bug 产物）metadata 留痕
- `src/utils/api/subscription-api.js`：normalizeSubscribePayload 增 action/previousPlanCode/creditApplied/remainingDays
- `src/components/SubscriptionPlans.vue`：按钮四态（TIER_RANK + currentTopTierCode + withStatus），buildUpgradePreview/requiredCostFor/confirmPlanPreview/confirmDue，升级弹窗显示抵扣明细，续订显示"到期后顺延"，FAQ 更新（升级折算说明+续费说明），confirmSubscribe 以 RPC 返回 pointsDeducted 更新积分、按 action 分文案
- 测试：tests/integration/subscription-api.integration.test.js 增 upgrade/renew 归一化用例，10/10 通过；ESLint 无新增问题（存量 onBeforeUnmount warning）；vue-tsc type-check 通过
- 注意：SQL 折算按 30 天/月近似，年付 12 个月按 360 天；previous_plan_code 空串表示非升级（jsonb_build_object 不接受 NULL）

## 迁移版本号冲突修复

- db push 报 `duplicate key ... schema_migrations_pkey (version=2026082604) already exists`：我建的 `2026082604_subscription_upgrade_proration.sql` 与用户并行创建的 `2026082604_admin_subscription_grant_skip_existing.sql` 撞版本号（该文件已应用成功），且 `2026082605_admin_subscription_edit.sql` 也已存在
- 修复：重命名为 `2026082606_subscription_upgrade_proration.sql`，版本序列唯一
- 教训：写新迁移前必须 `ls supabase/migrations | sort | tail` 确认最新版本号，本项目存在多会话并行创建迁移的情况

## 会员内容勘察（探索 SubscriptionPlans.vue 会员展示内容）

- 会员内容唯一展示入口 = SubscriptionPlans.vue；MemberDetailModal.vue 是 developer 团队成员弹窗，与会员权益无关
- 内容层问题（P0 数据冲突 / P1 文案质量 / P2 加分项）：
  - P0: ① Cloud+ 口径需统一——features 写"X 张"(150/300/450/900/1200)且后端 PLAN_CLOUD_IMAGE_LIMITS 同用张数(用户认可此口径正确)；但对比表 comparisonRows 写"GB"(500MB/20GB/50GB)且 Plus/Pro/Max 同标 20GB 无区分，需对齐为张数或加换算说明；② 抽奖保底随档位递减(Plus24>Pro18>Max12>Ultra8)——用户澄清是设计意图：越高档保底门槛越低(8次即保底)、越易触发，非 bug；保留数字，建议文案点明"保底门槛越低=越易触发"以免用户误解
  - P1: ③ "Agent & Plan" 拼写无误但含义不清(应补"自动化工作流")；④ Hero"一个计划全都拥有"与5档矛盾；⑤ features 未写并行 Agent 数(仅对比表有 1/2/4/8)
  - P2: ⑥ 页面 Token 额度(20万~1000万)需核对后端 quota 实际下发值；⑦ 缺个性化档位推荐
- 本轮仅分析建议，未改代码；待用户决定是否动手修
- **已动手修复（用户确认"先改Cloud+ + 保底文案改成更容易保底"）**：SubscriptionPlans.vue ① comparisonRows 第173行 Cloud+ 行由 GB(500MB/20GB/50GB) 改为张数(150/300/450/900/1200 张)，与 features、后端 PLAN_CLOUD_IMAGE_LIMITS 对齐；② 卡片 features 四条保底文案加门槛标注（Plus 24次"门槛较高"/Pro 18次"门槛中等"/Max 12次"门槛较低"/Ultra 8次"门槛最低·最易保底"），点明越高档越易保底。仅前端文案，未动逻辑/后端。P1 ③④⑤ 与 P2 ⑥⑦ 仍未动，待用户后续决定
- **试用机制设计（用户要求出方案，未改代码）**：核心约束——user_subscriptions.status check 当前仅 `active/expired/cancelled`（建表 2026033003 + 多处 admin RPC 硬编码校验），试用需加 `trial`；注册 trigger `sync_profile_from_auth_user_insert` 只建 profiles 不发订阅（Free 是前端 alwaysActive 常量），故试用推荐"用户主动领取"而非注册自动发；等级 RPC `get_user_subscription_tier`(2026063004) 与前端 `subscription-benefits.js` 的 `isSubscriptionRecordActive` 都只认 status='active'，试用若不纳入则领了无权益。建议：① 新 RPC `start_subscription_trial(p_plan_code, p_duration_days=3)` 写 status='trial'/cost=0/expires=now+days/metadata{source:'trial'}，每账号一次（查 metadata.source 防重）；② status 约束加 'trial'；③ 改 get_user_subscription_tier + batch + subscribe_with_points 的"当前有效最高档"判定为 status in ('active','trial')，转正/升级时把 trial 置 expired+metadata{trial_converted_to} 新建 active（无折算因为 trial cost=0）；④ 前端 subscription-benefits.js 用新判定函数认 trial，保障权益生效；⑤ 复用现有八周年 claim 按钮范式（Home/index.vue）加"免费试用"入口 + 试用中倒计时 + 转正引导；⑥ 后台 useDataAdminHelpers.js 已预留 trial/trialing/active_trial 徽章(info)，无需改即可显示。待用户确认后落地
- **试用触发门槛（用户细化，确认）**：① 试用固定 = Pro 档 / 3 天；② 显示"免费试用"按钮的唯一条件 = 用户当前无任何有效订阅层级(即 Free) **且** 从未领取过试用(per-account-once)；③ 已有任何 active 付费订阅 → 不显示；④ 已试过(即使已过期回 Free) → 不显示(per-account-once 拦截)；⑤ 试用中(status='trial') → 不显示试用按钮，改显示"试用中·剩余X天 + 转正引导"。边缘情况（用户确认 v1 不拦）：曾付费已过期回 Free 且从未试用的老用户 → 按"当前无有效订阅"放行显示试用，不加 `has_ever_subscribed` 标志，待出现滥用再收紧。规格已锁定，待用户说"落地"即开工
- **试用机制已落地（2026-08-25 用户说"动手吧"）**：改动文件——① `supabase/migrations/2026082609_subscription_trial_support.sql`（版本号 2609 不撞车；动态 drop+重建 user_subscriptions.status check 加 'trial'；新 RPC `start_subscription_trial(p_plan_code,p_duration_days=3)` 写 status='trial'/cost=0/防重 via metadata.source='trial'/已有 active+trial 拦截；重定义 get_user_subscription_tier + batch + subscribe_with_points 的"当前有效最高档"判定为 status in ('active','trial')，转正/升级把 trial 置 expired+metadata{trial_converted_to}）；② `subscription-benefits.js` 新增 isSubscriptionRecordGrantingBenefits(active|trial & 未过期)，resolveCloudBenefitFromSubscriptions/resolveHighestTierCode 改用它（trial 用户拿完整权益）；③ `SubscriptionPlans.vue` buildActiveSubscriptionMap 纳入 trial 驱动卡片状态，新增 canShowTrial/isTrialing/trialEverUsed 计算属性 + handleStartTrial(调 startSubscriptionTrial) + handleTrialUpgrade(转正 Pro)，模板加试用横幅/转正引导横幅 + chip 试用中标识 + FAQ 试用条目 + 横幅样式；④ `subscription-api.js` 新增 startSubscriptionTrial 封装 + normalizeTrialPayload；⑤ `useDataAdminHelpers.js` BADGE_STATUS_MAP 加 trial:'info'；⑥ 测试 tests/integration/subscription-api.integration.test.js 新增 3 个 startSubscriptionTrial 用例。验证：vitest 13/13 通过、ESLint 0 错误(仅存量 onBeforeUnmount 警告)、vue-tsc type-check 通过。上线前需 `supabase db push` 执行 2609 迁移
- **试用落地后 Bug 自查（2026-08-25 用户要求"自我查错+交叉验证"），发现并修复 3 个真实 bug**：①【致命】边缘函数 `supabase/functions/api-key-vault/index.ts` 的 `resolveUserPlans`(约1478行) 用 `.eq('status','active')` 过滤，导致 trial 用户在所有档位解析路径(quota-status/handleQuotaStatus、runtime-chat 的 resolveRuntimeIdentity、checkTokenQuota、coding 模式门槛)被当 free——前端 subscription-benefits.js 认 trial 但真正发 Token 的边缘函数不认，试用用户实际只拿 20万 Token 而非 Pro 200万。已改为 `.in('status',['active','trial'])`（唯一档位解析入口，一处修复覆盖全部路径），并加注释。②【高级】`subscribe_with_points` 的 renew 分支在 trial→同档时 started_at=v_cur_expires_at 且 else 分支立即置 trial 为 expired → 转正后 3 天权益空窗（trial 死、付费 Pro 未开始）。已捕获 v_cur_status，trial→同档改为 action='convert' 且 started_at=now，立即生效无空窗。③【中级】试用中 Pro 卡片被 withStatus 误标"续费"（实为 trial），已改为 status='convert' 文案"升级正式版"；confirmSubscribe 加 convert 提示；FAQ"顺延生效"改为"立即生效"（与修复②一致）。验证：vitest 13/13、ESLint 0 error、vue-tsc 通过、SQL 配平 16/16。注意：边缘函数改动需 `supabase functions deploy api-key-vault` 重新部署；另有 Cloud+ 上传配额服务端 2026081903 仍按 status='active' 过滤（次级权益，建议后续核实是否需同步纳入 trial)
- **远程核查确认（2026-08-25 用户 push 后要求核实是否最新版 2609）**：用 `supabase db query --linked` 直查远程库，三处决定性证据均确认 push 的就是修复后最新版 2609 —— ① subscribe_with_points 函数体含 'convert' 分支(HAS_CONVERT)；② start_subscription_trial 函数已存在(计数1)；③ user_subscriptions_status_check 约束已含 'trial'(ARRAY[active,expired,cancelled,trial])。另：push 时一并应用的 2610/2611/2612 = pity_admin_undo / pity_batch_ops_list / pity_batch_undo（保底/抽奖后台操作），均不涉及 subscribe_with_points，不覆盖订阅修复。结论：用户'似乎不是最新版'的疑虑不成立，SQL 层已全部正确生效。⚠️ 待办：边缘函数 Bug A 修复（api-key-vault .in(['active','trial'])）仍在本地，需 `supabase functions deploy api-key-vault` 部署才生效——这是 push 迁移无法覆盖的

## 数据管理面板（DataManagement）UI / 操作性审查（只读，未改代码）

- 用户诉求：对网站数据管理面板给排版/布局/UI 建议，重点「更好的操作性」，出全面审查报告
- 产出：`docs/数据管理面板UI审查报告.md`（含 file:line 证据、P0/P1/P2 清单）
- 关键核实结论：① **安全性是强项**——deleteItem/batchDelete/closeLottery/banUser/muteUser/deleteModerationItem 全部经 useConfirmDialog 确认（useDataAdminMutations.js:116/151/289/637/761/602，共11处 dialog.confirm），封禁禁言还含理由 prompt；切勿在后续迭代误删该确认层；② **P0 操作性硬伤**：表格排序(`<th @click>` 无 aria-sort/键盘, DataAdmin.vue:637) 与行内编辑(`@dblclick` 仅鼠标, :696+) 对键盘/读屏不可达；③ **P0 视觉不一致**：主文件用16个手写`<svg>`但只引1个lucide，而控制台(如ApiKeyConsole)纯lucide——应统一；④ P1：`/`全局键在AdminHeader与useDataAdminShortcuts两处绑定(后者生效、前者因searchable=false空转属潜伏bug)；选择仅当前页无"全选所有结果"；备份进度遮罩内嵌工具栏(:311)应提升为顶层；DataAdmin.vue 5300行单文件建议拆分；⑤ 工具栏5+动作面叠加，建议⌘K命令面板+操作分层
- 待用户决定是否据此动手改
- **交叉验证复核（用户要求复核问题是否真实存在）**：重新独立 grep+读码复核，原报告结论全部属实。更正一处：focus-visible 并非普遍缺失——`.btn`(console.css:825)/`.icon-btn`(console.css:1298) 已有，缺失的是 `.review-btn`/行内编辑按钮/`.fab-button`/单元格（DataAdmin.vue 自身 `<style>` 完全无 :focus-visible）；"三套搜索"精确化为"两套数据搜索(`searchQuery`模块内 + `globalSearchQuery`跨表) + 侧边栏模块过滤框错误复用 `globalSearchQuery` 变量"。已把更正追加进 `docs/数据管理面板UI审查报告.md` §8 并更新 P1-10 措辞。

## 发帖 UI Demo 测试页（已生成）

- 用户诉求：做一个发帖各种 Demo UI 的测试页，主要是加载 UI 和发送成功等状态，可预览，要求「网站里已有的 UI 一模一样」复刻进去。
- 产物：`post-ui-demo.html`（项目根目录，自包含静态页，已起 `python3 -m http.server 8731` 预览）。
- 复刻来源（1:1 取真实 CSS）：`src/views/Forum/components/PostComposer.vue` + `src/views/Forum/styles/composer.css` + `src/views/Forum/styles/replies-responsive.css`（mini-spinner / mobile-composer-submit）+ `src/components/CommonAlertModal.vue` + `src/views/Forum/styles/base.css`（.glass-panel）+ `src/styles/common/tokens.css`（--ease-out 等）。
- 覆盖状态：① 编辑器常态 ② 提交浮层进度条(post-submit-overlay 百分比+蓝条) ③ 发布按钮内进度条(is-staged-submitting) ④ 按钮 Spinner(isSubmitting 白 spinner) ⑤ 图片上传进度(循环滑动条) ⑥ 图片状态徽章(处理中/已检测/未通过) ⑦ 移动端发布按钮进度 ⑧ 发布成功弹窗 ⑨ 发布失败弹窗。进度动画用 rAF 模拟 0→100% 循环，可「重新播放」。
- 注意：图标用内联 SVG symbol 近似 lucide（hash/eye/image/file/more），非原始 lucide 组件；其余样式为源码原值。

## 数据管理面板 P0+P1 落地（已实施 + 交叉复核通过）

- 用户指示"先做 P0 和 P1，只要不拆分文件"。已全部实施（跳过 P1-9 拆分 DataAdmin.vue）：
  - P0-1 排序 `<th>` 加 `:tabindex`/`:aria-sort`/`@keydown.enter+space`（DataAdmin.vue ~615）
  - P0-2 行内编辑 5 类可编辑单元格加 `tabindex=0`/`role=button`/`aria-label`/Enter·Space 启动 + 悬停 pencil 按钮 `.cell-edit-trigger`（编辑态隐藏）
  - P0-3 18 处手写 `<svg>` 全部替换为 lucide（Plus/Search/Filter/Trash2/Download/Pencil/Copy/Check/X），raw svg 归零
  - P1-4 移除 AdminHeader.vue 空转的 `/` 监听（handleKey 已删）
  - P1-5 `.g-module-tabs` 原本就有 overflow-x:auto，仅补 overscroll/touch
  - P1-6 新增 ⌘K/Ctrl+K 命令面板（过滤+↑↓+Enter+Esc，含 50+ 模块切换），工具栏加「命令 ⌘K」入口
  - P1-7 新增跨页全选：`selectAllResultsMode` + 「全选所有结果(N)」按钮 + `.selection-all-banner` 横幅 + 表头复选框语义化
  - P1-8 备份进度遮罩从 toolbar-right 移出到模板根部 + `role=dialog`/`aria-modal`
  - P1-10 补 `.review-btn`/`.inline-edit-action`/`.fab-button`/可编辑单元格 `:focus-visible`（console.css）
  - 搜索解耦：移除 `:search-query="globalSearchQuery"` 双向绑定，侧边栏过滤框不再污染跨表搜索
- 校验：`scripts/verify-sfc-parse.mjs`（parse+compileTemplate+esbuild）通过；`npm run type-check` 0 错误；ESLint 0 errors（49 条既有 warning 非本次引入）。修复过程中发现 `.cell-edit-trigger` 误用未定义 `--surface`，已改 `--muted`。
- 报告已追加 §9 实施与复核记录（docs/数据管理面板UI审查报告.md）。

## 数据管理面板全面优化 4 阶段（已实施，用户要求"都做+不拆文件+复核+测试"）

- **阶段1 视图层**：通用视图切换器（viewMode=table/card/kanban/timeline，config 驱动 + localStorage 按表记忆 + defaultView）；看板（kanbanViewConfig 推断：kanban.statusKey>cardView.statusKey>status select 字段，已给 reviewPosts/reportedPosts/reviewComments/lotteryFulfillments 加显式 kanban 配置）；时间线（timelineViewConfig 自动推断 datetime 列，日志表自动可用）；卡片已覆盖 19 张表（新补 coreMemories），核心是修正认知：卡片机制本就 config 驱动且大范围配置。
- **阶段2**：通用状态分布条（statusBreakdownForCurrentTab 复用看板列+下钻筛选）；引用面板（行操作"关联"按钮 + relatedJumpsForItem 复用 RELATED_JUMP_MAP）；批量操作确认已完备无需改。
- **阶段3**：EditDrawer 加焦点陷阱 trapFocus。虚拟滚动与脏数据确认/草稿**暂缓**（分页已缓解；编辑状态在父组件深比较易误报——诚实标注待办）。
- **阶段4**：base.css 新增语义 token（--success/-info/-warning/-danger + *-soft，三套）；console.css 批量收敛 65 处硬编码色值；毛玻璃（.g-topbar/.drawer/.command-palette/.backup-progress-panel 加 backdrop-filter+color-mix 半透明）。⚠️ 深色 --primary 是品牌红 #fc2c50，token 化后相关控件深色下变品牌红（预期行为，需点验）。
- 配置约定（tables.js）：defaultView / kanban:{statusKey,statusMeta} / timeline:{dateKey,titleKey,bodyKeys} / cardView。
- 校验：SFC+esbuild 通过、vue-tsc 0 错误、ESLint 0 errors、vitest 全量通过。报告已追加 §10。
- 待办：虚拟滚动、抽屉脏数据确认/草稿、剩余 46 处中性色 token、独立主从详情。

## 发帖有奖活动（可配置运营活动，已落地代码）

- 用户诉求：发帖即可领积分，且要和数据管理面板对接（可配置"何时发帖得多少积分""每月一次"等）。决策：① 手动每期建活动（管理员每月开一期）；② 每日+每月双上限防刷；③ 复用通用表格 tab 接入管理面板；④ 可选论坛横幅。
- 落地文件：
  - `supabase/migrations/2026082701_post_reward_campaigns.sql`：表 `post_reward_campaigns`(id bigint identity, title, start_at/end_at timestamptz, points_per_post int, daily_limit/monthly_limit int 默认0, status text 默认'active') + `post_reward_claims`(user_id/post_id/campaign_id, 唯一索引(user_id,post_id) 幂等) + RPC `grant_post_publish_reward(p_post_id uuid)`(security definer：校验归属→查进行中活动→幂等占位→月/日上限校验→update profiles + 回填 claims + 写 points_transactions reason='forum_post') + `get_active_post_reward()`(返回进行中活动供横幅)。RLS：管理员 all + 登录 select。
  - `src/utils/api/forum/post-api.js`：新增 `claimPostPublishReward(supabaseClient, postId)` 与 `getActivePostReward(supabaseClient)`。
  - `src/views/Forum/ForumMain.vue`：发帖成功钩子里 `claimPostPublishReward(supabase, createdPostId)`（**修复上轮 bug**：曾漏传 supabase 客户端）；挂载时 `loadActivePostReward()`，把 `activePostReward` 传给 PostComposer。
  - 管理面板四件套：`config/tabs.js`(operations 加 postReward tab + TABS_ACTIONS)、`fields.js`(TAB_WRITABLE_FIELDS + POST_REWARD_STATUS_OPTIONS)、`tables.js`(postReward 表定义, columns/fields/cardView, datetime 用 type:'datetime')、`saveStrategies.js`(校验 end>start/points>0/上限≥0, toISOStringFromInput 处理时间)。
  - `src/views/Forum/components/PostComposer.vue`：新增 `activePostReward` prop + `postRewardBannerText` 计算属性（「发帖得 X 积分（至 MM-DD）」）+ 签到面板旁横幅元素 + 样式（含深色）。
- 校验：ESLint 0 error（仅存量 warning）；vue-tsc type-check 后台运行中待确认。
- ⚠️ 上线前待办：`supabase db push` 执行 2026082701 迁移；管理员需在 `/admin/data-management` 运营模块手动建活动并置 status='active' 才生效。
- **横幅改版（用户选方案2）**：把发帖框内的横幅(`PostComposer.vue` 内 `.post-reward-banner`)撤掉，改为论坛页**顶部常驻活动条**——`ForumMain.vue` 在 `</header>` 后插入 `<transition>`+`v-if="postRewardBannerText"` 的 `.forum-post-reward-banner`，`position: sticky; top:12px`(滚动跟随，灵动岛胶囊风格，毛玻璃)。`postRewardBannerText` 计算属性从 PostComposer 上移到 ForumMain(基于 activePostReward)。样式：base.css 新增 `.forum-post-reward-banner`(+ fade 过渡)，forum-dark.css 加深色覆盖。已移除 PostComposer 的 prop/computed/模板/CSS。
- **⚠️ 白屏事故 + 终版定稿（用户推翻顶部条，只要发帖成功灵动岛）**：报错 `forum-api.js does not provide an export named 'getActivePostReward'` → 白屏。根因：上轮把 `claimPostPublishReward`/`getActivePostReward` 加进 `post-api.js` 但**没同步到 re-export 桶 `src/utils/api/forum-api.js`**(所有调用方都从桶导入)，且 ForumMain 调 `claimPostPublishReward` 时根本没导入它(第二处隐患，发帖成功会 ReferenceError)。修复：① `forum-api.js` 桶补导出两函数；② ForumMain 导入块 `getActivePostReward` 换成 `claimPostPublishReward`；③ 按用户要求**整体撤掉顶部常驻条**——移除 ForumMain 的 `activePostReward` ref/`loadActivePostReward`/`postRewardBannerText` computed/onMounted 调用/模板 transition 块，及 base.css `.forum-post-reward-banner` 样式与 forum-dark.css 深色覆盖；④ `getActivePostReward` API 导出保留(无消费者，无害)。最终形态：**只有发帖成功灵动岛**——发帖成功弹 `发帖成功`(icon=post)，领到积分时弹**不同的岛** `发帖得积分`(icon=gift，ForumMain 2748 起 claimPostPublishReward 回调驱动；embedded 模式走 showEmbeddedSuccessIsland，独立页回退 showModal)。校验：ESLint 0 error、vue-tsc 通过。
- 玻璃态交互收尾（用户"优化"指令）：核查发现抽屉滑入动画（overlays.css modalIn/modalOut）与卡片 hover 微光原本已存在；本次补：看板卡片 hover（translateY+shadow）、时间线项 hover（translateX+shadow）、命令面板专属过渡 palette（scale 0.97+fade，替代 toast 过渡）。SFC 校验通过。
- **管理面板加"生命周期"徽章（用户确认"可以"）**：给 postReward tab 增加派生状态展示——即将开始/进行中/已停用/已结束，一眼看出每期活动所处阶段。实现（关键在**不改 statusKey/筛选**，避免服务器端按不存在列查询）：① `tables.js` postReward 加 `rowDecorator`（按 start_at/end_at/status 派生命中周期中文标签）、表格列 `{ key:'lifecycle', label:'生命周期', type:'badge', virtual:true }`、cardView.meta 加生命周期条目；② `DataAdmin.vue` fetchTabData 在 flatten 后、**写入缓存前**统一应用 `dataConfig[tabId].rowDecorator`（缓存行已含派生字段）；③ `DataAdmin.vue` allowedAdvancedFields 改为 `.filter(c=>!c.virtual)`（两处：主查询+lotteries 兜底）——**防高级筛选对 virtual 字段拼 SQL 查不存在的库列**；④ `useDataAdminHelpers.js` BADGE_STATUS_MAP 加 upcoming/ongoing/paused/ended + 中文键（即将开始=info/进行中=success/已停用=warning/已结束=muted）。衍生约定：**列配置支持 `virtual:true` 标记**（只读派生列，不进高级筛选白名单）。校验：ESLint 0 error、rowDecorator 逻辑 node 模拟 4 态全对、vue-tsc 通过。
- **⚠️ 新增记录面板看不到（用户报障，根因=注册遗漏第5处）**：`query-config.js` 里 postReward 出现 0 次——`TAB_SELECT_COLUMNS` 无该 tab 的 select 列，`fetchTabData` 里 `if (!table || !selectColumns) return;` **静默 return 不查库**，所以新增了记录列表始终为空且无报错。修复：query-config.js 补全 6 处——STATUS_FILTER_FIELDS.postReward='status'、DATE_FILTER_FIELDS.postReward='start_at'、TAB_SELECT_COLUMNS.postReward='id, title, status, points_per_post, daily_limit, monthly_limit, start_at, end_at, created_at'、TAB_DEFAULT_SORT.postReward={column:'start_at',ascending:false}、TAB_SORT_COLUMNS.postReward=new Set(...8列)、TAB_SEARCH_FIELDS.postReward=[id/title/status/start_at/end_at]。**教训：在管理面板加新 tab = 六处注册（tabs.js/fields.js/tables.js/saveStrategies.js/query-config.js 五文件），缺一即静默失效或白屏，务必逐文件核对。** 校验：ESLint 0、type-check 通过。

## 发帖有奖 · 排查"发了帖没得积分"（进行中）

- 用户报"发帖没获得积分"。远程库直查证据：① 表 `post_reward_campaigns`/`post_reward_claims` 已部署（迁移已 push）；② RPC `grant_post_publish_reward`/`get_active_post_reward` 存在；③ 活动 id=1「8月总结」status=active、points_per_post=5、start_at=08-25 14:17、end_at=08-31 14:17、**in_window=true**（活动配置完全正确，处于进行中）；④ **但 `post_reward_claims` 表完全空、`points_transactions` reason='forum_post' 完全空** → RPC 从未执行到插领取记录那步；⑤ 确有发帖：post 29540ce1「夏天要过完了吗」author 00ac36b4、status=approved、created_at=08-25 14:25（在活动窗内）。
- RPC 不发积分的 5 种返回（均 awarded=0、前端静默不弹岛）：NOT_AUTHENTICATED / POST_NOT_FOUND_OR_FORBIDDEN / NO_ACTIVE_CAMPAIGN / ALREADY_CLAIMED / MONTHLY_LIMIT / DAILY_LIMIT。已排除 NO_ACTIVE_CAMPAIGN（活动在窗）。
- 静态核查前端链路：createPost 纯文字路径返回 `{ ok, data:[post], error }`，`result.data?.[0]?.id` 能取到 post id；claimPostPublishReward 实现正确（`rpc('grant_post_publish_reward',{p_post_id})`）。**最可能根因**：用户 22:25 发帖，修 `claimPostPublishReward` 导入 bug 在 22:20 左右——发帖时浏览器跑旧代码，`claimPostPublishReward` 未定义 → ReferenceError 被 `.catch` 吞掉 → 帖入库但积分没发、无报错提示、claims 空。完美吻合证据。
- 已加诊断日志：ForumMain.vue 2763 `.then` 开头加 `logger.info('forum','发帖奖励诊断',{createdPostId, reward})`（之前只在 awarded>0 才有反馈，其余全静默=诊断盲区）。等用户强刷浏览器 + 复发一帖，看控制台 `[forum] 发帖奖励诊断` 的 reward 内容定位（若 reward.ok=false 看 message；若 .catch 触发"发帖奖励发放失败"则是 ReferenceError/网络）。

## 网站性能全面审查（只读，未改代码）

- 产出：`docs/性能审查报告-2026-08-25.md`
- 方法：源码勘察 + dist/stats.html 产物分析 + Playwright 实测（vite preview:4199，移动端视口，未登录态，测首页与 /#/user-space）
- 确认问题（均经源码+产物+实测三方交叉验证）：
  - P1 PostCard 逐卡调 get_user_subscription_tier RPC（实测首屏 4 次；批量接口 fetchUserTiersBatch 已存在于 useUserTier.js 但 PostCard 未用；RPC 失败不写缓存导致每次渲染重试）
  - P2 /user-space 默认 tab 是 posts（UserSpaceMain.vue:1736 resolveAccessibleTab 兜底 'posts'），与 watch 处注释"默认标签（profile）"不一致；未登录打开即拉论坛全套（实测 45 JS + 11 supabase 请求含 posts/周报/广告）
  - P3 首页 LCP 三跳：JS 执行 → home_heroes RPC → hero 图 146KB eager 无 srcset（二访有 localStorage SWR 缓存，首访无降级）
  - P6 compress-images 仅 build:full 跑，普通 build 跳过（实测 xnbag-1.webp 源=产物 297KB）
  - P4 巨型组件（ForumMain.vue 3808 行 / AssetsHubPanel 3108 行 / UserSpaceMain 2686 行）+ CSS 解析成本（ForumMain 304KB 未压缩但 gz 仅 49KB；DataAdmin 575KB/gz 77KB）
  - P5 PWA 预缓存全量 96 个 CSS（~2.5MB 未压缩）首装成本
- 交叉验证排除的疑点：主题×UI风格组合选择器仅占 CSS 2-3%非膨胀主因；sw.js 预缓存正常（单行压缩格式导致初判失误）；supabase-vendor 动态加载不阻塞首屏；UserSpace 数据层 TTL 缓存+abort+token 齐备
- 关键数据：首屏链约 125KB gz JS + 20KB gz CSS；实测 CLS=0、无 Long Task；BOHAIMain 116KB gz 仅 AI tab

## 性能审查报告 P5 建议撤回（缓存安全复核）

- 用户提出历史事故：发布后页面缓存丢样式（已修复过）。核实确认 vite.config.js:108-111 注释与 index.html:91-96 bohAssetRecovery 脚本正是该事故的防御层——全量 CSS 预缓存是**故意设计**非疏漏
- 撤回 P5（PWA 预缓存收缩）建议：会在"旧应用壳+新部署"窗口期重新打开 404 丢样式失败模式；SWR 运行时缓存会引入"新 HTML+旧 CSS"错配变体
- 已更新报告：P5 标记撤回，新增"六、缓存安全红线"章节（不变式：SW 服务的任何版本 index.html 其引用的全部 CSS 必须同步可得；禁止收缩 CSS 预缓存/给 CSS 配 SWR/加长 sw.js 缓存 TTL）
- 用户对缓存相关改动高度敏感，后续所有涉及 SW/缓存的优化建议必须先对照该红线评估

## 性能优化 P1/P2/P3/P6 落地（用户确认后实施，含预存 bug 修复）

**P1 tier 批量查询**：useUserTier.js fetchUserTiersBatch 加跨方法去重（预注册 tierRequestInFlight，让并发 fetchUserTier 命中 in-flight 不发单发 RPC；预注册必须在任何 await 前同步完成，否则子组件挂载抢先）；ForumMain.vue 帖子加载后批量预取 author tiers（fetchForumData + 快照恢复两条路径）；PostCard.vue 不变。实测 tier RPC 4 次→1 次。
- ⚠️ 暴露预存 SQL bug：批量 RPC get_user_subscription_tiers 返回 400（column "user_id" ambiguous，plpgsql 输出变量与列名冲突，42702），useTierMap 批量路径从未生效一直 fallback 单发。修复迁移 2026082702_fix_get_user_subscription_tiers_ambiguous.sql（所有 user_id 引用加表别名限定 s./r./u./b.），用户已 db push，curl 验证 200。
- 踩坑：首次复测仍 4 单发+1 批量，误判为时序问题改了预注册位置（其实位置本就对），根因是 RPC 400 触发 fallback 逐个单发。教训：先验证 RPC 本身可用再调前端时序。

**P2 注释修正**：UserSpaceMain.vue watch 注释改诚实，发现真实不一致——onMounted 走 posts 兜底、watch 走 profile 兜底，已标注待产品决策，不改行为。

**P3 hero 响应式**：HomeOverlayHero.vue 加 imageSrcset/imageSizes prop；BlockWallHero.vue 生成 390/768/1280 三档变体（sharp quality 72）+ srcset。实测首页图 222KB→113KB（省 49%），移动端选 1280w(64KB) 而非原 1920w(146KB)。

**P6 压缩幂等**：compress-images.mjs 加已处理清单（.compress-images-cache.json，size 签名可跨机器移植），只压新增/变更图避免代际损质；package.json build 主链接入 compress-images（build:full 降级为别名）。一次性省 2.11MB，二次构建 0 处理。

**验证**：vue-tsc 0 错误、ESLint 0 error（32 存量 warning 非本次）、vitest 106/107（1 预存失败 useBottomNavIslandQueue.js 文件缺失与本改动无关）、构建通过、Playwright 实测前后对照。

**改动文件**：package.json、scripts/compress-images.mjs、src/composables/useUserTier.js、src/views/Forum/ForumMain.vue、src/views/Home/components/HomeOverlayHero.vue、src/views/Home/components/BlockWallHero.vue、src/views/user-center/UserSpace/UserSpaceMain.vue(注释)、supabase/migrations/2026082702_*.sql、新增 src/assets/images/2024-1-fangkuai-{390,768,1280}.webp、新增 .compress-images-cache.json、大量 src/assets webp 被压缩。

## 暂存更改全面 Bug 审查 + 全部修复（2026-08-25 晚）

- 用户要求：先 `git add -A` 暂存全部更改，再对暂存内容做全面 Bug 审查（含交叉验证），最后「全部修复」。
- 审查范围：12 个 SQL 迁移（2602-2612、2701-2702）+ 新增/修改的 Vue 组件、API、composables、配置、测试。每个问题均从代码逻辑/数据流/调用链路三角度交叉验证。
- 确认 4 个 bug 并全部修复：
  1. **【Critical】billing_cycle 约束冲突** — `2026082609 start_subscription_trial` INSERT 写 `billing_cycle='trial'`，违反建表约束 `check (billing_cycle in ('monthly','yearly'))`（2026033003:8），试用订阅永远无法落库。修复：改为 `'monthly'`（status 仍为 'trial' 表达试用态），并加注释。
  2. **【Medium-High】批量等级查询回退丢失 trial** — `2026082702 get_user_subscription_tiers` 重写时从旧版复制 `status='active'`，回退了 2609 加入的 `status in ('active','trial')`；注释虚假声称"逻辑不变"。后果：单用户 tier 含 trial，批量 tier 不含，行为分裂。修复：改回 `in ('active','trial')`。
  3. **【Medium】保底批次撤销后可单用户双重撤销** — `admin_undo_pity_batch`(2612) 只写顶层 `undone=true`，不更新 details 内 per-user 标记；`admin_undo_pity`(2610) 对批量日志只检查 per-user undone 不查顶层 → 批次撤销后单用户撤销仍命中同批日志，若期间抽奖则腐蚀 pity 数据。修复：admin_undo_pity WHERE 加 `coalesce((l.detail->>'undone')::boolean,false)=false` 顶层守卫。
  4. **【Low】converted_from_trial 误标** — `subscribe_with_points`(2609) metadata 条件 `v_cur_id is not null and v_action<>'upgrade'` 对 'renew'(active 续订)也置 true。修复：改为 `v_cur_status='trial'`。
- 校验：vitest 13/13 通过。改动文件：2026082609（2 处）、2026082702（1 处）、2026082610（1 处）。
- ⚠️ 关键：2609/2702/2610 三处 SQL 修复均为 `create or replace` 函数体改动，**必须重新 `supabase db push`** 才能在远程库生效；本地代码修复不会自动同步到远程。

## 帖子编辑功能重构（已落地，用户指令"深入洞察代码开始落地"）

- **背景**：原编辑=弱模态框（标题 input + 6 行 textarea，PostDetailMain.vue 1572-1587），无图片/标签/毛玻璃。用户要求：点击编辑 → 竖屏全屏毛玻璃 UI、与发帖 PostComposer 一致；横屏用弹窗；提示词去掉"异步完成审查"。
- **关键勘察结论**：PostComposer 早已预留 `editMode`/`existingImages` props 但从未被使用；`forum_post_images` 表 insert/update/delete 仅授予 service_role（客户端不能直接增删改，必须走 security definer RPC）；`updatePost` 只更新文本不处理 tag/图片。
- **改动文件**：
  1. `supabase/migrations/2026082801_update_forum_post_images.sql`（新）：RPC `update_forum_post_images(p_post_id uuid, p_images jsonb)` security definer——校验作者/admin；删除被移除旧图记录；插入新图（复用 create RPC 的 URL/public_id/尺寸安全校验，moderation approved）；保留图按传入顺序重排 sort_order；同步 posts.image_count/cover_image_url。版本号 2801 不与现有冲突（最新 2703）。
  2. `src/utils/api/forum/post-api.js`：新增 `updateForumPostImages(postId, images)`（rpc 调用 + RPC 未部署降级提示 + 解析业务错误码 "CODE:message"）；`src/utils/api/forum-api.js` 桶补导出。
  3. `src/views/Forum/components/PostComposer.vue`：editMode 渲染——头部问候语变"编辑帖子/修改你的分享，记得保存～"+关闭按钮（新增 `close` emit、`.editor-close-btn` 样式），标题 placeholder"修改标题"，发布按钮文案"保存修改/保存中…"（submitButtonLabel computed），隐藏周签到面板/保存草稿按钮/mobile+desktop 更多菜单。
  4. `src/views/PostDetail/PostDetailMain.vue`：删除弱编辑框；编辑状态集（editNewPost/editSelectedPostTag/editPostImages/editRemovedExistingIds/isEditUploadingPostImage/editImageUploadStatus/isEditPortrait）；`isEditPortrait = width<=1024 && height>=width`（竖屏全屏/横屏弹窗，resize 监听 onMounted/onUnmounted）；PostComposer 编辑层（Teleport + `.post-edit-overlay`/`.post-edit-shell`，overlay 毛玻璃 backdrop blur 24px，竖屏全屏 padding 0，横屏 max-width 720px 居中弹窗 + 入场过渡 edit-composer-fade）；图片增删排（enqueueEditImageUpload 逐张 uploadForumImage、failed 拦截、删除已有图只记 id 不动 Cloudinary 资产避免破坏 Cloud+ 引用）；submitEditPost = updatePost + updateForumPostImages + posts.tag 回写 + 发 `boh:forum-post-updated` 事件 + 成功文案"帖子已更新"；取消有未保存确认（requestConfirm）。
  5. 提示词清理：PostDetailMain "帖子已更新，系统将异步完成内容复审"→"帖子已更新"；ProfileMain.vue:1703 "…同步至社区，系统将异步完成内容审查"→"…同步至社区"；useActionDraft.js 177/178/324 三处"…异步完成内容审查"→"…已发布成功。"
- **验证**：@vue/compiler-sfc 三个 SFC script 编译通过；vite build --outDir /tmp 成功（14.88s，无错误；直接 build 会被沙箱 dist 清理保护拦截，需 --outDir 临时目录）。
- ⚠️ 上线前：`supabase db push` 执行 2026082801 迁移；图片帖 Cloud+（boh_cloud_entries）联动在编辑场景暂未做（新图不会同步进 Cloud+ 条目，已接受为 v1 简化）；`update_forum_post_images` 受表约束 sort_order<3 影响时行为与发帖一致。
- **编辑按钮消失 Bug 修复（用户报"编辑帖子没看到发布按钮"）**：根因 = `replies-responsive.css:1011-1015` 在移动端模式 `.mobile-composer-section .editor-footer { display:none }`（发帖时由 ForumMain 外层 mobile-composer-bar 承担发布按钮），而编辑层竖屏时传了 `is-mobile-composer=true` 进入该模式 → footer（含"保存修改"）整个被 CSS 隐藏。修复：PostDetailMain 编辑层竖屏加自建顶栏 `.post-edit-bar`（取消/编辑帖子/保存，毛玻璃胶囊风格，与 mobile-composer-bar 一致），shell 改 flex column + 顶栏固定 + `.post-edit-scroll` 内容滚动；横屏仍走 PostComposer 自带 footer；竖屏时隐藏 PostComposer 内 close 按钮避免与"取消"重复。教训：PostComposer 移动端布局依赖外层容器提供提交按钮，复用组件时若外层无该按钮必须自建。
