# 2026-09-21 工作日志

## 安全审计报告核验 + 全量安全复检

产物：`docs/2026-09-21-安全审计报告核验与全量复检.md`

### 核验结论
- **不存在独立安全审计报告**，安全内容散在 `docs/boh-full-health-check-2026-06-26.md` 第一章（72/100）+ 第七章 + 5 个 `*_security_hardening.sql` 迁移里。
- 那份 6-26 报告**主体已失效（利好方向）**：报告标「⏳ 迁移待执行」的 P0-3/P0-4 实际都已执行；S-1 九项里 8 项已修、1 项作废（`messages` 表已不存在，报告里的 `addresses` 已改名 `user_addresses`）；S-2/S-3/S-4 已修。
- 仍属实的只有：S-6（session 在 localStorage，`supabase-client.js:18/112`）、检查表 #11（上传安全，原判「通过」偏乐观）。

### 本轮新发现（报告 13 项检查一项都没覆盖）
- **P0** 匿名可枚举用户名→邮箱：`resolve_email_for_login` anon 可调且返回明文邮箱。
- **P0** `cron.job#8` 明文内嵌 41 字符 `sb_secret_`（service_role 等价）；仓库与 Git 全历史已核**干净**（`git log --all -S` 命中的 2 提交是文档占位符）。
- **P0** anon 可 EXECUTE 232 个函数 / 149 个 SECURITY DEFINER / **33 个无守卫非触发器**；根因是 Supabase 默认给新建函数授 anon EXECUTE，`2026090906_rpc_revoke_anon` 未覆盖存量。`admin_*` 一类实测有 `current_user_is_admin()` 门（不可利用，但授权应收窄）。
- **P1** 75/88 张 public 表仍给 anon 表级 INSERT/UPDATE/DELETE（RLS 是唯一的门）。
- **P1** Cloudinary `upload_preset` 无签名且内联进 dist → 绕过 `assert_cloudinary_upload_allowed` 客户端预检直传。
- **P2** 线上零安全响应头（实测无 CSP/HSTS/X-Content-Type/Referrer-Policy/X-Frame/Permissions-Policy）。
- **P2** `.or()` 转义口径两套：`profile-api.js:384` 有转义，`ProfileMain.vue:1367` 没有。

### 修复策略关键约束（决定「撤 anon 权限」不能一刀切）
公开路由（`meta.requiresLogin` 缺省即公开）实测包含：`/forum`、`/forum/post/:id`、`/newsroom`、`/news/:id`、`/activities`、`/activities-wall`、`/lotteries`、`/block-wall`、`/profile/:username`、`/birthday`、`/lab`、`/shop`、`/gift`、`/ai-chat`、`/shows`、`/creator-studio` 等。
→ **游客会真调 RPC**（如 `/birthday` 调 `get_recent_birthday_profiles`、`/login` 调 `resolve_email_for_login`）。所以「`revoke execute on all functions from anon` 一刀切」会打挂所有公开页。
→ 正确顺序：**先按函数补门（函数体内，零破坏）→ 再收授权**；且收授权前必须用「未登录实测抓包」拿到准确的公开调用清单，不能靠静态推断（路由公开 ≠ 页面不调登录态 RPC，组件 mount 时会无条件调用）。
→ 相对安全的一条是 `revoke insert,update,delete on all tables in schema public from anon`：写操作（点赞/发帖/抽奖/上报/装扮购买）全部需要登录，anon 无合法写路径。

### 方法论沉淀（两条踩坑）
- **反证救了一次误报**：先假定 `resolve_email_for_login` 的限流键取自可伪造的 `X-Forwarded-For`，设计 A/B 对照实验——A（固定伪造头 11 次）第 11 次被拦、B（每次换伪造头）**仍全部被拦** → 网关覆写该头，限流按真实 IP，**假设被实验推翻**。凡「限流/鉴权可绕」类结论必须做对照实验再写。
- **只能靠目录快照判现状**：`has_function_privilege('anon', p.oid, 'EXECUTE')` + `pg_get_functiondef` 文本搜 `auth.uid()` / `current_user_is_admin()` 可批量分类 232 个函数；只看迁移文件会得出错误结论（revoke 过一次 ≠ 全覆盖）。
- 只读探针可用 `is_avatar_frame_owned_by(随机UUID,'nope')` 这类函数证明匿名可达而不污染生产；写类函数（`insert_moderation_log` / `increment_ad_clicks` / `reserve_ai_*`）**没有做活写入探针**，结论边界已在报告第五节写明。

---

## 修复实施（同一日，用户授权后执行并逐项测试）

### 1) `resolve_email_for_login` 去邮箱化 —— 代码完成，待部署
- 根因：`auth-login` EF 里的 `resolveEmailFromLoginId` 查 `profiles.email`，该列已在 `2026090911` drop → **EF 是坏的**，所以前端才一直用 RPC。前端**从未调用过该 EF**。
- 改动：
  - 新增 `supabase/functions/_shared/resolve-login-id.ts`（抽出以便可单测，因 index.ts 含 `Deno.serve` 无法直接测）：profiles 只查 `id, username`，邮箱经 `auth.admin.getUserById()` 取。
  - `auth-login/index.ts` 删除本地实现，改引用共享模块。
  - `src/utils/api/auth-api.js` `signIn()`：用户名登录走 EF，用 `setSession({access_token,refresh_token})` 采纳会话；**仅当 EF 404/501/网络错误时**降级到旧 RPC（5xx 不降级，避免静默复活枚举面），开关 `LOGIN_LEGACY_RPC_FALLBACK`。
  - 删除 `getEmailByUsername`（含 `utils/auth.js` 再导出与 `auth.d.ts` 声明）。
- 测试：`tests/unit/resolve-login-id.test.js` 24 通过、`tests/unit/auth-signin-edge-gateway.test.js` 24 通过；各做反证（改回老行为 → 8 红 / EF 分支短路 → 10 红），恢复后全绿。全量 120 文件 1973 用例全绿；`vue-tsc` 干净；`vite build --outDir dist-check` 通过。
- ⚠️ 测试过程中我一条断言写错了前提（断言「返回值不含 email」，实际 `setSession` 会返回调用者**自己**的会话含其邮箱——属设计如此）。改为守正确的不变量：**认证失败路径绝不携带任何邮箱**。
- ⚠️ **生效前置**：要先 `supabase functions deploy auth-login`，再发前端。CI **不部署 EF**（`.github/workflows` 无 `functions deploy`）→ 手工部署。顺序无关（有降级）。

### 2) 迁移 `2026092101_revoke_anon_execute_and_table_writes` —— **已应用**
- 结果：anon 可执行 224→**160**（撤 64 个函数）、anon 可写表 75→**0**；authenticated 表写权限 **75 不变**、可执行 239；service_role 仍可调 `reserve_ai_token_quota`。
- 关键实证（推翻了旧口径）：**只 `revoke ... from anon` 无效**——函数 EXECUTE 同时有 PUBLIC 授权，anon 经 PUBLIC 继承，`has_function_privilege` 仍为 `t`；必须 `revoke ... from anon, public`。表级授权是显式给 anon 的（无 PUBLIC 份额），故只需撤 anon。
- ⚠️ **`current_user_is_admin` 绝不能撤 anon**：它被 RLS 策略引用，策略按查询者角色求值，撤了会让游客查询直接 42501。已用程序把「被策略引用的函数」算出并排除。
- 策略选择：**优先「撤 EXECUTE」而非「加函数体内门」**——撤权对攻击者 fail-closed、不会误伤 service_role 路径；`check_rate_limit` 是 fail-open，加门反而会静默废掉限流。
- 库内调用关系必须查（决定能否加门）：`log_forum_rate_limit_event` / `record_lottery_join_attempt` / `refresh_forum_post_counters` 被**普通用户路径**（发帖/评论/点赞/抽奖）调用 → 只能撤权不能加门。
- ⚠️ 本机 `supabase db push` 不可用（5432 被沙箱阻断 `tls error EOF`）→ 改走 Management API（`read_only:false`）应用语句 + 手工写 `supabase_migrations.schema_migrations` 行（列：`version/name/statements text[]`）。语句与历史记录同一事务，失败整体回滚。
- 验证：① 应用前把整个迁移在事务里跑一遍 + `raise` 强制回滚（验语法与效果，不留痕）；② 匿名 RPC 探针应用前/后差分——公开页所需 RPC **全部不变**，攻击面全部转 `42501`；③ 表级匿名 `DELETE ... ?id=eq.<不存在>` 由可达变为 **401/42501**（非破坏性判据）；④ 生产站无登录态冒烟 9 个公开页，**RPC 失败 0 条**。SPA 路由的 404 是 GitHub Pages 既有行为（`404.html` 兜底），非回归。
- 未纳入本迁移（需匿名流量实测后再定）：`resolve_email_for_login`（降级仍需要）、`increment_ad_clicks`（改内部节流）、`execute_lottery_draw`/`execute_due_lottery_draws`（/lotteries 公开）、`assert_cloudinary_upload_allowed`（注册流程可能未登录态调用）、`cleanup_lottery_scheduler_logs`、`current_user_is_admin`。

### 3) `auth-login` EF 部署验证（用户已手工部署）
- 命令 `npx supabase functions deploy auth-login` **成功**；`WARNING: Docker is not running` 属正常（新版 CLI 走 API 打包，本地 serve 才要 Docker）。
- **如何证明新代码真生效**：CLI 只上传 import 图内的 `_shared/` 文件——目录里有 `altcha.ts` 但**未出现在上传清单**，而新增的 `resolve-login-id.ts` **出现了** → 部署的 `index.ts` 确实 import 了它。⚠️ 本项目 `auth-login` 新旧代码对外响应**完全相同**（都是 401 INVALID_CREDENTIALS + 同一文案），**打接口分不出新旧**，只能靠这招或看日志。
- 运行时冒烟：`POST /functions/v1/auth-login`（仅 anon key）返回干净 401 JSON → 三件事同时成立：verify_jwt 放行 anon key、模块全解析、代码跑到业务分支。
- 数据通路在 SQL 里先证掉：`profiles` 无孤儿（0 行在 auth.users 找不到）、两端 id 均为 `uuid`、32/32 账号有邮箱 → `auth.admin.getUserById` 必能取到 email。另核对 SDK：supabase-js@2.99.1 的 `GoTrueAdminApi.getUserById` 存在，且其 `validateUUID(uid)` 在 `try` **之外**（非 UUID 会直接抛，只有 isAuthError 才转成 `{data:{user:null},error}`）。
- **唯一未验证**：真实账号+正确密码的完整登录（无凭据）——已如实告知用户。
- 待办：前端改动（`auth-api.js` 等）**尚未提交/推送**；推送 main 才会触发部署。前端一旦上线，用户名登录才走 EF；之后再撤 `resolve_email_for_login`。

### 两项待办的准确口径（补充复核）
- **安全响应头**：Cloudflare **Transform Rules → Modify Response Header 免费版可用**（免费版 10 条规则、单条最多 10 个头）。分两步：① 零风险四件套 + HSTS（HSTS 在 SSL/TLS→Edge Certificates 有独立开关）可立即做；② CSP 必须先 Report-Only（站点 inline 样式多 + 跨 supabase/cloudinary，直接上强制会白屏）。备选 Worker 注入，但 Worker 挂 `www/*` 会接管全站请求，风险更高。
- **Cloudinary**：更正——「跟代码仓库无关」**只在「收紧 preset 约束」这条路上成立**；根治（签名上传）必须新增签名用 EF + 改 `cloudinary-client.js`，是代码活。
- ⚠️ **新发现**：`assertCloudinaryUploadAllowed` 函数体第一行是 **`if (!userId) return;`** → **未登录访客整道预检被跳过**（不只是"可绕过"，是根本没跑）。preset 为无签名且 `cloud_name=dkqae7j1m` / `upload_preset=BOHIMG` 已内联进 dist → 等同公开凭据。
  - 补正：`assert_cloudinary_upload_allowed` **函数本身对匿名是明确拒绝的**（抛 `CLOUDINARY_UPLOAD_RATE_LIMIT:NOT_AUTHENTICATED`）。所以准确说法是「客户端预检 + 未登录直接 return + 上传不经过 DB」共同导致匿名上传不受约束，而非函数写漏了。

### 4) Cloudinary 签名上传 —— 代码已就绪（2026-09-21）
- 关键前提核实：**所有 `uploadImageToCloudinary` 调用点都在登录后区域**；注册流程的头像走 **Supabase Storage**（`avatars` 桶）且发生在 signUp 成功之后 → Cloudinary 上传纯属登录用户操作 → 签名 EF 可强制要求登录态，能真正堵死匿名上传。
- 产出：`supabase/functions/_shared/cloudinary-sign.ts`（纯签名模块 + folder 校验）、`supabase/functions/cloudinary-sign-upload/index.ts`（新 EF）、`src/utils/cloudinary-client.js` 签名分支。
- 设计要点（可复用）：
  - **模式开关由服务端 secret 控制**（`CLOUDINARY_UPLOAD_MODE`，默认 `unsigned`）→ 部署 EF 零行为变化；切换只需 `supabase secrets set`，不用重发前端。
  - **不可达回落 / 明确拒绝不回落**：EF 404/网络故障 → 回落无签名（前端可先上线）；EF 明确 401/429/5xx → 抛错，**绝不静默降级**（否则等于把刚堵上的洞又打开）。
  - signed 模式下**参数一律以服务端返回为准**（含 `context=uid=`，客户端不可伪造）；去 context 重试必须**重新取签名**，否则 signature 与 params 不匹配会被 Cloudinary 拒。
  - 额度预检挪到服务端：EF 用**用户 JWT** 构造客户端调 `assert_cloudinary_upload_allowed`（用 service client 则 `auth.uid()` 为 null 会被该函数自身拒绝）。
- 测试：`cloudinary-sign.test.js` 20 个（签名向量由 Python hashlib **独立实现交叉校验**，非自我印证）、`cloudinary-signed-upload.test.js` 15 个。反证：把「一律回落 unsigned」的反模式打进去 → **4 红**，恢复全绿。全量 122 文件 / 2008 用例全绿，`vue-tsc` 与 ESLint 干净，构建通过。
- 待用户操作：部署 EF → 设 `CLOUDINARY_API_KEY`/`CLOUDINARY_API_SECRET`（**别贴进对话**）→ preset 改 Signed → `secrets set CLOUDINARY_UPLOAD_MODE=signed`。

### 5) 安全响应头 —— 自动化已就绪
- 产出 `scripts/cloudflare-security-headers.mjs` + `deploy.yml` 的 "Apply Cloudflare security headers" 步骤 + `package.json` 的 `security:headers` / `security:headers:dry`。
- 设计：只管理带 `[boh-security-headers]` 标记的那条规则（读-改-写，不覆盖手工规则）；失败一律告警 + exit 0 不阻断发布；默认四件套 + HSTS，CSP 仅 Report-Only（`--csp-enforce` 才强制）；`--remove` 只删自己那条。
- ⚠️ **前置**：现有 `CLOUDFLARE_API_TOKEN` 注释写明只有 **Zone.Cache Purge** → 跑该 phase 会 403，需追加 **Zone → Config（Transform Rules）→ Edit**。





### 6) 对已实现改动做独立复验（同日追加）

**复验原则**：不采信改动自带的注释/说明，把「无前端调用点」「实测有效」这类断言**反向核查**回代码与目录事实。

**三闸门实测（本机独立复跑）**：`vitest` 122 文件 / 2008 通过 + 1 跳过；`type-check` exit 0；`lint` **0 errors** / 376 warnings（全为既有 no-unused-vars）。

**授权判断反向核查**（这步不做就是盲信）：`2026092101` Part 1 把 8 个函数**连 `authenticated` 一起撤**，若前端有调用点会当场打断功能 → 逐条 grep `src/` 实测 **0 处调用点**；反向查白名单 5 个函数**均有**调用点（`auth-api.js:331` / `cloudinary-client.js:261` / `AdSlot.vue:41` / `DataAdmin.vue:4344` / `useDataAdminMutations.js:213,262`）→ 授权判断与事实一致，不会打断线上。

**复验中发现并修复 2 处缺陷**（均在前一轮我自己写的迁移里）：
1. **防复发语句只撤 `anon` 无效**（`alter default privileges ... from anon`）——函数默认对 `PUBLIC` 授予 EXECUTE，anon 经 PUBLIC 继承 → 此后**新建函数会重新变成匿名可调**，修复随时间衰减。已改 `from anon, public`。
   ⚠️ **关键教训**：`2026092101` **已在远端执行过**，改文件内容**不会重跑**。已生效的默认权限必须由**新迁移**纠正 → 新建 `2026092102_fix_anon_default_privileges.sql`。**「改文件救不了已应用的迁移」**。
2. **回滚附录 73 行语法错误**：写成 `revoke execute ... to anon`（恢复授权要用 `grant`）→ 事故时逐条报语法错误，回滚路径在最需要时失效。已全改 `grant`（残留 0），并注明 PUBLIC 隐式授权**有意不恢复**。

**修复精准性自证**：活跃语句 diff **仅 1 行**（防复发那行），73 条 revoke 一条未动；静态校验 77 条活跃语句全部以 `;` 结尾、无 `to <role>`、引号配平。

**本轮未验证（诚实边界）**：验证中途外网整体不可达（`api.supabase.com` / Supabase REST / 站点三处 `HTTP=000`，SSL EOF）→ **远端库状态无法复验**，「已应用」依据的是本会话早前实测（anon 224→160、表 75→0）；缺陷 1 的活探针已写入 `2026092102` 末尾注释，待网络恢复可一键复验。

**报告关系**：`docs/` 下有两份报告——本会话的 `2026-09-21-安全审计报告核验与全量复检.md`（P0-x/P1-x 编号）与另一路产出的 `2026-09-21-仓库可维护性审查报告.md`（SEC-x/批次 编号）。结论一致但编号不同；我已在报告里加了**编号映射表**（6.4）并指出那份「SEC-2/4 待执行迁移」与早前实测不符。

### 7) 新识别的残余：身份参数可伪造（本轮复验追加发现）

`2026092101` 对日志类函数**只撤了 anon**，但身份参数仍由客户端传入 → **登录用户仍可伪造**：
- `insert_moderation_log(..., p_moderator_id)` → 可把审核记录栽给任意 moderator
- `log_forum_rate_limit_event(p_user_id, ...)` → 可伪造他人限流事件
- `record_lottery_join_attempt(p_user_id, ...)` → 可伪造他人抽奖记录
（`increment_ad_clicks` 在白名单里，未撤权，任何人含游客可无限刷点击数）

修法：函数体内改 `coalesce(auth.uid(), p_<身份参数>)`。**这个写法对 cron / service_role 调用零影响**（此时 `auth.uid()` 为 NULL → 回落用入参），故零破坏风险。影响面是**审计链完整性**（非提权）。

已在报告新增第七章「下一步执行顺序」，把全部剩余项按「落地成本 × 风险」排成第 0~7 步。核心判断：**当前有 5 项「代码就绪但未启用」，长期半途会形成假安全感**（下次审计误判为已修，线上实际未防护）→ 先收口再新开工。

### 8) 推送 2026092102 + 实测推翻自己的结论（同日追加）

**已推送**：`npx supabase db push --linked --yes`（代理关掉后 5432 也通了，走官方 CLI 路径）。`migration list` 现 `2026092101` / `2026092102` 三列一致 → **顺带证实 2026092101 早已在远端生效**（另一份报告写「待执行」是错的）。

**❗ 实测结论：`alter default privileges` 防复发路线不成立**（我上一轮写进报告和技能的说法被推翻）：
- `defaclrole=postgres / public / f` 已变成 `{postgres=X, authenticated=X, service_role=X}` —— anon 与 PUBLIC 都移除了 ✅
- **但新建函数（owner=postgres）ACL 仍是 `{=X/postgres, postgres=X, authenticated=X, service_role=X}`**，`has_function_privilege('anon', 新函数) = **t**` ❌
- 排查：不是 `from public` 漏写；根因是**还有一条 `defaclrole=supabase_admin` 的默认权限条目**，且改它报 `42501 permission denied` → **碰不到**。所以「改默认权限」在本环境保证不了增量安全。
- 推论：全库 160 个 anon 可执行函数里 **92 个同时带 PUBLIC 的 EXECUTE** → 对它们只撤显式 anon 授权是**无效**的（必须连 public 一起撤）。`2026092101` 用的就是 `from anon, public`，存量侧没踩坑。

**增量防护只能靠**：① 新增函数时在同一条迁移显式 `revoke … from anon, public`；② 加检测（扫描 anon 可执行函数超白名单告警，符合项目十余个 `check:*.mjs` 门禁惯例）。**不要再用 `alter default privileges` 当防复发手段。**

**已修正的产物**：`2026092102` 文件内补「实测结果」段（原文件写着一个不成立的保证）；报告新增 6.7 节并在 6.3 加推翻指路；技能 `supabase-remote-db-audit` §6.12 ② 重写（含"先查清有几条 defaclrole 条目"的判据 SQL）。

**方法论**：这次是「先落文件、后做活探针」的顺序救了一次——探针（建探针函数读 `proacl`，raise 回滚）直接证伪了纸面结论。**权限类改动必须用「建对象 + 读 ACL」的活探针收尾，不能只看迁移跑没跑成功。**
