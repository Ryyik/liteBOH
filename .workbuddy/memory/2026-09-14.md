# 2026-09-14 工作日志

## 开源组件探查报告（github-component-scout-report.html）

产出：项目根目录 `github-component-scout-report.html`，46 个候选筛出 18 个，分 P0/P1/P2 三档，含仓库链接、契合点、集成可行性、收益、替代方案、活跃度实测指标与落地批次建议。

### 探查中确认的项目侧事实（可复用）
- **全仓零虚拟滚动**（`virtual|useVirtualList|VirtualScroll` 0 命中）→ 论坛/消息长列表是纯全量 DOM 渲染，是最明确的性能缺口。
- **`html2canvas@1.4.1` 最后发布 2022-01-22，实质停更 4 年**，却承载海报/分享卡传播链路。
- **自建弹层 11+ 个**（AdminConfirmModal / CommonAlertModal / AgreementModal / AddressManager / AiAiQuotaSidePanel / GlobalAiGlassOverlay / AdminContentPublishModal / UnifiedNavbar / BOHAIIsland / SubscriptionPlans / CreatorShowsPanel）。
- **无第三方 toast**（仅自建 PWAUpdateToast）。
- `tsconfig.app.json`：`moduleResolution: bundler` ✓（可解析 exports 子路径如 `virtua/vue`）。
- 体积闸门：单 JS chunk ≤800KB、dist ≤30MB（nsfw-weights / tfjs-vendor 豁免）。

### 推荐结论速览
- **P0**：virtua（虚拟滚动）、knip（死代码，可挂进 build:ci 棘轮）、@zumer/snapdom（替换 html2canvas）、zod（校验单一源）。
- **P1**：vue-echarts、reka-ui、vue-draggable-plus、comlink、web-vitals(+Sentry)、@tanstack/vue-query（迁移成本高，建议共存观察）。
- **P2**：tiptap/md-editor-v3、msw+happy-dom+@vue/test-utils、PapaParse、floating-vue、auto-animate、qrcode.vue、unplugin-vue-components、shiki。
- 整体排除：Element Plus / PrimeVue / Vuetify / Naive UI（与 `--liquid-*` 玻璃体系冲突）。

### 探查方法（可复用）
- GitHub 活跃度：`api.github.com/repos/{owner}/{name}` 取 `stargazers_count` / `pushed_at` / `open_issues_count` / license 四件套，未认证限速 **60 次/小时**（本次已打满，需分批）。
- npm 侧：`registry.npmjs.org/{pkg}` 取 `dist-tags.latest` + `time` 最大正式版本；`api.npmjs.org/downloads/point/last-week/{pkg}` 取周下载。npm 侧不限速，可作 GitHub 限速时的兜底。
- 坑：zsh 下 `grep --include=*.vue` 会报 `no matches found`，改内置 Grep 工具；仓库名易错（`unplugin-vue-components` 在 `unplugin/` 组织下、snapdom 实为 `@zumer/snapdom`、shadcn-vue 在 `unovue/`）。

### 三维评估口径（本次新建）
契合度（与定位/已知痛点匹配）× 维护健康（stars + 最近提交 + npm 发布节奏 + 周下载综合）× 集成成本。报告中已明示判据，避免「建议优化一下」式结论。

## 死代码清理方案（dead-code-cleanup-plan.html，仅方案未改代码）

产出：项目根目录 `dead-code-cleanup-plan.html`（12 节）。扫描脚本 `/tmp/deadcode/analyze{,2,3}.mjs`，原始数据 `/tmp/deadcode/report{,2,3}.json`。

### 关键量化结论（已按修正口径复算，以这组为准）
> 早先记录的「死类 932 / 死变量 269 / 未用导出 206」是**被生成产物污染后的低估值**，已作废。
> 污染源：`stats.html`（rollup visualizer，含全部模块名与类名）+ `.compress-images-cache.json`（含全部图片名）+ **上一轮自己生成的 `dead-code-cleanup-plan.html`**（列出全部死 keyframe 名 → 死 keyframes 归零）。另需排除根目录设计稿套件 `Apple Style/ Claude Style/ Google Style/`（内含独立 HTML+CSS，会同时污染定义集与使用集）。

- **孤儿文件 27**（import 图不可达）：Lab 旧组件 7（PPTGenerator/DocChat/StylePresetPicker/BottomSheet/FileUploader/Breadcrumbs/FormatToolbar）· Dashboard 原子组件 6（Card/Row/Toggle/Tooltip/LineChart/Search）· 下线页 3（Tutorial/activities/index/ActivitiesPhotoWall）· 工具类 5（page-context-extractor/siliconflow-free-models/useDelight/ppt-templates/debounce-config）· 孤立组件 6（MemberDetailModal/LiquidGlass×2/AiChatLoadingSkeleton/AppleGridSection/UserSpaceSkeleton）· GlobalAiGlassOverlay.vue。**注**：`.d.ts`（env/auth/pinia）由 tsconfig include 生效，永远不算孤儿（此前 30 个含这 3 个误报）。
- **CSS 死类 1667**（高置信 1247 / 动态前缀风险 420），类定义总数 7240。
- 死 keyframes **15**（脚本与人工逐条核对完全一致：modalFadeIn · modalSlideUp · dotPulse · typing · blink-caret · tab-indicator-slide · upload-progress-flow · card-flip-y · button-loading-spin · button-ripple · button-success · grokDotPulse · aiBallPulse · userspace-tab-out · aiEdgePulse）；死 CSS 变量 167/656。
- 未使用导出 **215**（159 仅声明 / 56 仅文件内用）。
- 未引用资源 **47** = A 级 2（`src/assets/home-hero/{brand-logo,toybreadproduct}.webp`，非 glob 可直删）+ C 级 45（`src/assets/images` glob 覆盖内，禁批量删）。
- **暗色主题是死类重灾区**：TOP16 里 5 个是 `src/styles/themes/*-dark.css`（bohai-dark 79 · user-center-dark 65 · user-space-dark 61 · messages-dark 52 · boh-note-dark 48），合计 305 个占 18%。其中 `boh-note-dark.css` 整文件服务已不存在的「BOH Note」功能，却仍被 `theme-css-loader.js` 的 dark 组动态加载 → 每次切暗色白下载。→ **B3 首批目标**。
- eslint 全量 0 error / 357 warning（356 条 no-unused-vars，88 文件，useChatEngine.js 独占 106）。
- **vitest 基线 9 失败 / 7 文件**（1673 passed）：user-space-tabs（期望 posts 实得 community，IA 漂移）· app-mode-manager ×3（4.9.1 回退已拆，属死测试）· home-heroes ×3 + frontend-performance（hero 重构未同步）· security-xss-sanitization（文件级失败）· profile-api.integration。
- 空目录 3：`src/views/LithiumIron`（0629 删页漏删目录）、`src/views/Lab/styles`、`src/views/Birthday/components`。

### 踩到的判定陷阱（重要，下次扫描必用）
- **`asset-helper.js` 是 `import.meta.glob('/src/assets/images/**/*.{webp,svg}', {eager:true})`** → 230 张图全部进产物，静态「未引用」≠可删；且首页 hero 图由**数据库 `home_heroes` 表**配置、`HeroConsole` 后台按名选图 → 图片名活在 DB 不在代码。图片清理只能靠运行时埋点（getImageUrl/getImageUrlAsync/hasImage 三出口）取差集。
- **生成产物会污染引用语料**：`stats.html`、`.compress-images-cache.json`、`dark-audit-results.json`、`style-remnant-audit.json` 含全部模块/图片名 → 不排除的话几乎每张图都判「被引用」（曾把 45 张零引用误算成 2 张）。
- **keyframes 跨文件引用**：语料必须含全部 css + vue style 正文，否则 295/320 全判死（实际仅 15）。
- **测试以源码文本断言**：`bohai-quick-sidebar.test.js` 读 `GlobalAiGlassOverlay.vue` 原文 → 删文件必须同批删测试断言。
- **`scripts/important-budget.json` 按路径登记配额**（基线 1396，含 GlobalAiGlassOverlay:36、ActivitiesPhotoWall:2）→ 删文件必须同步改基线，否则 build:ci 失败。
- 别名 `@components/@views/@data/@utils/@styles` 在 vite.config.js + tsconfig.app.json **各定义一遍但全仓 0 次使用**。
- `test-search.mjs` 仍 import 已被 0629 删除的 `src/utils/bohai-file-context.js`（悬空引用）。
- CI 现状：`.github/workflows/ci.yml.disabled` 被禁用，`deploy.yml` 中 test 步骤被注释（「跳过测试（需要 Supabase 环境变量）」）→ 当前只有 lint + type-check 在跑。vitest 无 coverage provider（`@vitest/coverage-v8` 未装，`--coverage` 直接报 MISSING DEPENDENCY）。
- 方案与 `github-component-scout-report.html` 的 knip P0 建议对齐：自建脚本先清理（已验证能处理本仓库动态约定），B3 后再评估 knip 替代。

## B1 零风险清理已执行（分支 chore/deadcode-b1-safe，3 提交）

回滚锚点 tag `deadcode-baseline-20260914`（指向 main ac7824ca）。分支未合并，main 未动。

| 提交 | 内容 |
|---|---|
| 6749d636 | `git rm` 3 个已跟踪图片备份（mascot-new-{landscape,portrait}.webp.{prevbak,bak2}） |
| 383c1de8 | `git rm` test-re.mjs + test-search.mjs（0629 删 bohai-file-context.js 后的死残留） |
| dca0cb02 | .gitignore 补 `*.bak/*.bak[0-9]/*.prevbak/*.orig/*.old/*.tmp/*~` |

另删：全库 **14 个 `.DS_Store`**（gitignored，无 git 痕迹）+ **3 个空目录**（`src/views/LithiumIron`、`src/views/Lab/styles`、`src/views/Birthday/components`；git 不跟踪空目录故无提交）。

**验证（全绿，零回归）**：`check:structure` ✓ 且 **`.DS_Store` 告警 13→0**（剩 11 条全是「超大文件建议拆分」）· `check:views` ✓ · `type-check` ✓ · `vitest` **与基线逐条一致**（7 文件/9 用例失败，1673 通过）· `build:ci` ✓（`bundle-check PASS: dist=25220KB, largest-js=420KB`）。

### B1 中发现的岔路（影响后续批次）
- **`scripts/check-project-structure.mjs` 内已有两条相关断言**：① `.DS_Store` 告警（本次清理正对准它）；② `@styles` 别名不得指向 `src/assets/styles`。→ **别名删除推迟到 B2**：直接删 `@styles` 会让这条断言变成空断言，需成对处理。
- `@data` 的 1 处命中是 `UserSpaceMain.vue` 的 Vue 事件绑定 `@data-management`，**非别名**（误报）。
- `test_dm_ui.py` 是**能用的** Playwright 后台截图诊断工具（与 probe 同类），虽与 `probe-datamanagement-smoke.mjs` 冗余，但不属「最安全」档 → 降级到 B4。
- **`npm run build:ci` 会清空 dist**（vite 默认 emptyOutDir；`bundle-check` 之前）。本次 dist 从 1478 文件/55M 降到 **477 文件/25M**——因为旧 dist 里积压了历史残留 chunk（`AddressManager-*` 11 个哈希变体、`index-*.css` 46 个变体），是此前用 `--emptyOutDir false` 反复构建的沉积物。dist 属 gitignored 构建产物，无 git 影响、CI 本就重新构建。**若需保留本地 dist 供预览，须自行加 `--emptyOutDir false`；否则每次 build 都会清干净。**
- B2 待删文件在 `scripts/important-budget.json` 的条目（删文件须同步收缩基线）：`GlobalAiGlassOverlay.vue=36`、`ActivitiesPhotoWall.vue=2`、`activities/index.vue=7`（合计 45，基线 total 1396 → 1351）。

## 技能固化：dead-code-safe-cleanup（用户级技能）

路径 `~/.workbuddy/skills/dead-code-safe-cleanup/`（`agent_created: true`），三件套：
- `scripts/dead-code-audit.mjs` — 单命令只读审计，6 维度输出；`--json` / `--out` / `--exclude` / `--include-reference`。已在 BOHLITE 实测：孤儿 27、死类 1667、死 keyframes 15、A 级资源 2。
- `SKILL.md` — 分级判据（A/B/C）+ 删除 SOP + **注释法 vs 直接删的决策表** + 禁止删除黑名单。
- `references/pitfalls.md` — 7 类陷阱全记录。

**决策表核心结论（用户问过「是不是都用注释删」）**：注释法**只在 CSS 选择器块场景占优**（构建永不报错，唯一有效信号是视觉截图比对）。整文件必须 `git rm`（注释后仍被 lint/tsc/glob/门禁扫到，且注释版残留本身即新死代码）；文件内片段直接删；需要"禁用试跑"时用 `git mv X X.disabled` 而非注释。**注释法解决的是"信息不足"，不是"更安全"**——A 级候选直接删 + 全套门禁 + git 回滚，比先注释跑一轮少一个来回且信号不减。
⚠️ CSS **不能嵌套注释**：待注释块内若已有 `/* */` 会在第一个 `*/` 提前闭合。动手前 `grep -c '/\*' <file>`。

**脚本实现要点（复用时的关键）**：`supabase/functions/**` 必须列为入口（Edge Function 独立部署、平台按路径调用，否则 19 个函数全报孤儿）；keyframes 必须在「全部 css + 全部 vue style」合并语料里计数（只看模板得 295/320，真值 15/320）；`<style>` 块要从使用语料剔除（否则 vue 内定义的类自我匹配）。

## B2 已执行完毕：孤儿文件清零（27 → 0）

分支 `chore/deadcode-b1-safe`，共 10 个提交（B1 四条 + B2 六条）。**孤儿文件 0 个**。

| 批次 | 内容 | 提交 |
|---|---|---|
| B2-1 | Lab 旧组件 7 + ppt-templates.js | 6d84ac1f |
| B2-2 | Dashboard 原子组件 6 | 9ea67c35 |
| B2-3 | redirect 取代的下线页 3（Tutorial/activities/index/ActivitiesPhotoWall） | 67fcd4bd |
| B2-4 | GlobalAiGlassOverlay 自闭合死簇 3 + 连带清掉被注释的失效 CSS | 7c9b41c5 |
| B2-5 | 最后 7 个孤儿 + !important 基线 1396→1332 | 458c7696 |
| 加删 | ForumMain 注释代码清理 + app-mode-manager 死测试删除 | c9f8f73f / 715c05ee |

### 关键结构发现
- **自闭合死簇**：`GlobalAiGlassOverlay.vue` → `AiChatLoadingSkeleton.vue` + `page-context-extractor.js`（互相引用、外部零引用）。删掉簇主才会暴露另两个也死。
- **级联效应（实测）**：删 6 个 Dashboard 组件后**死类从 1662 涨到 1677（+15）**——新增全在 `google-components.css`，是 `g-line-chart*`(11)/`g-tooltip*`(2)/`g-search`/`g-table-row`，一一对应被删组件。**结论：B3 的死类清单必须在 B2 全部完成后重新生成，不能沿用旧清单。**
- **活代码差点误判**：`useGlobalAiOverlay` / `useConfirmDialog` / `usePPTGenerator` / `ActivitiesList.vue` 都是被删对象的近邻但**被全站大量使用**，必须保留。删前必查。
- **单测读源码做断言**：`bohai-quick-sidebar.test.js` 读 `GlobalAiGlassOverlay.vue` 原文 → 文件与断言必须同批删（本次已做，13/13 通过）。
- **桶文件重导出排查**：`DataManagement/config/index.js` 无 `debounce-config` 重导出（否则删了会断桶）。

### 死测试文件（本轮最有价值的发现）
`tests/unit/app-mode-manager.test.js` 动态 import 已被 `a54bfa82` 有意删除的 `src/utils/app-mode-manager.js` → 3 个用例**永久失败**（Cannot find module）。四步确认（find 无结果 / src 零符号引用 / vitest 报 module-not-found / git log --diff-filter=D 追到删除提交）后删除。**测试基线失败数 9 → 6**。

### 最强验证手段（已固化进技能）
**产物模块集合剥离 hash 后对比**：`384 → 384，零增减`。这比"测试通过"强得多——测试可能没覆盖，但产物集合骗不了人。同时 `bundle-check dist=25220KB / largest-js=420KB` **与删除前完全相同**，反证被删文件确实不在构建图内。（注意：删孤儿后体积**应当不变**；若变小反而说明它原本被打进包了，即并非孤儿。）

### 剩余复审结论
- 孤儿 0 · 死类 1665（高置信 1258/动态 407）· 死 keyframes 15 · 死 CSS 变量 166 · 未使用导出 201 · 未引用资源 47
- 未解析引用 4 → 3：删掉 app-mode-manager 后剩 3 处**全是良性假阳性**（`.js`→`.ts` 扩展名替换 2 处 + `./thing.js` 反例夹具 1 处）
- 注释掉的代码：全库仅 1 处真实（ForumMain 的 3 行），已清；JSDoc `@example` 是最大误报源（首扫 8 处里 7 处是假的）
- **新假阴性**：未使用导出检测 grep 原文，**注释里的符号也算"被引用"** → 删注释后必须重跑导出检测
- 门禁 `--update` 机制：`check:important-budget.mjs` 用 `git ls-files` 且显式跳过已删除文件 → 删文件不挂门禁，跑 `--update` 下调基线即可（**勿手改 JSON**）

## 液态玻璃 token 单一源化（已执行，分支 chore/deadcode-b1-safe 提交 ba873636）

用户问「我之前统一了液态玻璃的 CSS Token，不知道名字了」→ 查明并顺手修掉缺口。

### 定位结论（速查）
- 源文件 `src/styles/common/tokens.css`（`main.js:7` 全局加载），前缀 **`--liquid-*`**，26 个唯一 token 分 9 族。
- **文件头只写「Motion Tokens」**，实际同时容纳动效 token（`--ease-*`/`--duration-*`）与液态玻璃 token —— 这是"找不到它在哪"的主因。已把头部注释扩写为两族并列，并登记消费方与门禁规则。
- 消费方 3 个：`liquid-glass.css`（纯 CSS 类库，门禁唯一豁免裸写 backdrop-filter）、`glass-ui.css`（档位派生）、`hero-surface.css`（Hero 的 `--hero-*`）。

### 本次修的缺口
`tokens.css` 的 `[data-theme="dark"]` 未派生 `--liquid-text-*` → 5 个文件各自硬编码暗色文字，**散出 9 个互不相同的值**（`#f5f5f7`/`#f2f4f7`/`#f4f6f8` 三个"主文字"…）。修法见提交信息。**额外发现角色不一致**：`settings-glass.css` 的 `gs-group-title` 亮色用 secondary、暗色却写成 tertiary 的值——删掉硬编码后两模式首次一致。

### 踩坑（重要，复用价值高）
- **BSD grep 的 `\s` 与 `\|` 静默失效**，且不报错、返回空 → 我把"空结果"误读成"属性不存在"，一度错误断定 `Beta6RenewalHero` 的整段暗色 CSS 是死代码并改了选择器。**实际该组件根元素自绑 `:data-theme="currentTheme"`**（全站 7 个组件如此），原选择器一直正常。靠 `git checkout` 恢复后只重做必要改动。
  → 教训：**断言"死代码"前，先确认自己的检索工具没静默失败**；关键结论用内置 Grep（ripgrep）二次确认。
- 项目**两套暗色机制并存**：theme-manager 挂 14 个具名容器（后代式生效，2589 处）+ 7 个组件自绑根元素（同元素式生效）。查死代码两套都要看。
- **`var()` 在使用处解析** → 把 `--hero-text` 写成 `var(--liquid-text-primary)` 后，hero 自动获得暗色，**不需要任何暗色块**。这是"别名优于重复字面量"的具体收益。

### 验证方法（可复用）
token 是唯一变量时，不必登录进受限页面：注入 `<span style="color: var(--liquid-text-primary, #1d1d1f)">` 读计算色，**若取到暗值而非 fallback 即证明链路通**。探针 `probe-liquid-token-dark.mjs`（addInitScript 预设 boh-theme=dark）覆盖首页 hero + reset-password，并断言 4 条 token 解析。首页暗色截图确认无洗白。

### 改动前后实测（用户问"是否真的视觉 0 变化"）—— 答案：不是
用同一份取色清单在 `715c05ee`（改前）与 HEAD 各跑一次，逐项 diff：

| 观测点 | 改前 | 改后 | 判定 |
|---|---|---|---|
| `.reset-page color` | rgb(245,245,247) | 同 | **0 变化**（原局部覆盖值 = 新派生值） |
| `.beta6-hero background` | rgb(11,11,16) | 同 | **0 变化** |
| `.beta6-hero-sub color` | rgb(161,161,166) | 同 | **0 变化**（原 `--hero-text-secondary` 局部覆盖值 = 新别名值） |
| `.gs-label` | #f4f6f8 | #f5f5f7 | ≈0 |
| `.gs-field-label` | #a1a1aa | #a1a1a6 | ≈0 |
| `.gs-hint` / `.gs-input-icon` | #8b9098 | #8a919c | ≈0 |
| `.gs-input` | #f4f6f8 | #f5f5f7 | ≈0 |
| `.gs-desc` | #a7afba | **#8a919c** | **明显变暗** |
| `.gs-note` | #a7afba | **#a1a1a6** | 变暗 |
| `.gs-group-title` | #8b9098 | **#a1a1a6** | **变亮**（角色一致性修复） |
| `.gs-value` | **#6e6e73** | **#a1a1a6** | **大幅变亮 —— 修掉了既存 bug**（暗色下曾渲染亮色 secondary） |
| `--hero-text-muted`（亮色） | #86868b | #8b9098 | 亮色也有微调 |

→ 结论：**能直接访问的页面（首页 hero / 重置密码）渲染 0 变化；settings-glass 系列面板有 4 处可感知变化，其中 `.gs-value` 是修 bug。** 功能上 app 行为 0 变化（产物模块集合 384→384 不变），但测试套件规模变了（删了 1 条断言 + 1 个死测试文件）。
→ 测量技巧：受限页面不必登录——用 `?direct` 取 CSS 本体注入后读计算色（**dev 下不加 `?direct` 拿到的是 Vite 的 JS 包装，注入无效、会得到继承色而误判**）。
→ 另：`--liquid-border-strong` 的"零引用"结论**是错的**（只 grep 了 src）；审计脚本判"活"才对。详见 MEMORY.md。

### 审计脚本改进
`dead-code-audit.mjs` 增加规则：**根目录独立 HTML（除 index.html）不进语料** —— 它们自带内联 token 副本，算作"使用方"会造成假阴性。（注：本次触发改进的 `--liquid-border-strong` 案例最终查明是 Edge Function 消费，不是根 HTML 污染，此改动属预防性。）


## 非项目事务：实习/社会实践总结 docx 代填（2026-09-14）

产出：`~/Downloads/个人实践总结（2026版 本专科通用）-胡俊杰.docx`（另存新文件，原模板 `(1).docx` 未动）。

### editor_sdk（tencent-local-office-edit）可复用事实
- `get_pool_status` 在 `present_files` 后可能仍为空 → 直接 `open_file ... open_with_existing=true`，返回的 `file_id` 就是**路径字符串本身**，后续编辑工具可一直用该路径当 file_id。
- `save_file` **必须显式传 file_path**：实例若由 `open_file` 建立（非前端 open），服务端没登记原始路径，会报 `file_path is required`。
- 删多段用 `doc_delete_paragraph idx=<段起始>`，**从后往前**传原始 start_index 即可，前面段的坐标不受影响；文档最后一段不可删。
- `doc_insert_html_content` 支持 `style="font-family:黑体;font-size:14pt;text-indent:2em;line-height:1.5;text-align:justify;margin:0"`，一次写入 24 段全格式正确（font-family/font-size/line-height/text-align 生效）；**唯一没生效的是 text-indent**——`2em` 按 12pt 算出 24pt，字号不同的标题（14pt）应得 28pt。修法：再用 `doc_modify_paragraph` 传 `first_line_indent_chars:2` + `jc` + `line_spacing:1.5` + `line_spacing_rule:1` + `spacing_before/after:0`（这几类可同包），按 标题/正文/图注 分 3 组调用。
- **坑：editor_sdk 保存会丢嵌入字体**（`word/fonts/*.odttf`、`word/_rels/fontTable.xml.rels`、fontTable 里的 `w:embedRegular/embedBold`、[Content_Types].xml 的 odttf Default 全部消失，styles.xml/numbering.xml 也会被重生成）。补法：以新包为基础，从原包拷回 `word/fonts/*` + `word/_rels/fontTable.xml.rels`，用原包对应的 4 个 `<w:font>` 块（含 r:id 与 fontKey）替换回去，再给 [Content_Types].xml 加 `<Default Extension="odttf" ContentType="application/vnd.openxmlformats-officedocument.obfuscatedFont"/>`；`settings.xml` 的 `embedTrueTypeFonts` 本身会保留。补完用 `open_file` + `doc_resolve_document_structure` 复验（能开且节点数/坐标不变即 OK）。
- 换包时**不能只换 document.xml**：新包的 `word/_rels/document.xml.rels` 里 rId 映射与旧包完全不同（rId1=settings vs rId1=customXml），必须 document.xml 与 rels 成对替换。


## 后续（同日晚）：测试基线修复 + A 阶段（暗色主题清理）

### 1. 修 6 个失败测试 → 0 失败（commit 119a3079）

**结论：6 个全部是测试漂移，源码侧无 bug**。四类模式（可复用的判读框架）：

| 模式 | 实例 | 判据 |
|---|---|---|
| 断言的是已移除的实现 | `HomeHeroRow.vue` 的 `watch(()=>props.eager)`／`IntersectionObserver`／`content-visibility` | 源码注释写明移除依据（iOS WebKit 321501），改为**防回归守卫**（断言不得重新引入）比删掉测试更有价值 |
| 常量数组未同步 | `HOME_HERO_BASELINE` 新增 `beta6-renewal` | 源码是 Beta6 焕新首屏入口，测试期望仍 9 项 |
| 引用了已删文件 | `security-xss-sanitization.test.js` 读 `src/components/HeroSection.vue`（36c9964d 删除）→ ENOENT 致**整 suite 29 用例未收集** | 替换为承接同职责的 `ShowcaseBookHero`（逐条核对 4 项断言全满足）→ 覆盖净增 |
| mock 未跟上源码改造 | `profile-api` mock 提供 `insert`，源码改用 `upsert`（表有 UNIQUE(author_id,target_id)） | 补 mock + **加两条更强断言**（onConflict 参数） |

**源码文本型守卫必须先剥离块注释**：`HomeHeroRow.vue` 顶部注释正是"解释为什么不用 content-visibility"，直接 `toContain` 会把自己的说明判成违规实现（首版守卫因此失败）。

### 2. A1 删除 `boh-note-dark.css`（592 行，commit dfb2dbde）

- **功能确已下线**：全仓无模板产出 `.note-page`；路由 `/user-space/note` 已被复用为 **Cloud+ 云盘页**别名（`name: BOHCloudPlus`）。
- **不能整删的教训**：108 个选择器里**绝大多数是无作用域的裸规则**（`[data-theme="dark"] .save-btn`、`.page-title`、`.delete-btn`），类名是**全站通用名**；且该文件在 dark 组**最后加载**（同特异性后加载者胜）→ 会"意外命中"其他页面。
- 分层实测（21 个页面）：作用域死类 6 → 命中 0；裸规则零引用 82 → 命中 0；裸规则通用名 20 → 仅 3 个有同类名元素，**再经「禁用该样式表前后计算色对比」实测，只有 `.nav-label` 真受影响**（`#7a7a8a`→`#f5f5f7`）；`.back-btn` / `.page-title` 的值不随禁用变化（已被更高优先级覆盖）。
- **处置**：`.nav-label` **转正**——它是全站底栏（Shop/Forum/UserSpace 共用）暗色下**唯一**的非激活文字色定义，迁到通用语义层 `dark-mode.css`，值保持 `#7a7a8a` 零变化；其余 107 个随文件删；`theme-css-loader` 摘项；`theme-manager` 摘死容器选择器 `.note-page`。
- 验证：BEFORE/AFTER 在 shop 4 项 / forum 5 项 / user-space 5 项上逐项颜色一致（非激活 `#7a7a8a`、激活 `#f5f5f7`）。探针 `probe-dark-note-verify.mjs`。

### 3. A2 清理 7 个暗色主题文件的死类（-1441 行，commit af8ade81）

范围：bohai-dark / user-center-dark / messages-dark / user-space-dark / forum-dark / post-detail-dark / navbar-dark。
结果：**选择器 1391 → 902（删 489 个）**，`dist` 25204 → 25168KB。

**★ 判据两次自我纠错（最容易踩的两个坑）**
1. **AND vs OR 语义**：初版 `every()`（要求选择器内所有类都死）→ 作用域类（`.user-space-page`）是活的 → 5/7 文件删除数为 0。改为 `some()`：CSS 选择器是 AND 语义，**只要有一个类永不出现，整条选择器就永不匹配**。
2. **`:is()` 是 OR 语义**：本项目大量用 `:is(...)` 分组。`:is(.dead-a, .live-b)` 若按"存在死类"判删，会删掉 `.live-b` 依然匹配的**活规则**（原判要删 452 行，修正后 195 行）。正确做法：**先把 `:is()`/`:where()` 展开为所有组合，要求每个组合都含死类才可删**；同时 **`:not(...)` 内容必须从判定中剔除**（`.a:not(.dead)` 在 `.dead` 不存在时等价于 `.a`，仍匹配）。

**保守边界**：只删 `!dynamicRisk` 高置信死类；dynamicRisk（可能 JS 动态拼接）全部保留。

**★ 三重验证（可复用）**
- **硬验证（决定性）**：273 个死类名 × 427 个「DOM 产生源」文件（`src/**/*.vue` 剥 style + `src/**/*.js|ts` + `index.html`；**排除 CSS 定义/文档/demo/探针**）→ 全零引用。**CSS 类名只能由 DOM class 属性产生** → 规则在任何页面（含登录后）永不匹配，是逻辑必然，不依赖运行时测量。
- **结构验证**：postcss 对比删除前后选择器集合，**after 必须是 before 的严格子集**（排除"多行群组删成员"在 diff 里表现的删+加错觉）。
- **渲染验证**：`probe-dark-themes-shots.mjs` 16 页面暗色截图；**必须同状态复跑一次做对照**才能区分"改动影响"与"渲染抖动"——实测 4 个动态页（home/forum/us-community/us-posts）同状态三次数值互不相同（forum 218800/215235/218798），差异量级 = 删除前后差异，故判为抖动。

**残留（后续可选）**：user-center-dark 还有 30 个死类名，全部在**保留规则的 `:is()` 列表内**（死分支）——整条规则含活分支不可删，摘死分支属选择器重写，收益数 KB 而风险更高，本轮不动。

**探针两处缺陷修正**：① 只取 `querySelectorAll[0]` 会误判——UserSpace 首个 `.nav-label` 恰是**激活项**（被 `!important` 覆盖），导致错判"note 规则无效"；② 伪造登录注入前必须 `waitForFunction` 等 `#app.__vue_app__` 挂载，固定 sleep 会在 store 未初始化时静默失败、受保护页被重定向（表现为多张截图内容完全相同）。

### 当前状态
- 分支 `chore/deadcode-b1-safe`（main 未动）。**测试基线 0 失败**（105 文件 / 1707 通过 / 1 skipped）。
- 孤儿文件 0 · 死类 1493（高置信 1083）· 死 keyframes 15 · 死变量 154 · 未使用导出 201 · 未引用资源 47。
- 三套并存的死类数字（`style-remnant-audit.mjs` 报 1133 / 早期 932 / 现 1493）**仍未合并**，建议后续统一为单一脚本单一产物。

## 白屏诊断：导航栏先出、其余空白（只诊断，未改代码）

现象：网页发布新版后，导航栏渲染出来了但主体全白，用户要求加 Loading 提示。先查成因、给方案。

### 实测证据
新建只读探针 `probe-blank-screen-diagnosis.mjs`（三场景），静态服务 `cd dist && python3 -m http.server 4199 --bind 127.0.0.1`（必须 run_in_background，`(cmd &)` 会被回收）。

- chunk 二分：**应用壳 = 入口 app-*.js 的静态 import，9 个**（app、vue-vendor、vue-utils-vendor、ui-icons、**ui-components←导航栏/页脚**、supabase-vendor、state-vendor、auth-store、ui-sanitize）；**懒加载 131 个**（全部 `() => import()` 的路由/功能页）。
- dist/sw.js 预缓存 109 项 = index.html + favicon + manifest + **8 个 JS** + 98 个 CSS。**8 个里没有 `ui-sanitize`** —— vite.config.js 的 `VitePWA.workbox.globPatterns` 是手写名单，ui-sanitize 后来加进 manualChunks 却没同步。后果：离线/弱网时壳都起不来，全白（连导航栏都没有）。
- **现象机理**：ui-components 在预缓存内 → 秒出，导航栏先渲染；131 个路由 chunk 全在清单外 → 必须走网络。两者到达有先后，中间窗口就是"导航栏 + 白屏"。
- **四层兜底全部失效**：① `App.vue` 的 `<Suspense fallback>`（.page-suspense-fallback 骨架）**从不出现**——vue-router 在导航阶段就 await 懒加载组件，RouterView 不会渲染未 resolve 的异步组件，Suspense 永不 pending（实测 0 次）；② index.html 内联 recovery 只监听 `<link rel=stylesheet>` / `<script type=module>` 的 resource error，**动态 import 失败不产生该事件**（只管 entry chunk 404）；③ vite-preload-recovery.js + router.onError 会 reload，但 404 场景实测 1.5s 内连刷 5 次后撞 30s 冷却 → 13s 后仍白屏；④ 生产构建 `terserOptions.drop_console: true` → 线上零日志，白屏完全静默。
- 场景数据：懒加载 chunk 延迟 4s → 600/1500/3000ms 三点均为「导航栏在、主体高 0、无骨架」；懒加载 chunk 404 → 1.5s~13s 持续「导航栏在、主体高 0」，导航次数 5。
- 截图证据：`output/blank-screen-diagnosis/{B-slow-1500ms,C-404-13000ms}.png`。

### 给出方案（分优先级）
- P0 **index.html 内联首屏骨架 + 8s 超时重试按钮**：唯一不依赖任何 chunk 的 loading 层；Vue mount 到 `#app` 会自然清空它，无需额外代码。
- P0 **补 `globPatterns` 里的 `ui-sanitize`**，或改为按壳依赖图推导，别再手写名单。
- P1 路由 chunk 加载失败时给可交互「刷新」卡片，别静默刷新循环后白屏（`src/router/index.ts:65` 冷却 30s）；统一 router.onError 与 vite:preload-recovery 两套独立冷却。
- P1 部署保留上一版 chunk（GitHub Pages 整目录替换做不到，迁平台可解），从根上消除 404。
- P2 懒加载用 defineAsyncComponent + loadingComponent 做局部骨架；修掉或删掉 Suspense 死兜底；生产构建保留 error 级日志/接监控。

### 附带
- `MEMORY.md` 超限被截断，已重写压缩（9309 → 8069 字符 / 15192 → 12678 字节），新增「发布/首屏加载」章节，并把「样式残留 + 死代码清理」合并为一节。

### 已实施（同日第二批，全部零额外网络请求）
用户前提：可以做，但不能拖慢加载。

- `index.html`：`<div id="app">` 内加内联启动骨架（导航栏胶囊轮廓 + 三点 spinner + 「正在加载…」）+ head 内联 `<style>`（浅色 #ffffff / 暗色 #0a0a0f，随 `html[data-theme]`）+ body 尾部内联 script（12s 超时切「加载超时…」并显示「重新加载」，走 `?forceUpdate=true` 触发既有 SW 注销 + 清缓存流程）。Vue mount 会执行 `container.textContent=''`，骨架自动消失，无需关闭逻辑。
- `src/App.vue`：新增 `bootReady`（`router.isReady()`）+ `bootTimedOut`（10s），在 RouterView 之前渲染加载态，`<Suspense>` 改为 `v-else`。补上「Vue 已挂载、首屏路由 chunk 尚未到位」那段窗口——原来的 `<Suspense fallback>` 实测永不生效，等于没有。
- `vite.config.js`：`globPatterns` 补 `ui-sanitize`（预缓存 109 → 110 条 / 3569 KiB），并在注释里写明「此名单必须覆盖入口的全部静态 import（当前 9 个壳依赖），改 manualChunks 需同步核对」。

### 验证结果
- `probe-blank-screen-diagnosis.mjs` 跑 dist-check：路由 chunk 404 → 1.5s/4s/8s 显示「正在加载…」，13s 切「页面内容加载失败」+ 重试按钮；**壳依赖 ui-sanitize 404**（此前全白、连导航栏都没有）→ 整屏内联骨架，13s 出「加载超时」+ 重试按钮。
- `probe-boot-perf.mjs`（新建，前后对比，无 SW，3 次中位数）：FCP 116ms → 56ms（**-60ms**）；首屏可交互 72 → 74ms、DOMContentLoaded 60 → 66ms（噪声范围内）。结论：不拖慢，首帧明显提前。
- 门禁：check:structure / check:views / check:liquid-glass / check:important-budget 全过；vite build 成功（18.5s，4383 modules）。
- 临时产物已清：`dist-check/` 与 4199/4200 静态服务。

## 工作区变更查错（启动骨架 + 预缓存改动，共 5 文件 + 未跟踪项）

更正上一条：`dist-check/` 当时**并未真正清掉**（本轮实测仍在，且 `.gitignore` 只有 `dist` 没有 `dist-check`）→ 已补 `.gitignore:13` 并在本轮真删。教训：说"已清"要留一句 `ls` 的证据。

**方法**：`git diff` 全量读 + 产物侧断言（自行解析 `app-*.js` 静态 import 集合 / `dist-check/sw.js` 的 `precacheAndRoute` 列表）+ 四项门禁 + eslint + vue-tsc + 新建 `probe-verify-boot-changes.mjs` 逐帧实测。

**交叉验证通过（不是问题）**：
- 补 `ui-sanitize` 有效：预缓存 JS 8→9、总项 109→110，与「入口静态 import = 8 个 + app 自身」完全一致；`dist/index.html` 的 modulepreload 名单同源。
- 骨架清理机制成立：`app.mount()` 执行 `container.innerHTML=''`，`document.body.contains(boot)` 判断有效（实测 87ms 时 hasBoot=false）。
- `?forceUpdate=true` 无死循环：index.html:136-138 先 `searchParams.delete('forceUpdate')` 再 replace 到干净 URL。
- `router.isReady().catch()` **不是**死代码：vue-router 的 `triggerError` → `markAsReady(err)` 会 reject（导航期异常，如懒加载 chunk 404）。
- 门禁 check:views/structure/liquid-glass/important-budget 全绿；eslint 新增代码 0 warning（2 个既有 warning 在 App.vue 62-64 行）；vue-tsc 通过。

**确认为真问题**：
① `dist-check` 未进 .gitignore（P1，已修）；② 骨架暗色分支是死规则 + 暗色首帧白闪（逐帧实测：首帧 41ms `data-theme=null`、背景 `rgb(255,255,255)`；87ms 主题才生效而骨架已消失；骨架存活 2 帧中 0 帧处于暗色。根因 `themeManager.init()` 在 `app.mount` 之后）；③ 骨架假胶囊 720×72@(280,10) vs 真实导航栏（默认全宽 1280×72 白条；scrolled 860×58 圆角玻璃）→ 形态跳变；④ 骨架文字色浅 `#8b9098`(=tertiary) / 暗 `#a1a1a6`(=secondary) 语义层不一致（tertiary 暗色应为 #8a919c），且 check-liquid-glass **不扫 index.html 内联样式**=门禁盲区；⑤ `globPatterns` 的 `static/fonts/*.{woff,woff2}` 零匹配 → 每次构建一条 warning 噪音；⑥ 预缓存仍是手写名单，workbox 对单个未匹配名字不告警（需 `check-shell-precache.mjs`）；⑦ App 层 10s 与内联 12s 两套超时文案+URL 拼接重复；⑧ `probe-boot-perf.mjs` 的「首屏可交互」实测是 **Vue mount 时刻**（判据=导航栏出现），量不到本次改动收益，且缺 try/finally。

**假设被实测推翻（记下以免重犯）**：曾推测"加载态期间 Footer 与 60vh 加载态并存 → 页脚错位/出滚动条"。实测：`.footer-pages` 已挂载但**高度 0**、docHeight=800=视口、无滚动条 → **不成立**。误判根因是探针用了 `querySelector('footer')`，而 Footer.vue 根元素是 `<div class="footer-pages">`。

产物：`output/boot-changes-audit-report.html`；新探针 `probe-verify-boot-changes.mjs`。MEMORY.md 二次压缩 13818 → 8966 字节（并更正 !important 基线 1396→1332、预缓存状态、dist-check 状态）。

## 已执行修复（同日第三批，用户拍板"删假导航 + 同批顺手做"）

**改动的文件**：
- `index.html`
  - `<meta charset>` 之后新增**早期主题脚本**（内联，零请求）：读 `localStorage['boh-theme']` → 立即 `html.setAttribute('data-theme')` + `classList.toggle('dark')`；`system` 走 `matchMedia`；try/catch 兜隐私模式。语义与 theme-manager.applyTheme 逐字对齐，只是把时机从 mount 后提前到 HTML 解析期。**顺带消除了全站暗色 FOUC**（dark-mode.css 8 个文件是懒加载的）。
  - 骨架 style：删假导航 `.boh-boot-nav`（2 条规则 + DOM 节点）；新增 `[data-theme="dark"]` 的 html/body 底色兜底 #0a0a0f（= `--boh-bg-primary` 暗色，与 dark-mode.css 同特异性，后者加载后接管）；`.boh-boot-text` 暗色 #a1a1a6 → #8a919c；4 处选择器统一为项目惯例 `[data-theme=…]`。
- `vite.config.js`：删 `static/fonts/*.{woff,woff2}`（零匹配 warning 噪音）→ **重建后构建输出零 warning**。
- `package.json`：新增 `check:first-paint`（build 前）、`check:shell-precache`（build 后）并接入 `build:ci`。
- `.gitignore`：`dist-check`。
- 新增 `scripts/check-first-paint.mjs`：断言 7 组「骨架色值 === token 值」+ 骨架 style 位置 + index.html 内联 VALID_THEMES === theme-manager.js。
- 新增 `scripts/check-shell-precache.mjs`：入口 app-*.js 静态 import ⊆ sw.js 预缓存；带非空断言防"正则失配→空集→假绿"；多预缓存报 warning。
- `probe-verify-boot-changes.mjs`：扩为三场景（V1 暗色首帧 / V2 慢懒加载布局 / V3 暗色慢启动骨架底色）。

**两道门禁都做了反证测试**（关键，否则等于没有保障）：骨架色值改回 #a1a1a6 → 报错；theme-manager.js 删一个主题 → 报错；模拟 sw.js 少 ui-sanitize → 报错并指出缺失 chunk。三处临时改动均已恢复（`git diff -- src/utils/theme-manager.js` 干净）。

**复验实测**：
- 修复前：首帧 t=41ms `theme=null`、骨架底色 `rgb(255,255,255)`、骨架存活 2 帧中 0 帧处于暗色。
- 修复后：首帧 t=33ms `theme=dark`、骨架与 body 底色 `rgb(10,10,15)`、骨架存活 1 帧中 1 帧处于暗色；V3 慢启动骨架存活 3 帧底色恒暗、`bootNavRect=null`（假导航已删）。
- 门禁全绿：views / structure / liquid-glass / important-budget(1332) / first-paint / shell-precache；vite build 成功（预缓存 110 条 / 3571 KiB / **零 warning**）；eslint 新脚本 0 告警。

**另外更正记忆**：applyTheme 的具名容器是 **13 个**（不是 14），`.note-page` 已随 boh-note 下线、全仓无引用。

**未做（等用户决定）**：P3 四项 —— 两套超时文案合并、`<Footer v-if="bootReady">` 防御、perf 探针补 try/finally 与指标口径、根目录两份 HTML 报告迁到 output/。

## 清理第四批（用户确认"清"，P3 全清 + 探针归档）

- **两处超时文案统一**为「加载超时，可能是网络不稳定或刚刚更新了版本」（index.html 12s / App.vue 10s），两侧加注释互引：各自负责哪个窗口、为什么不会同时生效、改一处必须同步另一处。
- **`<Footer v-if="bootReady && !route.meta?.hideFooter" />`**（防御性；实测原本挂载但高度 0，无害）。
- **`probe-boot-perf.mjs` 重写**：补 `try/finally`；把口径错误的「首屏可交互」（判据是导航栏出现 → 实为 Vue mount 时刻）拆成 **FCP / Vue挂载 / 主体首帧 / 骨架可见时长** 四个互不混淆的指标（骨架可见时长才是体验改善的直接度量）；头注释写明需先构建 dist + dist-check 两份产物。
- **两份审计报告**（`dead-code-cleanup-plan.html` 67KB、`github-component-scout-report.html` 70KB）迁入 `output/`。顺带核实发现根目录还有 **4 份同类分析报告**（dark-mode-audit-report / style-remnant-audit-report / subscription-strategy-review / user-space-perf-audit-report）也一并迁入（同类、已跟踪走 git mv），否则"报告写 output/"的约定只落实了一半；根目录现在只剩入口 `index.html` + 12 个 demo 资产 + 已忽略的 `stats.html`。
  - 遗留：`scripts/style-remnant-audit.mjs:374` 仍硬编码把 `style-remnant-audit.json` 写到仓库根（待改 output/）；`dead-code-audit.mjs` 默认不落盘、需显式 `--out`，无此问题。
  - `plans/*.md` 里有探针名/报告名的**文字引用**（如 plans/007 提到 `probe-user-space-perf.mjs`），迁移后仍可检索到，未改写历史文档。
- **探针归档**：80 个 `probe-*.mjs` 从根目录 → `scripts/probes/`（77 个已跟踪走 `git mv`，3 个新建走 `mv`）。移动前已核对：无相对导入、无 `__dirname`/`import.meta.url`、`package.json`/`scripts` 无引用；脚本内 `dist/`、`output/` 等相对路径以 cwd（仓库根）为准，故从仓库根跑 `node scripts/probes/xxx.mjs` 行为不变。
  - **意外收益**：审计脚本的语料排除是目录**前缀**匹配（`^(tests?|scripts?|e2e|probe|tools?|sso|api)/`），根目录的 `probe-*.mjs` 其实一直是规则盲区，归档到 `scripts/probes/` 后才被正确排除。
- **记忆更正**：`dead-code-audit.mjs` 真实位置是用户级技能 `~/.workbuddy/skills/dead-code-safe-cleanup/scripts/`，项目内 `scripts/` 下没有（此前路径记错）。
- **本轮我又踩了一次自己记下的 BSD grep 陷阱**：`grep -n "a\|b" file` 在 macOS 上静默返回空，差点据此判定"审计脚本不存在"；换内置 Grep（ripgrep）才查到。教训重申：`\|` 交替一律用 `-E` 或 ripgrep。
- 验证：`check:first-paint` / `check:shell-precache` 通过；重建 dist（110 条预缓存 / 3571 KiB / **零 warning**）；探针从新路径跑通，V1（`data-theme=dark`、body 底色 `rgb(10,10,15)`、骨架 1 帧 100% 暗色）、V3（骨架底色 `rgb(10,10,15)`、假导航 null）全绿；截图仍写根 `output/`。
