# 2026-08-23

## 线上库直查复核（supabase REST + anon key，项目 ref: nplnlefdwfgtyimfkyih）

用户授权用 CLI 直查线上 Supabase 数据，进一步验证了待确认项：

- ✅ ai_quota_config 线上 9 行齐全（guest/free/plus/pro/max/ultra/boh-ai-plus/boh-pro/boh-max），**free 行存在**，"缺 free 行"不成立。
- ✅ **线上 daily_token_limit 已与 UI 完全一致**：free=20万/plus=80万/pro=200万/max=500万/ultra=1000万（guest=3万，人工调过）。即迁移文件里的旧值（plus=200万/pro=300万）在线上已被调成 UI 文案——**"UI 与实付漂移"在线上已不存在**，原分析基于迁移文件得出，已过时。
- ✅ 2026081904 已上线（lotteries 有 pity_reward_title 列）→ 保底阈值线上 = 24/18/12/8，与 UI 一致。
- ⚠️ user_subscriptions 匿名不可读（RLS），无法确认 legacy boh-plus/boh-ultra 是否真实存在；全量 32 用户档位：max×5/ultra×1/pro×2，24 人空（无订阅或未识别，RPC 无法区分）。
- 🐛 **新发现**：批处理 RPC `get_user_subscription_tiers` 线上对所有输入报 42702（user_id 歧义），迁移 2026070103 的函数有 bug，若有调用方会挂。
- ⚠️ bohai_model_configs 匿名不可读；若 min_tier 使用 legacy 别名（如 boh-ultra），normalizeTier 会归一为 free → 高档模式对所有人开放（真正的越权方向），需 service role 复查。
- 复核结论更新：原分析 7 条中，核心"漂移"两条在线上已不成立（已对齐），别名漏判代码层仍存在但影响面待确认，缓存 5 分钟窗口真实。

## 订阅层级权益一致性复核（只读审计，未改代码）

对一份"订阅层级权益真相碎片化"分析做了代码级复核，结论如下：

- ✅ 成立：权益数字多处硬编码。UI（SubscriptionPlans.vue BASE_PLANS/CODING_PLANS）、Edge Function（api-key-vault: WEB_SEARCH_DAILY_LIMIT_FALLBACKS/CODING_PLAN_BONUSES）、多处迁移（2026062601/2026063003/2026070204/2026071301/2026071602）各自写死数值。
- ✅ 成立（当前状态）：Plus UI 80万 vs DB 200万（2026071301 设 daily_token_limit）；另有 free 20万 vs 100万、pro 200万 vs 300万 的漂移（原分析未提）。
- ❌ 过时："Ultra 实际不限量"不成立——2026063003 曾设 -1，但 2026071301 已改为 1000万，与 UI 一致（2026070204 注释仍写"不限"，迁移内部注释矛盾）。
- ❌ 过时：抽奖保底 25/20/20/10 是 2026081802 旧值，2026081904 已改为 24/18/12/8，与 UI 一致。
- ✅ 成立但定性错：resolveUserTier 漏判 boh-plus/boh-ultra（只认 boh-ai-plus/boh-pro/boh-max），但后果是降级 free（少给权益），不是越权。DB 侧 RPC get_user_subscription_tier 同样漏 legacy 别名（影响抽奖保底判定），范围更广。
- ✅ 成立：缓存仅订阅成功时前端清（SubscriptionPlans.vue:527）；管理后台改订阅也会清（DataAdmin invalidateSubscriptionCache）；自然到期/直接改库无失效，5 分钟窗口真实（代码注释明说可接受）。
- ❌ 不成立（代码层面）：ai_quota_config 缺 free 行——迁移种子有 free(100万)/guest(20万)，无删除迁移；缺行兜底是 tokenLimit=0 直接拦截，并非"游客额度兜底"。
- ⚠️ 部分成立：TIER_ORDER/TIER_RANK 常量严格只 3 处（SubscriptionPlans/subscription-benefits/api-key-vault），算上 SQL RPC 排序 case 与 TIER_LABELS 共 6-10 处。
- ✅ 成立：缺一致性测试——subscription-benefits.test.js 只测 Cloud+ 数字，无别名分支/UI-DB 对照测试。
