# 2026-09-10

## 头像框设计规划（未写代码）
- 产出 plans/avatar-frame-design-plan.md：五档订阅（Free/Plus 8/Pro 20/Max 40/Ultra 70 积分月）对齐的个性化头像框方案。主推"毛孩俱乐部"猫狗主题（Free 纸箱猫灰环 / Plus 布偶猫蓝 / Pro 银渐层银 / Max 金毛金 / Ultra 三花&柴犬彩虹），备选果冻玻璃、方块像素、星轨金属、节气国风、IP 联名。
- 素材结论：MVP 用 AI 生成+人工矢量化精修 → 正式期门面款外包（约 ¥200–800/个）→ 素材库仅占位。交付 SVG 240×240 中心挖孔（内径预留 6–8% 间隙）+ 暗色提亮版 + 24px 预览。
- 落地场景 P0：论坛帖卡/回复 xs、帖子详情评论、个人主页大头像位（唯一动效位）；P1：UserSpace 四面板、关注列表；触发：订阅支付成功弹层点亮、Pro 试用临时框 3 天回收、到期置灰保留解锁史。
- **发现不一致待修**：订阅页文案 Pro/Max 都写"金色昵称"，但 subscription-benefits.js 映射 pro=nickname-silver、max=nickname-gold；头像框主色跟昵称色同源，立项时必须先统一。
- 复用事实：等级查询走 get_user_subscription_tier(s) RPC（5min 缓存，useUserTier），头像 URL 统一 getAvatarUrl（sm/xs），商店已有"BOH 装饰"品类可挂框。

## 头像框 Demo（HTML）
- 产出根目录 avatar-frame-demo.html：单文件无依赖，SVG symbol 单源（5 框 + 3 占位头像），框环色走 CSS 变量随 html[data-theme] 亮/暗切换（暗色环提亮蓝→#2997ff）。
- **用户拍板：Pro 定稿银色**（代码 pro=nickname-silver 是对的），订阅页"金色昵称"文案待修；Demo Pro 卡片已带"文案待修"角标。
- Demo 含：五档全家福 148px / 24–128px 尺寸阶梯（JS 动态生成）/ 论坛帖卡+评论楼层+个人主页场景模拟。挖孔几何：头像 r82 / 环 r100 w7（内径 96.5，间隙≈6%）。

## 头像框 Demo V2（用户反馈：太简陋 + 一圈白边）
- **白边根因**：头像 r82 与环内径 96.5 之间 6% 间隙露页面底色。修法：头像放大 r96 贴环内缘（环 r103 w9 → 内径 98.5，间隙<2%）+ 加内圈阴影环（r97 黑 op.08）。规划文档里"预留 6-8% 间隙"的保守值实测观感差，已按贴合式修正。
- **精细度升级**：Pro/Max 环用多段线性渐变模拟金属拉丝 + 白高光弧 + 深暗部弧；项圈加缝线 dashed + 铆钉；铃铛改真铃铛造型（顶环+钟形+底球+高光）；Max 加环上爪印+骨头狗牌；Free 加纸箱（翻盖+胶带+爪印章）；Ultra 加围巾流苏+五处四角星光；猫脸头像加大眼双高光/ω嘴/脸侧胡须/额头狸花纹。
- 注意：Write 该 demo 文件时会被系统注入 data-page-node-id 属性导致"modified since read"，重写前必须先 Read。

## 自制头像框全站接入调研（用户要自己画 PNG 框）
- 产出 plans/avatar-frame-integration-spec.md。全站头像普查结论：**约 31 处渲染点、无统一组件**，圆形 21 处 vs 圆角矩形 10 处；getAvatarUrl 仅 5 处用（其余直连原始 URL）；无装饰框层先例（只有在线点/编辑徽章/红点等功能型叠加）。
- **三个动态形状陷阱**：①PostDetail 周年皮肤把 author-avatar 改 2px 方形；②PostCard:446 移动端 media 把 42px 作者头像 50%→12px；③shell-community(.user-avatar 44px 用 --radius-avatar=14px) 和 Lab(--radius-sm=6px) 用共享变量控圆角——只能改 class 不能改变量。
- 统一圆形清单（10 处）：PostCard .reply-avatar 12px、CommentThread .mini-avatar 9px、profile-panels .apple-avatar 24px、BlockWall 10px、Partners 12/18px、Messages .x-avatar 18px、BetaPreview 14px、shell-community、Lab、PostCard 移动端覆盖。
- 叠加方案：wrap(position:relative, overflow:visible) + frame 层（absolute inset -12% / 124% / pointer-events:none / z-index:3），圆角留在 img 上。绘制规格定稿：**1024×1024 RGBA PNG（内孔直径 824 全透明、外沿≤984、最细线≥24、主环带 64-120、≤400KB）**，框层 124% 时内孔恰好=头像直径无缝。512 源在主页 180px 3x 屏欠采样（需 669px）故主推 1024。
- 接入分两阶段：Phase1 无表变更（public/avatars/frames/ + CSS 变量挂自己）；Phase2 profiles 加 avatar_frame_url + 作者序列化带出才全站他人可见。树洞匿名/官方号/周年皮肤期间不挂框。

## 全站统一圆形落地 + 打样图（晚间）
- **统一圆形已改码（10+ 处）**：PostCard .reply-avatar 12px→50% + 移动端 media 12px→50%、CommentThread .mini-avatar 9px→50%、profile-panels .apple-avatar 24px→50%、BlockWall .mini-avatar 10px→50%、Partners .mini-avatar 12px + .partner-avatar-img 18px→50%、Messages .x-avatar 18px→50%（含移动端 media 10px）、BetaPreview .release-avatar 14px→50%、shell-community **直接改 --radius-avatar 变量为 50%**（该变量仅此文件 2 处用）+ 4 处 media 硬编码（12/10/10/11px）→50%、Lab .message-avatar var(--radius-sm)→50%。复扫 grep（multiline 负向断言）确认 avatar 类无残留非圆；周年皮肤 2px 方形保留（[data-anniversary-skin] 前缀选择器，grep 模板匹配不到属正常）。
- **基建**：新建 src/styles/common/avatar-frame.css（boh-avatar-wrap + boh-avatar-frame，top/left -12% + 124% + pointer-events:none + z-index:3，暗色 drop-shadow 增强，[data-anniversary-skin] 下 display:none），已在 main.js tokens 后引入。
- **打样图**：avatar-frame-template.html（根目录）。canvas 1024 绘制：红虚线内孔 Ø824（中心标"头像露出区/必须全透明"）、蓝实线外沿 Ø984、淡虚线十字轴、**四角口袋图例**（左上画布/PNG、右上外沿、左下主环带/最细线、右下格式/体积——初版图例放左上会压内孔区，改四角）。功能：干净模式（Esc/按钮退出）方便截图、下载 BOH-avatar-frame-template-1024.png（真透明底可导入 Procreate 打底）。棋盘格用 CSS repeating-conic-gradient 垫在 canvas 后面，不进导出图。

## 订阅体系诊断（只读调研，产出报告）
- 产出根目录 `subscription-strategy-review.html`：五档订阅（Free/Plus 8/Pro 20/Max 40/Ultra 70 积分月，年付=10个月）的定价与权益诊断 + 三层方案 + 路线图 + 3 个决策点。**未改任何代码**。
- **P0 未修漏洞 · 服务端不校验价格**：`subscribe_with_points` 与 `start_subscription_trial` 的 p_points_cost / p_duration_months / p_plan_code / p_duration_days 全为调用方传参，RPC 只拒负数（2026090903:76-83、:175；2026082609:63-69）。登录用户可直连 RPC 传 points_cost=0 + duration_months=120 → 0 积分拿 10 年 Ultra；试用可传 ultra/90 天。修复=建服务端价格表按 (plan_code, billing_cycle) 取价，忽略传参。
- **权益真相源四处分裂且已不一致**：订阅页硬编码（SubscriptionPlans.vue:282-288 / 对比表 :320-330）vs `ai_quota_config.daily_token_limit`（2026071301:8-19）vs `boh_cloud_image_limit_for_user`（2026081903）vs useLabQuota TIER_QUOTA_MAP。**实际日 Token = Free 100万/Plus 200万/Pro 300万/Max 500万/Ultra 1000万，页面宣传 = 20/80/200/500/1000 万，前三档全不符**（宣传值疑似沿用 guest 档 20 万的旧口径）。
- **单位价值倒挂**：每积分日 Token = Plus 25万 > Pro 15万 > Max 12.5万 < Ultra 14.3万；Pro 是凹点且是主推档。Plus→Pro 价格 ×2.5 但 Token 仅 ×1.5，增量单价 12 积分/百万 vs Plus 自身 4 积分/百万。
- **付费墙仅前端**：`record_lab_usage`（2026070203:78-108）只 insert 不校验次数，PPT/Word/代码月度限额只在 useLabQuota.js:10-17 判断，可绕过。页面"10/15/20/30/不限"不可信。
- **试用以外的权益缺口**：`boh_cloud_image_limit_for_user` 只认 `status='active'`，不含 'trial'（2026081903:29）→ Pro 试用期 Cloud+ 仍是 150 张而非 450，与"完整体验 Pro"承诺不符（get_user_subscription_tier 等三处已纳入 trial）。
- **积分经济闭环断**：周签到 5 积分（2026090905:263）≈ 21.7/月 ≥ Pro 月费 20 → 签到即可长期覆盖 Pro。
- **不可验证承诺**：对比表"Agent 任务并行 1/1/2/4/8"全仓无实现（orchestrator-prompt.js:40 对所有人写 "2~4 个 Worker"）；页面"多模态 free:false"与首页 AgentPreviewHero.vue:6"多模态 Free 开放"直接矛盾。
- **未变现通道**：Coding 附加包机制已就绪（2026080101 + fields.js:129-134 的 coding-lite/plus/pro/ultra min_tier）但订阅页未上架；日 Token 超限硬断、无加油包。
- **无降档路径**：RPC 对低档直接返回 LOWER_TIER_NOT_SUPPORTED（2026082609:347-355），且无自动续费/宽限期 → 压低首次付费意愿。
- 与既有规划的关系：`plans/007-beta6-plan.md:105-111` 已识别"充值二维码是转化漏斗最大漏水点"与"权益四套真相源"；`plans/avatar-frame-design-plan.md:21` 已记录 Pro 昵称色文案矛盾。本次新增的是服务端价格漏洞、Token 梯度不符、签到覆盖 Pro、Lab 前端校验、trial 云盘缺口、Agent 并行无实现。

## 订阅诊断复核（同日晚，逐条核实）
- **全部 P0/P1 结论核实成立**；0903 之后的迁移（0907/0908=主视觉数据、0910=敏感字段 RPC）不触及订阅函数，无"被后续修复"情况。
- **AI Token 配额是服务端强制的**（比报告初稿判断更强）：运行时在 Edge Function `api-key-vault/index.ts`——getQuotaPolicy 读 ai_quota_config.daily_token_limit → reserve_ai_token_quota 预扣 → 超限 429「今日 BOH AI Token 额度已用完」。tier 来源 resolveUserPlans（查 user_subscriptions，**含 'trial'**，5min 缓存）。
- **F5 更尖锐的实锤**：试用期间 Edge Function 给足 Pro 的 AI 额度（resolveUserPlans 含 trial），前端 subscription-benefits.js 也含 trial 显示 450 张，但云盘上传闸门（2026042802 触发器调 boh_cloud_image_limit_for_user，2026081903 只认 active）第 151 张就拦 → UI 承诺/前端展示/AI 侧全对，唯独上传闸门漏了 trial。
- **F6 升级为「假限制」**：`min_tier` 列 default 'free'（2026071401:6），五种种子模式（fast/pro/multimodal/plan/agent-cluster）均未设 min_tier、无后续 update → 默认配置下「多模态 Free 不可用」「Agent & Plan 仅 Max/Ultra」都不成立，全部档位（含游客）可用。例外：管理后台可运行时改 min_tier，需查线上库确认。
- **F8 修正：Coding 附加包是「上线后被撤」不是「未开发」**：订阅页曾有完整售卖区，8/25 commit 5a0baa21 大重构（2800→900 行）删除；服务端仍在生效——api-key-vault 有 CODING_PLAN_RANK 门禁 + CODING_PLAN_BONUSES 加成（coding-lite +50万Token/日+10次联网 → coding-ultra +600万+120次）；管理端 fields.js:66-69 留有 ¥5/12/20/35 价格标签。恢复上架=补回前端入口。api-key-vault 里"与订阅页 CODING_PLANS 文案一致"注释已过时（页面无此常量）。
- **新增真实分层杠杆**（写方案时漏了）：TIER_MAX_OUTPUT_TOKENS（free 1200/plus 1800/pro 2400/max·ultra 4096）与 web_search_daily_limit（10/30/60/120/240），均在 Edge Function 强制，权益单一源应收编。
- FAQ 写「每日签到」但实现只有周签到（forum_weekly_checkins），文案不符（小）。
- 报告已同步修正：TL;DR/F5/F6/F8/4.3/3.4/路线图 P2/footer。

## P0 止血迁移已写好（待用户确认后 push）
- 产出 `supabase/migrations/2026091001_subscription_server_side_pricing.sql`，dry-run 通过（远程仅缺此迁移，无占用冲突）。**尚未 push，等用户确认后 `supabase db push --yes`。**
- 四件事打包：① 新表 `subscription_plan_prices`（RLS 开、无用户策略，仅 service_role/definer 访问），种子 = 页面价 Plus 8/Pro 20/Max 40/Ultra 70，年付=×10、12 个月，PK (plan_code, billing_cycle)；② `subscribe_with_points` 重写：按表取价取时长、忽略调用方传参，无价格记录（free/coding-*/未知码）返回 PLAN_NOT_AVAILABLE，三态/折算/流水逻辑逐字保留 0903 基线，plan_code 归一化 lower(trim())，metadata 加 `price_source:'server'`，签名不变；③ `start_subscription_trial` 锁定 pro/3 天，忽略传参（全参数带默认值），两守卫保留，签名兼容；④ `boh_cloud_image_limit_for_user` status 纳入 'trial'。
- 兼容性已核：前端传参（SubscriptionPlans.vue:452-458 pointsCost=页面价、durationMonths=1/12、trial=pro/3）与服务端取值完全一致 → 合法用户零变化，仅堵直连伪造。顺带修掉旧行为里『0 积分插垃圾 free 订阅』的路径。
- 自检修过一个 bug：trial 函数 v_plan_code/v_days 未声明（已补 declare）。
- 定价口径决策（用户拍板）：**不强调「1 积分=1 元」锚定**，页面以积分为主单位即可；报告 3.1/决策二已按此改写。

## 2026091001 已推送上线（21:58，用户确认）
- `supabase db push --yes` 成功，远程仅应用了 2026091001 一个迁移，无冲突。
- 远程验证全过：① 价格表 8 行种子正确（4 档 × 月/年，年付=×10/12 个月）；② pg_proc 确认 subscribe_with_points 函数体含 PLAN_NOT_AVAILABLE 守卫与 price_source 审计标记；③ 三函数烟测可执行（db query 以 postgres 身份 auth.uid()=null → 正确返回 NOT_AUTHENTICATED；boh_cloud_image_limit_for_user(随机uuid)=150）；④ **事务内 set role authenticated + 伪造 jwt.claims 模拟登录态：'free' 与 'coding-lite' 购买均返回 PLAN_NOT_AVAILABLE**（rollback 无残留）——服务端取价守卫端到端生效。
- 未做的验证：真实浏览器登录态下直连 0 积分 ultra（需真实 JWT）；逻辑上 ultra 月付现在会按 70 扣、积分不足返回 INSUFFICIENT_POINTS。
- supabase CLI 2.108.0：db query 用位置参数传 SQL（`--command` 不是合法 flag）。

## 旧样式残留全站审计（只读扫描，产出报告）
- 新增 `scripts/style-remnant-audit.mjs`：引用链扫描（ESM import/动态 import/@import url()/`<style src>`/css→css 传递五种形态）+ 全站语料分词死类判定 + token 四家族统计 + 主题机制/字体/backdrop-filter/z-index 检查。产出 `style-remnant-audit.json` + 报告 `style-remnant-audit-report.html`。
- 关键结论：**8 个死 CSS 文件共 4918 行**（BOHAI responsive-layout/compact-input/embedded-animations 拆分迁移未删旧件、Health 与 freemodels-config 旧版、3 个纯注释桩）；**icomoon 图标字体体系全死**（fonts.css 86 行+woff 6.4KB，main.js 还全局引入，字形类零使用）；**全局死类 1372/3658（37.5%）**——glass-ui.css 76% 死（liquid 之前的旧玻璃层）、animations.css 89%、section__header.css 74%；旧工具类 kit（box-shadownone/.padding 等）在 11 个全局文件重复定义且全死；mixins.css 空文件、variabls.css 拼错名。
- Tailwind v4 引擎 0 使用（工具类全站零命中）但 preflight 仍在全局生效，style.css @theme 30 个 shadcn token 在用——不能整删，需"迁 token→摘引擎→视觉回归"三步退场。
- 孤儿 token：--liquid-brand 未定义（Newsroom 5 处靠 fallback #0071e3）、--hero-preview-height 死旋钮（ShowcaseBookHero 4 处永远走 fallback）；--ring-*/--boh-visual-height 是 JS 运行时注入非孤儿。body.page-* 死选择器 14 组（全站无人挂载 page-* body 类）；--apple-* token 38/59 死；data-boh-theme 旧机制 91 处、prefers-color-scheme 散装暗色 11 文件；multiScoped 打包重复（google-components 7× 且 main.js 还全局一份）；z-index 已见 int32 天花板段 26 处。!important 1396=基线，门禁通过。
- **环境坑**：沙箱 FS hook 会静默 SIGKILL 大量读文件的 node 进程（exit 137 无报错），审计脚本同样要前缀 CODEBUDDY_BROKERED_FS_HOOK_ENABLED=0 CODEBUDDY_SAFE_DELETE_SANDBOX=0。扫描器初版漏 `<style scoped src>` 形态导致 16 文件误判死文件，已修复。

## P1 清理执行（9/10 下午，已验证）
- 删除 12 文件：8 死 css（BOHAI 三件套/Health/freemodels/3 桩）+ fonts.css + icomoon.woff + mixins.css + variabls.css；main.js 摘 fonts/variabls/mixins 三 import；scripts/p1-strip-utility-kit.mjs 从 10 个全局文件摘 37 个死工具类块（buttons 的 `.box-shadownone,.btn:hover` 摘头保活、globalpage `.box-sizing,body` 保 body、footer `.border-none,.footer-home` 保 footer 规则）。净 -5,416 行。
- **body.page-* 误报抓回**：复核时发现 9/8 暗色修复记忆与"死规则"结论矛盾，彻查找到 App.vue:71-76 `page-${route.name}` 动态挂载 → 取消删除并在报告更正。探针 body="page-home is-loaded" 实锤。
- 验证链全绿：审计重扫死文件 0/死类 1133；vite build 17.2s 零错误；!important 门禁 1390<基线 1396（门禁脚本顺手修了 ENOENT 崩溃——git ls-files 含已删文件时 readFileSync 会炸，补 existsSync 跳过）；probe-nav-dark 四页 ALL PASS 零 pageerror。
- 坑两枚：①这批 SCSS 编译 css 是 CRLF，横幅注释是三行一体的单注释块（`/* ***\n* Title\n*** */`），批量删除正则按两段式写会 0 匹配；②bash grep 的 `\|` 交替不可靠（又踩一次），类名复查必须用内置 Grep 工具。
