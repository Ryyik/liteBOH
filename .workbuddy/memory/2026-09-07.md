# 2026-09-07

## 数据管理面板全量查错
- 修复新闻保存崩溃：`Cannot read properties of undefined (reading 'includes')`。根因是 `DataAdmin.vue` 的 `createFieldValidator` deps 漏注入 `NEWS_CATEGORY_VALUES`，保存时逐字段校验 `news.category` 走到 `deps.NEWS_CATEGORY_VALUES.includes()` 崩溃。修复：DataAdmin.vue 补注入 + `useDataAdminValidation.js` 加 `Array.isArray` 防御守卫并补 JSDoc。
- 全量审计结论（composables 13 个 + config + EditDrawer + saveStrategies）：工厂 deps 注入现已全部对齐；`TAB_WRITABLE_FIELDS` 覆盖全部 20 个保存策略 tab；`TABS_KEEP_ID_ON_INSERT` 与数字 ID 策略一致；模板层 tags/specifications 均有 `|| []` 兜底。`lotteries` 的 `pity_overflow_reward_title/description` 是死代码默认值（tables.js 无此字段、不保存），无害。
- 回归探针：`probe-datamanagement-validator.mjs`（6 项校验器单测，全部 PASS）、`probe-datamanagement-smoke.mjs`（vite 编译 + 页面加载 0 错误）。

## 竖屏发帖编辑器重构（PostComposer）
- 修复正文不全宽：`.composer-body-shell` 是 flex 子项被 shrink-to-fit 挤成 textarea 默认 cols 宽度，补 `flex:1; width:100%; min-width:0`。
- 标题 input→自动增高 textarea：maxlength 50、`composer-title-count` 计数、`autoResizeTitle`（watch+onMounted，上限 132px）。
- 字体根因：弹层 Teleport 到 body，input/textarea 默认等宽字体；弹层根与 .editor-card 显式中文字体栈 + 表单元素 `font-family: inherit`。
- 用户两轮拍板最终形态：纯白背景、悬浮圆角玻璃顶栏（22px 圆角）、发布=纯蓝胶囊（#0071e3）、玻璃设置组（20px 圆角）+ 彩色图标 chip（位置绿/草稿橙/标签蓝）、添加图片=玻璃卡蓝加号、头像行显示 用户名+「公开」徽标（greeting-full/greeting-name 双形态切换）；第一版的渐变氛围底+色斑被否决已删。
- 暗色：弹层根覆盖 `--liquid-text-*`（tokens.css dark 未派生文字 token）+ user-avatar dark 适配；drawers-skeletons 里竖屏编辑卡的 24% 白+blur !important 改为全透明（纯白底下无意义还费性能）。
- 探针：probe-mobile-composer.mjs（420x900 注入 Pinia 登录态→FAB→度量+截图）、probe-desktop-composer.mjs（1440 回归）、probe-composer-dark.mjs（dark 截图）。Light/Dark/桌面三端 0 报错。

## 提交 a180938（多图上传并发+预热管线）测试验证- 测试全绿：ESLint 0 error（24 个警告全为旧代码既有，已核对 HEAD~1）；单测 30/30（image-compression/forum-post-api/cloud-upload-guard/forum-image-moderation）；集成 20/20；vite build 通过（须 `--emptyOutDir false` 覆盖写，npm script 走 build:ci 会被 safe-delete shim 拦 dist 清空）。
- 新探针 probe-commit-pipeline.mjs：发帖器选 3 张真实 PNG → 预热管线全链路验证。通过轮次 4~9s 全部「已就绪」，Cloudinary 3 个上传请求间隔 20~63ms（3 路并发生效）；移除图/关闭面板 0 pageerror。probe-webp-compression.mjs：直调 compressImageFileToUploadLimit，PNG 176KB→webp 56KB、JPEG→webp，文件名/MIME 归一化正确。
- 发现环境性问题（非该提交引入）：NSFW 模型 jsdelivr CDN 偶发 >20s（SCRIPT_LOAD_TIMEOUT_MS），首图标记「处理失败」并取消其余预热（约 40% 轮次）。失败可重试（getNsfwModel 失败会重置 modelPromise 缓存、发布时现场重建管线），旧代码同样会在发布时失败。可考虑：加长超时或自动重试一次。
- 压缩单测只覆盖 getImageCompressionPlan/format，未覆盖 normalizeCompressedFile 的 webp 归一化（本次直测补位）。

## BETA 6 版本规划（产出 plans/007-beta6-plan.md）
- 三个 Explore 子代理并行摸底（订阅/积分/用户空间、首页/新闻/活动、视觉/性能/工程化），结论即本的第一个 million。
- 核心判断写进文档：站点病根是「结构重心失衡」而非性能——`posts` 无 source_type/source_id 外键，导致首页/新闻/活动三板块无法沉淀为用户资产 → 订阅只能卖功能（Token/云盘）而非身份。所以 BETA 6 主题 = 补一条贯通主链，不是再造新板块。
- 四条主线：P0-1 Content Spine（posts.source_* + useEngagement + useUnifiedFeed）、P0-2 商业化收口（支付回调 + entitlements 单一层 + 账本为真相）、P1-3 活动平台化（activity_campaigns 终止一活动一套表）、P1-4 首页编排化（hero 定向/start_at-end_at/happening 头部/promo-modal 注册中心）。
- 投入配比建议：中台层 35% / 首页 20% / 用户空间 15% / 活动 15% / 视觉 10% / 技术债与安全 5%（最后一项标注「不参与排期博弈」，因 BETA6 接支付会让 API Key 硬编码+RLS 缺失从泄密风险升级为资损风险）。
- 用户偏好：先口头说不要 MD 要对话输出，随后又改口要文件。最终两者都给了。

## 与另一份 BETA 6 研究报告交叉验证（并入 plans/007-beta6-plan.md 附录 C）
- **最有价值发现（我方漏）**：glass-ui.css:18-20 三个 filter 变量自引用 → CSS guaranteed-invalid → 全站 .glass-container-* 的 backdrop-filter 实为 none，毛玻璃静默失效。佐证是 14-17 行定义的 --glass-blur-* 三档完全没被引用，属「漏写 blur()」笔误。已提升为文档的 P0-0，并存入 MEMORY.md。
- **我方错误被纠正**：token 数错——实际是四套并行（--liquid-* / style.css 的 @theme / --apple-* / --boh-*），此前只查了 tokens.css 和 style.css，没做全仓命名空间扫描。教训：统计"有几套 token"必须 `grep -o -- "--[a-z]*-"` 全仓去重，不能只看两个文件。
- **对方错误（我方核否）**：称「两套 hero 系统共存」不成立——HeroSection.vue 全仓 0 引用，是孤儿，正确处置是删除或改造为基类，而非合并两套系统。教训：判断"在用"必须 grep import，不能凭文件存在。
- **对方有效洞察（已采纳）**：页面内搜索/筛选控件藏在 navbar island 导致可发现性风险；用户成长路径缺可视化；新闻/帖子/活动均缺分享能力；首次访问无引导。
- **战略分歧**：对方主张自下而上先清偿设计系统债（30% 给了 token 合并）；我仍主张自上而下先通数据主干。核心论据：视觉债是付利息的债，数据主干债是结构性债（每新增一个板块重付一次）；且在 posts 无 source_type 时做"首页动态流"只能前端拼三张表，反而制造新胶水代码。另指出对方 token 合并估 1-2 周偏乐观（真实 3-4 周）。
- 修订配比：视觉收敛 10%→15%，活动平台化 15%→10%，中台 35%→30%，新增 P0-0 玻璃修复+回归 5%。

## a180938 引发的发帖三连回归修复（卡2%/提前加载态/重试失效）
- 用户实测反馈三症状：①选图后图片卡片提前显示「优化中/待审核」+转圈且极慢 ②点发送进度死停 2% ③失败后重试永远秒败。根因全在 a180938：
  1) 重试失效（最重）：processPublishImagesForQueue 失败分支只 cancel 其他图的管线，失败图自身 entry（state=failed）残留 draftImagePipelines；retryPublish 的 scheduleDraftImagePipeline 被 has(uploadId) 守卫拒绝重建 → 拿到旧 failed promise 秒败死循环。修复：失败分支补 cancelDraftImagePipeline(img.uploadId) + scheduleDraftImagePipeline 对 failed 残留删除重建。
  2) 卡2%：进度模型从细粒度（压缩/上传 onProgress 连续推进）改成整图 resolved 才 +80/total。修复：entry.progress 字段（压缩0-0.35/检测0.35-0.5/上传0.5-1.0，uploadApprovedForumImageQueued 透传 onProgress）+ processPublishImagesForQueue 里 300ms interval 聚合轮询，finally 清理。
  3) 提前加载态：Beta5 选图即预热后，管线 updatePendingPostImage 写 optimizing/queued/uploading → PostComposer badge+spinner。修复：prepareForumImageForUpload 加 options.quiet（管线静默，stable 路径不变）；shouldShowImageStatus 收紧为仅 failed；移除 is-processing class 绑定与 CSS。
- 附带修复：forum-image-moderation.js loadScriptOnce 的 script.onerror 现在会 remove() 死标签——此前 CDN 失败后残留 `<script data-boh-dynamic-src>` 会让后续所有重试命中「已存在标签」等待分支白等满 20-30s 超时（审核转圈极慢帮凶）。Beta5 选图分支恢复 scheduleForumImageModerationPreload({immediate:true}) 与压缩并行；灵动岛无图帖文案「审核中」→「正在发布」。
- 验证：vite build ✓；probe-composer-publish-fix.mjs 三段闭环（CDN 先延迟3s abort→失败→放行注入 stub nsfwjs/tf→重试）全部通过：A 预热期 0 spinner/0 processing badge；B 进度 12→30 连续推进后正常报失败；C 重试→管线重建→全链走通→发帖成功。mock 要点：Cloudinary 返回需带真实 cloud_name 路径(/dkqae7j1m/image/upload/)与 folder 前缀(boh-cloud-plus/forum/)否则 validateCloudinaryUploadResult 拒绝；create_forum_post_with_images RPC mock 须返回单对象非数组（normalizePostRecord 期望对象）。
- 探针补充知识：移动 overlay 发送按钮是 ForumMain 渲染的 .mobile-composer-submit（PostComposer 的 .post-btn 在移动端 display:none）。

## 上传管线 P0 优化落地（B1/B2/C1/C2/C3/A1/D2/E1）
- **B1 模型本地化（最大收益）**：nsfwjs@4.3.0 + @tensorflow/tfjs@4.22.0 npm 化。关键机制：nsfwjs npm 包内 `esm/model_imports/*.js` 用动态 import 引 `dist/models/*/{model.min.js,group1-shard*.min.js}`（UMD 权重脚本），`nsfwjs.load('MobileNetV2')` 按注册表自动加载——**无需 CDN、无需 script 标签、无需 public/ 拷贝**。forum-image-moderation.js 重写：删光 loadScriptOnce/CDN 逻辑，改 `Promise.all([import('@tensorflow/tfjs'), import('nsfwjs')])`。
- **manualChunks 三个坑**：①`nsfwjs/dist/models` 全归一个 chunk 会把三模型权重合并成 39MB；②排除规则只写 mobilenet_v2/ 时，inception/mid 落回 `node_modules/nsfwjs` 匹配进 nsfw-vendor（36MB）——必须显式 `return undefined` 让它们自然分块。最终：tfjs-vendor 1.09MB/gz269KB、nsfw-weights 3.6MB/gz2.5MB（MobileNetV2 权重）、nsfw-vendor 40KB，全部按需加载。
- **B2**：vite.config runtimeCaching 加 `/static/js/(tfjs-vendor|nsfw-vendor|nsfw-weights)-*.js` CacheFirst（hash 文件名内容不可变，90 天）。
- **B3**：MODERATION_SURFACE_MAX_SIDE 448→224（MobileNetV2 输入原生 224，省 resizeBilinear 二次插值）。
- **D2 输入让路**：moderation 模块内 document 捕获 input/keydown 记 lastUserInputAt，classify 拿锁后 `yieldToTyping()`（静默 150ms 窗口，最多等 800ms 防饿死）；选图预热加 300ms debounce（批次过滤已移除图，clearAllDraftImagePipelines 清 timer）。
- **C1**：getCurrentSupabaseUserId 60s TTL 缓存 + onAuthStateChange 失效（此前每图 3 次 auth RTT！）；assertCloudinaryUploadAllowed 60s 成功缓存 + in-flight 去重（失败/限流不缓存）。
- **C2**：pending 登记并行化不阻断（失败仅 logger.warn，兜底清理时限远大于登记延迟，claim 竞态可忽略）。cloudinary-client.js 需补 import logger。
- **C3**：sendUploadRequestWithRetry 3 次尝试（500ms/1500ms 退避），错误挂 `error.status`（XHR/fetch 两路径），仅无 status（网络层）或 ≥500 重试，4xx 直接抛。context 降级重试保留在外层。
- **A1**：压缩首击率分桶（≥8MB→0.78 / ≥4MB→0.82 / 其余 0.85）+ maxIteration 8→4。
- **E1**：ForumMain `online` 事件自动 retryPublish 全部 network 失败项（2s 节流）。
- 验证：build ✓（chunk 体积正常）；probe-composer-publish-fix.mjs 更新后全绿——mock 改为 Cloudinary 首次 500×3（自动重试生效的证据）、重试放行后成功；Phase C 成功即端到端验证了本地模型真实 classify 返回 approved（上传前强制校验 moderation.status）。
- **单测回归修复（P0 收尾）**：C1 模块级缓存（userId/预检）跨用例泄漏导致 5 个用例挂（未登录/限流分支被缓存短路）→ cloudinary-client.js 导出 `__resetCloudinaryUploadGuardsForTests()`，测试 beforeEach 调用；C2 行为变更（pending 失败不再 reject）更新契约断言；C3 补「4xx 不重试」与「5xx 重试两次后成功」两用例。最终 5 文件 112/112 全绿。教训：模块级缓存必须配测试重置钩子，否则 mock 分支不可达。

## A2 压缩/检测解耦 + 生产预览验证（P1 第一批）
- **A2 实现**：scheduleDraftImagePipeline 拆两级——压缩进 2 路并发池（COMPRESSION_CONCURRENCY，deviceMemory<4 降 1 路；acquireCompressionSlot/releaseCompressionSlot FIFO 计数器），检测仍挂 draftPipelineChain（链任务内 await compressionPromise 再 moderate），draftPipelineChain=preparePromise.catch(noop)。多图总耗时从相加变取 max。entry.promise=moderationPromise.then(上传) 不变；取消/失败/进度字段全兼容。
- **生产预览验证（probe-production-preview.mjs @ vite preview 4173）四项全过**：SW 接管 ✓；模型 chunk 真实网络加载+缓存写入（caches['nsfw-model-vendor'] 2 条）✓；生产构建发帖全链成功（真模型推理 approved）✓；**reload 后二次选图 3 条响应全部 fromServiceWorker=true（CacheFirst 命中，缓存条目不增）**✓。
- **两个关键排查教训**：
  1) `vite preview` 遇到不存在的文件（旧 sw.js precache 请求已被覆盖式 build 替换 hash 的 chunk）会 ReadStream ENOENT 未捕获直接崩进程——**覆盖式 build（--emptyOutDir false）+ SW 站点 preview 必须先干净重建 dist**。
  2) **Playwright page.on('response') 对 SW 缓存命中的响应同样触发事件**——「有事件」≠「走了网络」，必须用 `response.fromServiceWorker()` 判定；且同页二次 `import()` 因 ES module map 缓存直接 0 事件，reload 后才有事件。此前误判 CacheFirst 未命中，白查一轮。
- 遗留：404 一条（favicon 类，无关）。真机手测清单（用户自行）：打开发帖器→选图→打字流畅度→发送→飞行模式切换看自动恢复。

## BETA 6 产品与架构规划（只读调研 + 规划，未改代码）
用户要求输出六项：优先方向 / UX结构缺陷 / 视觉优化 / IA 方案 / 开发重心 / 下阶段能力。全程只读，最后的 MD 文件按用户要求删除，结论直接对话输出。
- 核心判断：站点病灶不是性能而是「结构重心失衡」——论坛+用户空间按产品在做，首页/新闻/活动按运营物料在做（一次性页面+一次性表+一次性视觉）。BETA 6 主题应是「补一条贯通全站的主链」，而非再加第五块内容区。
- 关键架构事实（后续复用，勿重复调研）：posts 表无 source_type/source_id 外键 → 活动不产出帖、新闻不可转帖、英雄区挂不了 UGC；HeroSection.vue 全仓 0 引用（孤儿）；subscribe_with_points 不写 points_transactions（表里 reason='subscription' 预留未用）；充值仅二维码人工核对；权益判断有四套真相源（subscription-benefits.js / useUserTier.js 之外，SubscriptionPlans.vue、ProfileMain.vue、Shop 各判各的）；活动=一活动一套表（仅抽奖有完整生命周期管线），activities 表只有 5 字段是「历史相册」；Newsroom 详情是模态框无路由无 SEO。
- 量化现状：src/styles 12847 行 / 736 处 !important（forum-dark 177、unified-nav 157）；两套 token（tokens.css --liquid-* vs style.css @theme）；暗色仅覆盖 9 个页面；上帝组件 DataAdmin 6043 行、Lab 4358、ForumMain 4302；近 100 commit 含 38 条 fix，forum composer 复发 7 次、image/upload 7 次；PROJECT_MANUAL 5.1 三项高危（API Key 硬编码/RLS 缺失/客户端权限）仍 ⏳ 待修复。
- 建议优先级：P0 内容主干 + 商业化收口（支付回调/单一 entitlements/账本为真值）→ P1 活动平台化 + 首页定向编排。人力配比建议：中台 35% / 首页 20% / 用户空间 15% / 活动 15% / 视觉 10%，安全 bearer 另计不入排期。
- 用户偏好确认：规划类产出偏好直接在对话里输出，不要落 MD 文件。

## 离线总结（智能概览灵动岛）自动触发逻辑修复
- 问题：每次上线都自动推送离线总结，含上次上线已浏览过的内容。根因：推送窗口用「上次会话最后心跳时间戳」（`offlineAnchorAt` = DB last_active_at 快照）做秒级锚点，上次在线日当晚新发的内容次日仍被判为新增；且已读去重只有 sessionStorage（同标签页），跨浏览器会话无任何「已推送」记录。
- 修复（按用户拍板的天粒度规则）：新增 `src/utils/overview-day-marker.js`——按用户维度在 localStorage 存「上次在线日」日键（`boh_overview_last_online_day:<userId>`，localStorage 不可用降级内存）。`useOverviewIsland.js` 的 `maybeShowOverviewIsland` 改为：游标=今天 → 直接不推（当天上线=当日及之前全部默认已浏览）；否则推送游标 = 上次在线日的次日零点（`getDayFrontierIso`），用该游标作为 `get_offline_overview` 的 p_anchor；检查成功即写游标=今天。无新一天内容 → total=0 不弹。首次启用无游标时以会话锚点所在日为「上次在线日」。离线天数文案改按日历日差计算。
- `/overview` 概览页（useOfflineOverview）保持秒级锚点不动——它是手动补看视图，天粒度只管自动推送。forceShowOverviewIsland 的 simulateDays 现在会同步回拨天粒度游标，可端到端复现真实自动检查。
- 验证：probe-overview-day-marker.mjs 18 项断言全过（日键/游标跨年跨月滚动/非法日键防御/26号→27号场景推演）；node --check 两文件语法通过。

## 论坛内容类型筛选器修复（新闻/活动接入后"筛选不工作"）
- 探针实测根锤（probe-forum-filter.mjs）：点「新闻」渲染 0 卡 + 无限滚动追拉 3 页全空、空状态文案误导「这里空空如也」；点「活动」偶发 2 卡。根因：内容类型筛选是纯前端过滤（prepareForumPosts），服务端每页 10 条混合内容按 created_at desc 排，官方卡 created_at 继承新闻/活动旧日期沉底 → 当页目标类型常为 0；hasMore 到头就停，用户视角=筛选器坏。
- 修复三层：①migration 2026090705 给 list_forum_posts 加 p_kind_filter（'post'=post+repost/news/activity），drop 旧 8 参签名防 PostgREST 双函数匹配歧义（PGRST202），grants+notify pgrst 齐全；②post-api.js getPosts 解析 pagination.kindFilter，rpcPayload 透传，legacy 降级（PGRST202→去掉新参重试+客户端按前缀正则补滤），两条 REST 直查路径（cursor 模式 + fallback）加 .in('post_kind', kindValues)，缓存 key 加 kindFilter；③ForumMain fetchForumData 传 kindFilter，客户端兜底过滤'post'分支纳入 repost，快照 key 纳入 contentType（防筛选态写快照污染无筛选会话），返回态保存/恢复 selectedContentType，空状态文案区分内容筛选。
- 回归：guest 路径 REST 直查 `post_kind=in.(news/activity/"post,repost")` 全生效，四态截图正确（新闻=官方新闻卡、活动=17 张纯活动卡、论坛=普通用户帖、全部=混合），0 pageerror；ESLint 0 error（24 警告全为遗留）。
- **待办：2026090705 migration 需用户应用到远程 Supabase**——登录用户走 RPC 路径，未应用时 PGRST202 降级为客户端过滤，稀疏页问题在登录态仍存在（guest 反而因 REST 直查先吃到修复）。
- 探针知识：官方卡渲染层剥【新闻】/【活动】前缀（displayTitle/displayBody），判定卡片类型不能查 textContent 前缀，看徽章/作者/截图。

## 官方卡封面裂图修复（部分图加载失败，新闻/活动都是）
- 用户报「部分图加载失败，活动也是」。DB 取证（probe-official-card-images.mjs）：news/activities 表老数据的 image 字段存的是 **Vite 资源引用** `@/assets/images/xxx.webp`（26 条，部分 .png 但目录里全是同名 webp）+ 4 条 `data:image/svg+xml` 灰占位 + 3 条 Cloudinary 完整 URL；镜像卡原样进 posts.cover_image_url → `<img src="@/assets/...">` 浏览器解析不了 → 裂图。只有 3 条新云图正常。
- **修复位置血泪教训：第一反应改 ForumMain.getPostImages 完全无效**——API 格式层 `normalizePostListRecord`（forum-format.js）会用 raw cover **预构造 `images` 数组**，getPostImages 开头 `if (images.length) return` 直接短路，页面层修复永远执行不到。调试绕了大弯（HMR/SW/缓存都排查了一遍，用 DBG console.log 才实锤"代码加载了但没执行到"）。正确做法：**在 format 层 resolveStoredCoverUrl（新增，走 asset-helper 的 getImageUrl，data:/http 直通、@/ 别名解析、png 同名 webp 兜底）**，normalizePostRecord + normalizePostListRecord 双入口接入；ForumMain cover 分支的 resolve 保留（hydrateOfficialPostKinds 事后补 cover 的路径仍需要，且 resolve 幂等）。
- 回归：活动态 15/15 bundled 全加载 0 破损；新闻态 10 bundled + 1 remote + 4 data-svg 占位全正常，requestfailed 0；ESLint 0 error。探针保留：probe-official-card-images.mjs（DB 字段取证）、probe-card-image-check.mjs（活动态）、probe-news-image-check.mjs（新闻态）。
- 调试方法论：活组件 Vue props（el.__vueParentComponent 链找 props.post）+ 管线直测（页面内 import 模块调函数）+ DBG 标记日志三者对照，能一步定位"代码没执行"vs"代码执行了但输入错"。

## 导航栏分享灵动岛（复制链接 + 站内转发）→ 最终形态：帖子内分享按钮触发
- **用户拍板最终交互（推翻导航栏按钮方案）**：分享入口在 **PostCard 操作栏的 share 按钮**（.share-btn-v2），点击 → 灵动岛弹出选择器（复制链接/站内转发）；导航栏不放按钮（.nav-share-btn 已删）。ForumMain.sharePost 重写：showIsland.custom(ShareIsland, { target: buildPostShareTarget(post), isLoggedIn, requireLogin, onCopied, onClose })；旧 shareMenuState 弹窗/copyPostLink/closeShareMenu 已删；onCopied 回调点亮卡片 is-copy-success 徽标（shareCopiedPostIds marker 机制保留）。
- useShareTarget 注册表退居基础设施（buildShareUrl 被 ShareIsland 用）；Newsroom/PostDetail 的 setShareTarget 注册已移除（无消费方的死代码）——未来新闻详情要加分享按钮时，setShareTarget + showIsland.custom(ShareIsland) 两行接入。注意 PostDetail 是 hideNavbar 页（无导航栏=无岛宿主），它的分享若要弹岛需先解决宿主存在性。
- 站内转发链路：未登录 → requireLogin 收岛+拉登录框；target.forward={postId} 直转，={sourceType,sourceId}（新闻官方卡）先走 `findQuoteRepostSourceId`（forum-interaction-api.js 新增，posts 按 source_type+source_id+approved 查卡 id）→ `createQuoteRepost` RPC。
- **🔴 Vue scoped 编译器大坑（本项目实测）**：`:global(#id[attr]) .child` 混合写法编译后**剥掉后代部分**，规则变成 `#id[attr]{...}` 直接命中容器本身——暗色规则全废+样式污染，且 CSS 里多行分组 `:global(...)` 一旦括号/逗号错位整个 style 子请求 500 挂页面（tailwindcss vite 插件 "Missing opening ("）。**唯一安全写法：整条选择器包进 :global** `:global(#unified-nav-container[data-theme="dark"] .child)`，且分组各行为独立 :global 规则、不要把逗号留进括号内。GlobalNavStatusCard 存量 13 处混合写法已一并修复（其暗色预览行样式首次真正生效）。
- 回归（probe-share-island.mjs，RPC stub）：注册目标→岛弹出带主题名 ✓；复制链接→剪贴板=深链 ✓；转发撰写→提交→已转发到论坛 ✓；无目标禁用 hint ✓；暗色 computed style 全对 ✓；0 pageerror。探针知识：pinia 登录注入必须等 auth init 异步流跑完（~2.8s），早了会被清掉。
- **🔴 岛关闭回归 bug（2026-09-08 用户实测发现）**：sharePost 的 onClose 只置空本地句柄、没调 `handle.close()` 清岛槽位 → ×/Esc/成功自动关闭全部失效，岛永远不消失。教训：**island handle 的 close() 是唯一能真正摘掉岛的入口**，onClose 回调里必须调它；且探针的 `waitFor(...).catch(() => {})` 静默吞掉了断言失败——关闭类行为必须用 PASS/FAIL 断言式探针（probe-share-island.mjs 已改）。已修复并四态断言全 PASS。

## 官方卡头像（2026-09-08）
- 「方块之家」官方卡（post_kind=news/activity 或 author_id 空+作者名匹配）头像：PostCard/PostDetailMain 加 `isOfficialCard` computed → `getImageUrl('favicon.webp')`（src/assets/images 下 1024px **透明底**像素苹果，Read 预览器把透明衬成黑色别被骗）+ 容器 `.is-official` 白底+内描边（feed.css / PostDetail style.scoped.css 的头像容器原本 background:#1d1d1f 深色首字母圆）。官方卡作者名点击进 profile 已禁用（无此账号）。普通帖头像不受影响。
