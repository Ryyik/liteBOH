# 2026-09-19

## 横屏帖子详情弹窗化 — 方案对齐（未实施，等推进指令）

结论（已与用户对齐）：
- 分流判据：`(orientation: landscape) and (min-width:1024px) and (min-height:600px)`（项目既有横屏断点，JS 侧 matchMedia 单源化）。命中 → 弹窗；未命中（竖屏/窄窗）保持整页路由现状。桌面端也走弹窗。
- 后退键：pushState 同步 URL（打开弹窗时 hash 变为详情 URL，后退=关弹窗；刷新落整页兜底，深链语义保留）。
- 形态：居中玻璃大卡（960–1080px），PostDetailMain 双栏（主帖+评论）完整复用。

架构要点：
- 复用 PostDetailMain.vue（不复制逻辑）：加 `postIdOverride`/`modalMode` props；modalMode 下隐藏页头返回条、goBack→emit close、分享 URL 自构造 `/#/forum/post/:id`、goToProfile/goToQuotedPost 先关弹窗再跳、forum-return-state 旁路。
- 新增：`utils/forum-viewport.js`（判据单源）、`composables/usePostDetailModal.js`（模块级状态）、`views/PostDetail/PostDetailModal.vue`（Teleport→body + Esc/遮罩 + body 滚动锁 + pushState）、统一入口 `openForumPost()`。
- 三入口改调 openForumPost：ForumMain.vue:3756 / UserSpaceMain.vue:816 / ProfileMain.vue:1579。
- 弹窗 z-index = 2147482400：> bottom-nav 2147482200 / BohaiSidebar 底层，< ImageViewer 2147483000 / CommonAlertModal 2147483647。内层 fixed 子层（编辑器 220300）在弹窗 stacking context 内自然置顶，无需改。
- 列表同步：ForumMain 补听 `boh:forum-post-updated`（deleted 已有监听）patch 列表项。
- 兜底：判据失配（旋转退出横屏）自动关弹窗。

待办：实施时需探针（modalMode 无 router 调用、编辑面板可视、后退关弹窗后 navbar 高亮正确）+ 反证。

## 横屏帖子详情弹窗化 — 已实施（2026-09-19 上午）

落地与方案一致，新增 3 文件 + 改 5 文件 + 1 探针：
- 新增 `src/utils/forum-viewport.js`（FORUM_LANDSCAPE_QUERY 单源 + isForumLandscape + onForumLandscapeChange）
- 新增 `src/composables/usePostDetailModal.js`（模块级单例 isOpen/postId + open/close + openForumPost 分流；pushState 键 `bohPdModal`，保留 router 自身 history.state 字段；close restoreHistory=false 用于跳转主页场景留条目给后退兜底）
- 新增 `src/views/PostDetail/PostDetailModal.vue`（App.vue 常驻轻宿主；PostDetailMain 走 defineAsyncComponent 共享整页 chunk；overlay z-index 2147482400）
- PostDetailMain：props postIdOverride/modalMode + emits close/open-post；activePostIdSource computed 统一 watch；goBack/goToProfile/goToQuotedPost/深链/分享/举报 limited/空态按钮全部 modalMode 分支；`boh:forum-post-updated` detail 扩展携带 post 对象（refreshPostStats 与编辑保存都派发）
- 三入口（ForumMain:openPostDetail / UserSpaceMain:openProfilePost / ProfileMain:navigateToPost）isForumLandscape() 命中即 openForumPost；ForumMain 新增 handleForumPostUpdated patch forumData（shallowRef 不可变更新）
- 探针 `scripts/probes/probe-pd-modal.mjs` 16/16 PASS；反证（分流条件强制 false）A1-A5/B1 转红 → 还原全绿
- 坑：竖屏 .post-card-v2 中心是 image-post-thumb（点图不触发 open）→ 探针点击目标用 `.post-title-v2`
- 门禁：vite build dist-check PASS、build:ci 全链 PASS（bundle-check PASS）、改动文件 eslint 0 errors
- 未提交未推送（等用户指令）

## 论坛横竖屏单源判据加固 + 993/992 裂缝带修复（已实施 2026-09-19 下午）

安全修复 4 组（全部等价替换或明确消除错位，无行为回退）：
- **993/992 → 1024/1023**（成对改，缺一会有 993-1023 裸两栏空洞）：base.css:158 两栏档 + base.css:192 单列档 + ForumToolbar.vue:644（scoped 必须同改）+ 3 处注释。993-1023 区间从"两栏"变"单栏"，与 JS 竖屏编辑器判据对齐。
- **forum-viewport.js 双判据扩展**：新增 `FORUM_PORTRAIT_COMPOSER_QUERY = '(max-width: 1024px) and (orientation: portrait)'` + `isForumPortraitComposer()` + `onForumPortraitComposerChange()`；内部 mql 注册表泛化（懒建 + 共享监听），原横屏 API 不变。语义严格等价旧手写 `width<=1024 && height>=width`。
- **ForumMain**：isMobileComposerMode 初始化/更新改由单源订阅驱动（弃手算 + resize rAF）；旋转关闭不再裸置 `isMobileComposerOpen=false`，新增 `leaveMobileComposerForViewportSwitch()` 静默落草稿（persistPostDraft + clearPostDraftSaveTimer + savePostDraftToDatabase）再关，不弹确认框。
- **PostDetailMain**：isEditPortrait 改调单源 + onForumPortraitComposerChange 订阅（弃裸 resize）；:1514 遗留的 updateEditPortrait() 调用已修（eslint 抓到）。
- **PostComposer 899 → 900**（JS isNarrowScreen + 2 处 CSS），与 UserSpace 900 档收敛，1px 影响。

新探针 `scripts/probes/probe-forum-viewport.mjs`：**21/21 PASS**。VP1-9 真值表（9 档三方一致：JS landscape / JS portraitComposer / CSS 两栏 grid-area + FAB 投影）；B1-3 分流边界（1000×800 整页 vs 1024×600 弹窗）；C1-6 旋转保护（overlay 关 + 内容转内嵌 + localStorage 落草稿 + overflow 解锁）；D1/D2 镜像对（横屏 chip 可见/大卡片隐藏，竖屏反向）。
反证：base.css 改回 993/992 → VP5/VP6 必红；去掉落草稿 → C5 必红；还原后 21/21 复绿。

既有探针回归：toolbar-hero 22/22 ✓；pd-modal 29/30（J4 属未提交的 PostDetail「竖屏连续内容」改造未对齐项，该规则在 style.scoped.css:1341 新增 hunk 里，与本次无关）；mobile-composer 已过时（goto `#/forum`→tab=posts，FAB 按设计仅 community tab 显示）。

坑（新探针复用）：`/forum` 重定向到 `/user-space?tab=posts`（我的），论坛社区段入口 = `/#/user-space?tab=community`；1280×800 分栏档下 community tab 内嵌编辑器整体 display:none（landscape-rail.css 设计，chip 行 `v-if="isMobileComposer"` 只存在于 overlay，需实例强制打开才能测 chip 档）；dev HMR 抖动会重挂 #app（injectAuth 须带重试 + 每档 goto 重载而非切档断言）。

## 弹窗布局二次改造 — 小红书式（2026-09-19 中午，已实施）

参考小红书桌面端详情弹窗重构 modalMode 布局（整页/竖屏零改动）：
- 左媒体区（hasMedia = detailImages 非空才渲染；16px 外边距 + 圆角 12px；箭头/dots 悬浮图上；点击放大走 ImageViewer）；无图帖单栏信息区（用户明确不要渐变占位）
- 右信息栏内部滚动：作者行（头像+帧+昵称+时间/位置+**关注钮**+菜单）→ 标题 → 正文/官方富文本 → 转发引用框 → 「共 N 条评论」→ CommentThread
- 底部固定操作栏：输入框（说点什么.../回复 @xxx，Enter 提交带 isComposing 保护）+ 点赞/评论/分享（数字在图标下）；**底栏是弹窗内唯一输入框** → CommentThread 新增 hideComposer prop 抑制内嵌框
- 关注：profile-api 的 isFollowing/followUser/unfollowUser 直接接；canFollowAuthor（modalMode+登录+非官方卡+非自己）；canFollowAuthor watch 驱动 refreshFollowState
- 样式单源 `src/views/PostDetail/modal-layout.css`（全挂 .post-detail-page--modal 前缀；:deep() 隐藏 CommentThread section-header/空态卡壳）
- **坑① @import 必须在 style 块所有规则之前**（放块尾整个文件被忽略，量出来的边距还是旧 clamp 值）——已移到首个 style 块紧跟 style.scoped.css
- **坑② 探针伪造登录 id 撞真库用户**（00ac36b4 是真用户且是首帖作者 → canFollowAuthor 拦掉关注钮）→ 探针换随机合法 UUID
- **坑③ fade-in-up 入场动画未结束采样几何**（0.1s delay + 0.5s，带 translateY 尾巴）→ 布局断言前等 900ms；A8 断言改用「panel↔actionbar 底边距恒定」而非绝对 bottom
- **坑④ 竖屏卡片中心是 image-post-thumb** 点图不触发 open → 探针点击目标用 .post-title-v2（上轮已修，沿用）
- 探针 21/21 连续两次全绿；反证（禁用 modalMode 分支）A5/A6/A7 转红 → 还原全绿；build:ci 全链 PASS；lint 0 errors
- 未提交未推送

## 竖屏详情 A+B 改造 + 横屏重叠修复（2026-09-19 下午，已实施）

参考 Threads 竖屏详情页，范围 = 底栏接管 + 纯 CSS 快赢（连续时间线暂缓）：

**横屏重叠修复**（用户截图反馈）：弹窗右上关闭钮与右栏作者行按钮重叠 → `.pd-info-scroll` padding-top 52px（14+34+4）给关闭钮让位。

**A 竖屏底部固定操作栏**：
- 整页模式 fixed 底栏 `.pd-actionbar--page`（输入框+发送+心/评论/分享），卡内 `.post-footer` 动作栏**移除**（底栏唯一入口，用户拍板）
- 底栏类去 `.post-detail-page--modal` 前缀 → 弹窗内嵌/整页 fixed 两用（`modal-layout.css`）
- `CommentThread.hideComposer` 全局启用（弹窗+整页都传 true），内嵌输入框全面下线
- 键盘适配：visualViewport resize/scroll → 根元素 `--pd-dock-inset`（window.innerHeight - vv.height - vv.offsetTop），`.pd-actionbar--page { bottom: var(--pd-dock-inset) }`；viewport-fit=cover 已有 → safe-area 生效
- 整页 `.post-detail-page` padding-bottom 80px → calc(132px + safe-area) 防底栏遮尾
- focusModalReply 泛化（去 modalMode 门槛，modalReplyField ref 存在即聚焦）
- 注意：整页底栏 z-index 2147481200（低于弹窗 2147482400）

**B 纯 CSS 三连**：
- 楼中楼竖向引导线：CommentThread `.comment-item-x:not(:last-child)::before`（left 16px 头像中线，头像 z-index:1 盖线头）
- 整页 dots 贴图缘：`.post-detail-image-carousel` relative + `.post-detail-image-dots` absolute 玻璃胶囊
- 动作行紧凑化由底栏接管自然达成（图标+计数纵排）

**探针升级到 25 项**（probe-pd-modal.mjs）：新增 A6 边距几何 / A7 关注钮 mock 翻转 / A8 底栏固定（panel↔actionbar 底边距恒定，非绝对 bottom）/ A9 无图单栏 / H1-H4 竖屏底栏。新坑四连：
- **dev 冷转换 2400 行 PostDetailMain 首次 >30s** → 探针 A5 假红（chunk 未挂载 overlay 空壳），预热后消失；生产构建无此问题
- **injectAuth 后 auth store 异步 getSession(null) 不定时重置 isLoggedIn** → 关注钮间歇消失 → 断言前幂等补打 injectAuth
- **feed hottest 排序不稳定** → 首卡可能是官方镜像卡（无关注钮=正确行为）→ 探针选非官方卡（排除 .post-author-avatar.is-official / .post-kind-badge）
- **`( & )` 分离启动 dev server 会被 Bash 会话回收** → 必须 run_in_background 常驻
- 反证：底栏 v-if 置 false → H1 红 → 还原全绿；25/25 连续两次 PASS；build:ci 全链 PASS；lint 0 errors
- 未提交未推送

## 弹窗细节四连改（2026-09-19 下午二，已实施）

用户反馈三项 + 竖屏连续内容：
1. **弹窗骨架撑满**：`.post-detail-page--modal .post-detail-skeleton` flex:1 + 列比例对齐正文（1.12fr/1fr）。探针 J1 用 mock 详情请求延迟（route 规则 `GET id=eq.` delay 1500ms）让骨架停留可测几何。
2. **弹窗去滚动条**：`--modal` 下 `.pd-info-scroll`/`.comments-list`（:deep）+ 宿主 `.pd-modal-scroll` 统一 `scrollbar-width:none` + `::-webkit-scrollbar display:none`，滚动能力保留。
3. **作者行固定条**：`pd-author-row` 移出 `.pd-info-scroll`，成为 `.pd-modal-info` 三段结构（固定作者条 48px 让位关闭钮 + hairline 分隔 → 滚动内容 → 底栏）。探针 J3 断言直接子级 + 滚动后 top 不变。
4. **竖屏连续内容**：1024 断点下 `.x-main-column`/`.x-side-content` 玻璃壳透明化 + `post-x-layout` gap 0 + padding 统一 20px；tier 淡底规则限定 `@media (min-width:1025px)`（竖屏连续流不受 tier 底干扰）。

**important-budget 棘轮事件与特异性重构**（重要经验）：
- 新增 5 个 !important 被棘轮拦（style.scoped.css 24→29）。正确解法不是上调基线，而是**削存量 + 特异性重构**：
  - `.x-post-card` 去壳规则 → `.post-detail-page .x-post-card`（特异性压 .glass-panel，顺序无关），删 5 个 !important
  - CommentThread `.x-side-content` → `.x-side-content.x-side-content`（两处：基础规则 + 1024 断点内的 background 0.66），删 5 个 !important
  - 我的竖屏/弹窗覆盖全改无 !important（(0,3,0)/(0,4,0) 压子组件 (0,2,0)/(0,3,0)）
  - tier 淡底 4 个 !important 保留但限定横屏
  - 结果：total 1327 → 1317（净 -10），零新增
- **教训**：CSS 覆盖子组件/全局类时优先「重复类名提特异性」，!important 只在对方也 important 时才对拼；grep 颜色值定位第三处来源（CommentThread 断点内还有一份 background 0.66）。
- 探针 30 项全 PASS（新增 J1-J4）；J1 反证（规则选择器改名）转红 → 还原全绿；build:ci 全链 PASS；lint 0 errors
- 未提交未推送

## 评论区小红书式改造 + 评论点赞（2026-09-19 下午三）

**弹层被详情弹窗压住的修复**（用户截图反馈）：Teleport-to-body 的层 z-index 低于弹窗 2147482400：
- 删除确认 `.detail-confirm-overlay` 210100 → 2147483600
- 登录弹窗 `.boh-login-modal-overlay`（login-modal.css）10000 → 2147483500
- 都取「> 2147482400 且 < CommonAlertModal 2147483647」带。编辑器/举报浮层非 Teleport，在 panel stacking context 内自然置顶 ✓

**评论区两栏改造**（CommentThread.vue）：评论条 grid 48px 头像列 + 内容列（昵称行右上「···」收编删除=本人/admin 才渲染；正文/时间/动作行进内容列）；楼中楼 32px 头像 + 同构缩进（child-replies-wrap 旧 border-left/缩进覆盖清零，缩进由两栏自然形成 ≈60px）；动作行 = 赞（is-liked 红色 + 计数）+ 回复；单条子回复且全载时不显示展开链（产品逻辑：预览直接可见）；竖屏 40px 头像。

**评论点赞全链**：
- 迁移 `supabase/migrations/2026091905_comment_likes.sql`：comment_likes 表（PK(comment_id,user_id)+RLS 本人写/登录读+anon/authenticated 分别 grant）+ comments.like_count 触发器（security definer）+ RPC toggle_forum_comment_like（未登录 raise NOT_AUTHENTICATED）。**未 push：CLI 直连 db:5432 被 fake-IP 吞（dial 198.18.0.22 timeout，pooler 同样 fake-IP）→ 需用户网络放行 + 「确认」**（dry-run 也因此没跑成）
- API：forum-interaction-api 加 toggleCommentLike（RPC 主通道 + comment_likes 降级，同 toggleLike 模式）+ getLikedCommentIds 批量已赞
- 接线：PostDetailMain handleToggleCommentLike（乐观更新+回滚，顶层/子回复双 patch）+ loadTopComments/loadChildReplies 后 hydrateCommentLikeState；forum-api.js re-export
- 探针 K0-K5（mock comments/previews RPC/点赞 RPC/comment_likes）：36/36 PASS
- 探针规则顺序坑：`post_id=eq.` 含子串 `id=eq.` → comments mock 规则必须在详情 delay 规则之前
- **用户移除评论竖线**：`.comment-item-x::before` 引导线删掉（对齐图1 小红书无竖线版）；36/36 PASS
- **无图帖弹窗不全宽根因**：`.detail-container` 历史样式 `margin: 0 auto` 在 flex column 容器（`.post-detail-page--modal`）中吸收剩余空间 → 容器收缩成内容宽并居中（flex 子项 auto margin 特性）。修复：modal 覆盖 `margin: 0`。另：hasMedia 语义改为只认正文图（`!image.isCoverFallback`，buildCoverFallbackImage 打标），cover fallback 不再撑起媒体列（否则「无正文图但有封面字段」的帖出现空白媒体列）。A9 增强：单栏 info 全宽断言（panel-32），实测 infoW 1086 全宽 ✓；36/36 PASS；build:ci PASS
- 未提交未推送；DB push 等用户确认

## UserSpace 横屏分栏 — 全屏布局残留盘点（2026-09-19 晚，仅检查未改动）

分栏机制：landscape-rail.css + side-rail.css，判据 `landscape+≥1024+≥600`，作用于 `.user-space-page .tab-page`（left: rail-w）。

已正确分栏 ✅：5 tab 内容区、mobile-composer-overlay（body 级收到右区）、bottom-nav-glass（隐藏）、发布悬浮球、Messages 详情（≥1024 inline split）、BohaiSettingsPanel（embedded 不 teleport）、CloudPlus/CommunityTab（无 fixed）。

残留全屏 ❌（三层）：
1. **AI tab BohaiSidebar 最严重**：≥1024 `.sidebar{position:fixed;left:0}` 宽 248–312 z-2147483450，embedded 只适配 top/bottom（--userspace-top-offset/--userspace-bottom-nav-offset）**left 未消费 --userspace-rail-w** → 打开会话历史即叠在 UserSpace 左栏上（BohaiSidebar.vue:534/645、shell-header.css:101）。
2. **Forum 5 个 fixed 弹层未收右区**（对照：composer-overlay 已收）：mobile-draft-panel-overlay（rr.css:691 z-200100，发布内外两处）、weekly-report-overlay（weekly-report.css:20 z-12000）、forum-confirm-overlay（drawers-skeletons.css:489 z-210100，确认+引用转发）、checkin-overlay（composer.css:1343 z-200050）、composer-image-preview-overlay（composer.css:862 z-220200）。
3. **模态类（遮罩全屏+居中卡，属模态语义可另议）**：.modal-overlay（profile-panels.css:711，ThemeModal/SubscriptionPlans 用）、CommonAlertModal、AvatarFramePickerModal、.glass-modal-overlay（glass-ui.css:77，FollowList/AvatarCrop 用）。
另：notification-panel-portal 是 dead CSS（模板无引用）。

**用户纠正后的澄清（同晚）**：
- AI 岛（UnifiedNavbar 内岛体 + useGlobalAiOverlay）与消息 tab 内嵌 BOH AI 分区**共存**，非替代：MESSAGE_SECTION_ITEMS 仍有 {id:'ai'}（UserSpaceMain.vue:475），切到分区才挂 AsyncBOHAI embedded（bohaiActivatedOnVisit 性能优化 P0-1）。BohaiSidebar 盖左栏仅在内嵌分区里点 BohaiSidebar.vue:99 的 sidebar-open-btn 时发生。AI 岛本体是全屏 overlay，无此问题。
- **他人空间（/profile/:username，ProfileMain.vue）确认无左栏**：meta.hideNavbar=true（community.ts:89）→ 无顶部导航、无 AI 岛（canOpen=false 回退 /ai-chat）、无分栏，横屏 = 全屏居中布局。上轮盘点范围只覆盖 /user-space，未含他人空间——是否纳入分栏体系待用户决策。

## 他人空间横屏分栏 — 已实施（2026-09-19 深夜，14/14 探针 + 反证闭环）

用户指令"先把他人空间做了"。落地（4 文件改动 + 1 探针新增）：
- **navItems 单源化**：UserSpaceMain 本地数组 → `useUserSpaceTabs.js` 导出 `userSpaceNavItems`（UserSpaceMain/ProfileMain/UserSpaceSideRail 三处消费；lucide import 从 UserSpaceMain 移除）
- **ProfileMain.vue**：根内挂 `UserSpaceSideRail`（current-tab="others" → 指示器自动隐藏）；nav-click → `/user-space?tab=xx`；action 分发 compose/search→社区 tab、theme→themeManager.toggle()（applyTheme 派发 theme-changed，既有 onThemeChanged 自动同步）、home/logout→push('/');未读数走 getNotificationStoreSync + 登录兜底 loadNotificationStore
- **landscape-rail.css** 追加他人空间段落：`body.page-userprofile`（App.vue 按 route.name 自动挂，勿手挂）→ --userspace-rail-w 88/240 两档；`.profile-page` position:relative + **padding-left:var(--userspace-rail-w)**（一条 padding 让 sticky 页头 + margin:auto 限宽容器全部改为相对右区居中）；--rail-pill-bg/--rail-item-bg-* 亮暗补挂（side-rail.css 只挂 .user-space-page）
- **ProfileMain 引入方式**：非 scoped `<style src=".../landscape-rail.css">`——scoped 的 @import 会把 body.page-userprofile 附加 data-v 属性作废；rail 顶隙沿用 fallback 78px（跨页品牌位不跳变）
- **探针 probe-profile-rail.mjs 14/14**：A rail 渲染/宽 240、B 内容右移/无重叠/右区居中（shell.center=840=右区中心）、C 无高亮/淡染、D 点击跳转、E 暗色白染、F 主题切换、G 竖屏零副作用。mock：一条 route 规则表（profiles username=eq → 单对象 + Content-Range 0-0/1，其余 rest/v1 → */0）
- **坑**：getComputedStyle 序列化 0.075→`.075`、rgba→hsla，断言须正则抓数值非字面匹配；BSD grep 不支持 BRE `\|`（用 -E）
- **反证**：stash 4 文件 → 探针 D1 前超时崩（rail 不存在=红）→ pop → 复跑 14/14 绿
- 门禁：vite build dist-check PASS（precache 117）、改动文件 eslint 0 errors；未提交未推送

## 他人空间 rail 滚动跑偏修复（2026-09-19 深夜二轮，20/20 + 反证）

用户截图反馈：他人空间滚动后左栏只剩「首页」贴在最底下，品牌位/导航组全部滚走。
- **根因**：UserSpace 页面容器高度=100dvh（滚动在 .tab-page 内部）→ side-rail.css 的 `position:absolute; inset:0 auto 0 0` 等效锁视口；他人空间 .profile-page 是普通文档流长页（高度=整页内容），absolute 把 rail 拉满文档 → 顶部组随滚动上移出视口、footer 组（margin-top:auto）贴文档底部。
- **修法**：landscape-rail.css 他人空间段落补 `.profile-page .userspace-rail.liquid-glass { position: fixed }`（特异性 0,3,0 压 side-rail.css 0,2,0；inset/width 原样继承，fixed 下相对视口）。
- **探针升级 20 项**：C3 rail position=fixed、C4 top0/bottom900、H1-H4 滚动 800px 后 rail 仍贴视口（品牌位/主导航/首页按钮全在视口内）。
- **探针坑（竞态）**：手动 setAttribute('data-theme','dark') 会被 `:data-theme="currentTheme"` Vue 绑定在数据加载重渲染时改回 light（冷启动慢时碰巧 PASS，热了就 FAIL）→ 必须派发真实 `theme-changed` 事件走 onThemeChanged 同步 ref。
- **反证**：stash landscape-rail.css → C3=absolute、H1 top=-732、H2/H3 滚出视口（红）→ pop → 20/20 绿 ×2 遍。
- 门禁：vite build dist-check PASS；未提交未推送。

## 【补记】横屏弹窗 + 评论区 Threads 式改造 + 评论点赞（2026-09-19 全天，probe-pd-modal.mjs 43 项）

（前段日志被并行会话覆写，此处按时间线补记要点；细节见探针 scripts/probes/probe-pd-modal.mjs 与 git diff）

1. **横屏弹窗化**：openForumPost 单源分流（forum-viewport.js 判据 + usePostDetailModal.js pushState 键 bohPdModal）+ PostDetailModal.vue 宿主（z 2147482400）+ PostDetailMain modalMode 分支；三入口收敛。
2. **弹窗小红书式布局**：左媒体（16px 边距）+ 右信息（作者行/关注/标题/评论）+ 底部固定操作栏；样式单源 modal-layout.css。
3. **弹层压叠修复**：删除确认 2147483600、登录弹窗 2147483500（>弹窗 2147482400，<CommonAlertModal 3647）。
4. **竖屏 Threads 式（只竖屏）**：身份条 fixed 顶（头像 44px 完整显示框、关注胶囊、无分享——分享只留底栏）；detail-container padding-top 80px 补偿；媒体前置（carousel 提为 x-post-card 直接子级 + x-post-card-media，竖屏 order:-1 全出血贴屏、横屏 min-1025 order:3 恢复原序）；正文 17px/1.75；评论段头「共 N 条评论」+「- 到底啦 -」标记；底栏横排；无图帖单栏全宽。
5. **评论点赞全链**：迁移 2026091905_comment_likes.sql（comment_likes + like_count 触发器 + toggle_forum_comment_like RPC）**未 push**（fake-IP 吞 5432，等用户网络放行+确认）；API toggleCommentLike/getLikedCommentIds；CommentThread 两栏 + ··· 菜单 + 动作行含赞（hideComposer 全局）。
6. **无图帖全宽根因**：`.detail-container` 的 `margin:0 auto` 在 flex 容器中收缩成内容宽 → modal 覆盖 `margin:0`；hasMedia 只认正文图（!isCoverFallback）。
7. **important-budget 棘轮**：特异性重构削 10 个存量 !important（1327→1317），零新增。
8. **弹窗去滚动条 + 骨架撑满 + 作者行固定条**（J 组）。
9. **探针坑合集**：@import 须在 style 块首；auth store 异步重置伪造登录（幂等补打）；feed 排序不稳首卡可能是官方卡；dev 冷转换 2400 行组件 >30s；route 规则顺序（post_id=eq. 含 id=eq. 子串）；`( & )` 起 dev server 会被回收；固定弹层断言用相对距离非绝对坐标；布局断言前等 fade-in-up 动画结束。
10. 最终：**探针 43/43 PASS**、build:ci 全链 PASS、lint 0 errors、important-budget 1317/1327。未提交未推送；DB push 等用户网络放行 +「确认」。
