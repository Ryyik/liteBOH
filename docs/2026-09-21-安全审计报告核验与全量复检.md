# BOH 安全审计报告核验 + 全量复检

**核验日期**：2026-09-21
**核验对象**：`docs/boh-full-health-check-2026-06-26.md` 第一章「安全审查报告 (72/100)」+ 第八章修复路线图安全项
**生产环境**：`www.blockofhome.cn`（GitHub Pages + Cloudflare）/ Supabase `nplnlefdwfgtyimfkyih`
**核验手段**：远端库 `pg_policy` / `pg_proc` / `acl` 现时点快照 + 生产环境只读 HTTP 探针 + 全仓源码 grep + 构建产物 JWT 解码 + 依赖审计
**统计口径**：`pg_stat_*` 为建库起累计值；策略与函数快照取 2026-09-21 当日

---

## 一、结论速览

**问题 1：网站存在安全审计报告吗？**

存在，但**没有独立的安全审计报告**。安全内容散落在三处：

| 出处 | 性质 | 安全内容 |
|---|---|---|
| `docs/boh-full-health-check-2026-06-26.md` 第一章 | **主报告**，13 项检查 + S-1~S-6 风险项 + P0~P3 路线图 | 安全审查 72/100 |
| 同文件 第七章 | 数据库审计 76/100 | RLS 策略缺陷清单 |
| `supabase/migrations/2026*_security_hardening.sql` 等 5 个迁移 | 修复记录（非报告） | 2026062602 / 070801 / 070802 / 081501 / 090803 |
| `论坛模块审计报告.md` | 无安全章节 | — |

**问题 2：报告内容与现状是否相符？**

**主体已失效（且是利好方向）**：报告 6-26 发布后，仓库在 7 月、8 月、9 月又落了 5 个安全加固迁移。报告里标「⏳ 迁移待执行 / 待修复」的项，**现状绝大多数已修好**。所以如果现在还拿那份报告当风险清单，会**高估风险、追一堆已经不存在的问题**。

真正需要关注的是**报告没覆盖的面**：本轮在生产环境实测出 **2 个高危 + 3 个中危**新问题，全部集中在「Supabase 函数级授权（EXECUTE）」与「安全响应头」两块，报告 13 项检查里**一项都没查**。

| 维度 | 报告结论 | 现状 |
|---|---|---|
| XSS / DOMPurify | ⚠️ 2 处未消毒 | ✅ **已修**（覆盖率 100% 属实） |
| RLS 策略缺陷 | ❌ 9 张表有问题 | ✅ **9 项中 8 项已修**，1 项作废（表已不存在） |
| 硬编码密钥（仓库） | ✅ 通过 | ✅ **属实**（含 Git 全历史核验） |
| 客户端权限验证 | ⚠️ 关注 | ❌ **仍不足**：75/88 表仍给 anon 表级写权限 |
| 文件上传安全 | ✅ 通过 | ❌ **该判定偏乐观**：preset 无签名、可绕过 |
| Session 存 localStorage | ⚠️ 长期项 | ✅ **仍属实**（未变） |
| 函数级授权 / 匿名 RPC 暴露面 | — | ❌ **报告未覆盖，本轮实测为最严重问题** |
| HTTP 安全响应头 | — | ❌ **报告未覆盖，线上实测零响应头** |

---

## 二、报告安全项逐条核验

### 2.1 13 项检查表

| # | 报告结论 | 现状 | 证据 |
|---|---|---|---|
| 1 | API Key 硬编码 ✅ | ✅ 属实 | 全仓 grep `sk-` / `AIza` / `ghp_` / 私钥块 → 无真实密钥；`.trae/skills` 命中的是「模式说明文档」 |
| 2 | anon key 公开属设计 ✅ | ✅ 属实 | `.env` 已被 `.gitignore:42` 忽略；`dist` 内 JWT 解码**只有 `role=anon`**，无 service_role |
| 3 | 无 VITE_ 前缀 secret ✅ | ✅ 属实 | `.env` 仅含 URL / anon key / Cloudinary 公开名 / 功能开关 |
| 4 | DOMPurify 10/12，Login/Join 未消毒 ⚠️ | ✅ **已修，结论失效** | `Login/index.vue:8` + `:76-79`、`Join/index.vue:188` + `:218-221` 均 `DOMPurify.sanitize()`，import 自 `@/utils/dompurify.js` |
| 5 | RLS 策略缺陷 ❌ | ⚠️ **主体已修**（见 2.2） | 全 88 张 public 表 RLS 全部开启 |
| 6 | 客户端权限验证 ⚠️ | ⚠️ **部分未修** | 管理端 RPC 均有 DB 侧二次校验（实测 `admin_supabase_project_status` → `{"ok":false,"error":"需要管理员权限"}`）；但 anon 表级写权限未回收 |
| 7 | SQL 注入 ✅ | ✅ 基本属实（1 处例外） | `profile-api.js:384` 有 `replace(/[,.\()']/g,'')` 转义；但 `ProfileMain.vue:1367` 同一语义未转义 |
| 8 | Session 存 localStorage ⚠️ | ✅ **仍属实** | `src/utils/supabase-client.js:18` + `:112` |
| 9 | CSRF ✅ | ✅ 属实 | Bearer Token 模式 |
| 10 | 认证流程 ✅ | ✅ 属实（并有增强） | `auth-login` EF 服务端封禁复核（`index.ts:140` 命中封禁即撤销 session 返回 403） |
| 11 | 文件上传安全 ✅ | ❌ **判定偏乐观** | `cloudinary-client.js:384` 用**无签名** `upload_preset`，preset 名内联进 `dist/static/js/ui-components-*.js` |
| 12 | 密码处理 ✅ | ✅ 属实 | — |
| 13 | 敏感数据泄露 ⚠️ | ⚠️ **仍属实**（见 P1-1） | 本轮新增：匿名可读任意用户资产信息 |

### 2.2 S-1 RLS 明细逐条核验（远端库 `pg_policy` 现时点）

| 报告问题 | 现状 | 实证策略名 |
|---|---|---|
| posts 缺管理员 DELETE | ✅ **已修** | `posts_admin_delete`（`current_user_is_admin()`）、`posts_admin_manage`(ALL) |
| comments 缺管理员 DELETE/UPDATE | ✅ **已修** | `comments_admin_delete`、`comments_admin_manage`(ALL) |
| profiles 阻止管理员改积分 | ✅ **已修** | `profiles_admin_update`(UPDATE, `current_user_is_admin()`) |
| activities 任何登录用户可写 | ✅ **已修** | `activities_admin_insert/update/delete` + `允许所有人读取活动` |
| news 同 activities | ✅ **已修** | `news_admin_insert/update/delete` |
| notifications 缺 INSERT 策略 | ⚠️ **仍无，但非缺陷** | 通知由 `create_*_notification()` 等 SECURITY DEFINER 函数写入，属设计使然；`boh_count_unread_notifications` 已单源化 |
| user_impressions 缺 UPDATE 策略 | ✅ **已修** | `user_impressions_update_own` |
| addresses 完全缺策略 | ✅ **已修**（表已改名） | 现名 `user_addresses`，5 条策略 |
| moderation_logs 完全缺策略 | ✅ **已修** | `moderation_logs_admin_only`(ALL, `current_user_is_admin()`) |

### 2.3 S-2 ~ S-6

| 编号 | 报告结论 | 现状 | 证据 |
|---|---|---|---|
| S-2 | Login/Join 协议弹窗 v-html 未消毒（高危） | ✅ **已修** | 两处 computed 均包 `DOMPurify.sanitize(rawContent)` |
| S-3 | comments SELECT 公开 pending/rejected（中危） | ✅ **已修** | `comments_select_visible` 已带 `COALESCE(comments.status,'approved')='approved'` **且**校验父帖状态 |
| S-4 | posts SELECT 公开所有状态（中危） | ✅ **已修** | `posts_select_visible` = `approved OR 作者 OR admin` |
| S-5 | messages 表策略重复/冲突（中危） | ⚠️ **条目作废** | `public` schema 下**已无 messages 表**（私有消息模块已下线） |
| S-6 | Session 存 localStorage（中危） | ✅ **仍属实** | `supabase-client.js:18/112`；与「线上无 CSP」叠加后风险放大（见 P2-2） |

### 2.4 路线图安全项状态订正

| 报告标注 | 实际 |
|---|---|
| P0-3「posts/comments SELECT 公开 pending/rejected」⏳ 迁移待执行 | ✅ **实际已执行** |
| P0-4「activities/news 写策略过宽」⏳ 迁移待执行 | ✅ **实际已执行** |
| P0-1「addresses/moderation_logs 无 RLS」✅ 已修复 | ✅ 属实 |
| P0-7「Login/Join v-html 未消毒」✅ 已修复 | ✅ 属实 |
| P3-31「Session 迁移 httpOnly cookie」长期 | ⏳ 未动 |

> **口径更正说明**：`docs/2026-09-20-数据库与加载性能审查报告.md` §1.5 已独立记到「4 张表开启 RLS 但零策略」，本轮复核一致：`_rate_limits`、`forum_post_submissions`、`lottery_pity_backfill_runs`、`subscription_plan_prices`。这 4 张**只有 service_role 能访问**，是设计而非缺陷。

---

## 三、本轮新发现（报告未覆盖）

### P0-1 🔴 匿名可枚举「用户名 → 邮箱」

`public.resolve_email_for_login(p_username) RETURNS text`，SECURITY DEFINER，**anon 有 EXECUTE**。

**生产实证**：
```
POST /rest/v1/rpc/resolve_email_for_login   (仅 anon key，无任何登录态)
{"p_username":"<任意用户名>"}   → HTTP 200，返回该账号邮箱明文
```

**限流实测**（做了正反两组实验，结论与初始假设相反，按实测记录）：

| 实验 | 设计 | 结果 | 结论 |
|---|---|---|---|
| A | 固定伪造 `X-Forwarded-For` 连打 11 次 | 第 11 次 `400 rate_limit_exceeded` | 限流**生效**，键 = `resolve_email:<ip>` |
| B | 每次换一个伪造 `X-Forwarded-For` 再打 3 次 | **3 次全部 400 被拦** | 该请求头**不可伪造**，网关覆写为真实 IP |

→ 更正：**不存在「换头绕限流」**。真实可利用面是 **10 次/分/IP = 约 14,400 次/天/IP**：已知用户名时一次请求即可拿到邮箱；批量爬取会被限流挡住，但换 IP 仍可推进。

**根因**：函数体 `select au.email from auth.users au join profiles pr ...` 直接把邮箱当返回值交付给调用方。`2026090910_fix_sensitive_rpc_no_email` 这个迁移名字暗示做过一次「去 email」处理，但实测现状仍返回邮箱。

**修法**（三选一，推荐 ①）：
1. 改造为**不返回邮箱**：登录链路改为「EF 服务端用 service_role 解析」（EF 已有 `auth-login`，把解析挪进去，客户端不再有该 RPC）。
2. 保留 RPC 但加 **Altcha 人机校验**（项目已有 `altcha-challenge` EF + `auth_altcha_proofs` 表）。
3. 最低限度：返回布尔「是否存在」而非邮箱。

落点：`supabase/migrations/<新>_resolve_email_no_plaintext.sql`、`src/utils/api/auth-api.js:209,625`、`supabase/functions/auth-login/index.ts`

---

### P0-2 🔴 `cron.job` 内嵌明文 `sb_secret_` 高权限密钥

生产库 `cron.job#8`（`generate_forum_weekly_report_weekly`）的 `command` 里**明文内嵌了一个 41 字符的 `sb_secret_` 密钥**——这是 Supabase 新版 **service_role 等价密钥，可完全绕过 RLS**。

**已核验的边界**：
- ✅ 仓库工作区**无**该密钥（grep `sb_secret_` / `sb_publishable_` → 空）
- ✅ **Git 全历史无**（`git log --all -S"sb_secret_"` 命中的 2 个提交，经逐条 `git show` 确认都是**文档/注释里的占位符**，不是真密钥）
- ❌ 但库里是明文，且 `cron.job_run_details` 已累积 **11,296 行**——job 的 command 一并留痕，会进**数据库备份**

**风险**：任何能看 Supabase Dashboard / 拿到备份的人，即获得项目最高权限；一旦泄露无法通过 RLS 挽回。

**修法**：
1. **先轮换**该 secret key（Dashboard → Settings → API keys）。
2. 不再内联，改为从 DB 级设置读取——项目里已有现成模式：
   `alter database postgres set "app.settings.service_role_key" = '<新值>';`
   然后 `cron.job` 里用 `current_setting('app.settings.service_role_key')` 拼接 header。
   （`.workbuddy/memory/2026-09-15.md` 记录过同样的处置路径，本次是同一坑复发。）
3. 清理 `cron.job_run_details` 中的历史 command 留痕（`cron.job` 已有 7 天保留策略，但要确认它覆盖到含密钥的那批行）。

落点：`cron.job#8`（库内对象，非文件）+ 迁移文件里注册该 job 的 `do $cron$` 块

---

### P0-3 🔴 匿名可用 149 个 SECURITY DEFINER 函数，其中 33 个无任何身份校验

**规模实测**（远端库 `pg_proc` + `has_function_privilege`）：

```
public schema 中 anon 可 EXECUTE 的函数          232 个
  └─ 其中 SECURITY DEFINER                      149 个
       ├─ 有守卫（函数体内 current_user_is_admin() / auth.uid()）  93 个  ✅
       └─ 无守卫且非触发器函数（真实暴露面）        33 个  ❌
```

**根因**：Supabase 默认 `alter default privileges` 会给 `public` 下**新建函数自动授 anon EXECUTE**。项目已在 `2026090906_rpc_revoke_anon.sql` 做过一轮 revoke，但**没有覆盖全量**——存量函数仍是默认放开。记忆里那条「`auth.uid() is null` ≠ service_role」的教训就是这个坑，本次是**同一坑的存量部分**。

**已实测确认「有守卫」的一类（安全，但授权应收窄）**：
`admin_supabase_project_status` → 匿名调用返回 `{"ok":false,"error":"需要管理员权限"}`；`admin_delete_user_account` / `admin_get_user_sensitive` / `admin_grant_points` / `admin_ban_user` 等函数体内均有 `current_user_is_admin()` 前置门。**结论：不可利用**，但把 149 个 admin 类函数暴露给 anon 的 EXECUTE 是纯粹的纵深防御负债。

**已实测确认「无守卫且可被利用」的清单**（按危害排序）：

| 函数 | 危害 | 类别 |
|---|---|---|
| `resolve_email_for_login(p_username)` | 用户名→邮箱反查（P0-1） | PII 泄露 |
| `reserve_ai_token_quota(p_user_id, …, p_reserved_tokens)` | 传任意 `p_user_id` 给**指定用户**灌入 pending 预占 → 该用户 `v_used+v_pending` 超限 → **AI 配额被定向耗尽（拒绝服务）** | 定向 DoS / 业务破坏 |
| `reserve_ai_web_search(p_user_id, p_tier, p_daily_limit, p_since)` | 同上路径；且 `p_daily_limit` 由调用方传入 | 定向 DoS + 门参数外置 |
| `check_rate_limit(p_key, p_max_requests, p_window_seconds)` | 限流键与上限**全由调用方决定** → 可对他人限流键预占计数实现**锁定**；任何「靠它判限流」的路径可自证放行 | 限流规避 / 锁定他人 |
| `increment_rate_limit(p_key)` | 匿名可对其他用户的限流键累加 | 同上 |
| `insert_moderation_log(p_target_id, p_target_type, p_ai_result, p_ai_reason, p_moderator_id)` | 匿名可**伪造审核日志**并填入任意 `p_moderator_id` → 审计链可污染 / 可栽赃 | 审计完整性 |
| `increment_ad_clicks(target_id)` | 匿名可无限刷广告点击 → 计量造假 | 数据完整性 |
| `boh_enqueue_web_push()` | 匿名可触发推送入队 → 骚扰用户 / 消耗配额 | 滥用 |
| `sync_forum_like_counters()` / `sync_forum_comment_counters()` / `sync_forum_post_text_fields()` / `refresh_forum_post_counters(p_post_id)` / `sync_official_forum_card()` | 零参/单参全表重算，匿名可反复触发 | 廉价放大 DoS |
| `archive_boh_ai_*_when_inactive()`（4 个） / `archive_expired_completed_gifts(p_months)` / `cleanup_*` | 匿名可触发批量归档/清理 | 数据破坏 / DoS |
| `get_user_subscription_tier(p_user_id)` / `get_user_subscription_tiers(p_user_ids)` / `boh_cloud_image_limit_for_user(p_user_id)` / `boh_cloud_count_forum_images(p_user_id,…)` / `points_card_preset_capacity_for_user(p_user_id)` / `is_avatar_frame_owned_by(p_user_id, p_frame_id)` / `get_ai_token_usage_since(p_user_id, p_ip_address, p_since)` | **跨用户读取**会员等级 / 图片配额 / 用量 / 装扮归属 | 越权读取 |
| `get_recent_birthday_profiles(p_limit)` | 匿名读取近期生日用户资料（含 `birth_month/birth_day`） | PII 泄露 |
| `record_lottery_join_attempt(p_lottery_id, p_user_id, …)` / `log_forum_rate_limit_event(p_user_id, …)` | 匿名可伪造抽奖/限流事件记录 | 数据完整性 |
| `complete_points_card_preset_purge(p_preset_id, p_deleted, p_error)` | ⚠️ 已有 `auth.role()<>'service_role'` 门，**安全**（列出以示已核） | — |

**跨用户读取实证**（匿名，仅 anon key，取到真实用户 UUID 后）：
```
POST /rest/v1/rpc/get_user_subscription_tier        {"p_user_id":"d62665a9-…"}  → 200  "plus"
POST /rest/v1/rpc/boh_cloud_image_limit_for_user    {"p_user_id":"d62665a9-…"}  → 200  300
POST /rest/v1/rpc/points_card_preset_capacity_for_user {"p_user_id":"d62665a9-…"} → 200  [{"tier_code":"plus","capacity":6}]
POST /rest/v1/rpc/is_avatar_frame_owned_by          {"p_user_id":"d62665a9-…","p_frame_id":"boh-8th"} → 200 false
```
用户 UUID 可从公开可读的 `profiles`（`profiles_select_public` = true）直接获得 → **UUID 不是秘密，不构成防护**。

**修法**（按收益/成本排序）：

```sql
-- ① 总闸：回收 anon 的表级写权限 + 函数默认 EXECUTE（覆盖存量）
alter default privileges in schema public revoke execute on functions from anon;
revoke execute on all functions in schema public from anon;
revoke insert, update, delete on all tables in schema public from anon;

-- ② 按白名单回授「匿名确实需要」的那几个（登录链路）
grant execute on function public.resolve_email_for_login(text) to anon;  -- 若采纳 P0-1 方案①则不回授
-- 其余匿名必要的只读/公开函数（如 list_public_bohai_modes）逐个补

-- ③ 给「无守卫的写类/跨用户读类」函数补门（函数体内，不能只靠 grant）
--    只读且确实要公开的（如 get_recent_birthday_profiles）→ 改为不返回敏感列
```

> ⚠️ **两条落点约束**：
> 1. 门必须写在**函数体内**（`auth.uid()` / `current_user_is_admin()` / `auth.role()<>'service_role'`），因为 `revoke from public` 撤不掉 Supabase 自动授予 anon 的那部分（记忆里踩过）。
> 2. 撤权后**必须分别** `grant execute` 给 `authenticated` 与 `service_role`，漏 `authenticated` 会 42501（mock 不暴露）。

落点：`supabase/migrations/<新>_revoke_anon_function_execute.sql`、`supabase/migrations/2026090906_rpc_revoke_anon.sql`（存量未覆盖部分的续作）

---

### P1-1 🟠 75/88 张 public 表仍对 anon 授予 INSERT/UPDATE/DELETE

```
public 表总数                              88
其中 anon 有 INSERT / UPDATE / DELETE       75
```

RLS 目前是**唯一的门**：全 88 张表 RLS 均开启，4 张零策略表实际拒止，所以**当前不可利用**。但这意味着——
- 任何一张表将来 RLS 被误关 / 加了宽松策略，**立刻**变成匿名可写；
- 违反最小权限，且与项目自己的约定不一致（记忆：「新表 revoke 后须分别 grant anon + authenticated」→ 说明是按表手工加固，没有做 schema 级总闸）。

**修法**：见 P0-3 修法 ① 的 `revoke insert, update, delete on all tables ... from anon`，并加 `alter default privileges`.

---

### P1-2 🟠 Cloudinary 上传 preset 无签名 → DB 预检可被绕过

- `src/utils/cloudinary-client.js:384` → `formData.append('upload_preset', uploadPreset)`，**无 signature / api_key**（无签名上传）。上传只带 `file` / `upload_preset` / `folder` / 可选 `context`。
- 该 preset 名 + cloud name 内联进了构建产物（实测命中 `dist/static/js/ui-components-DUslnSIf.js`）→ **等同于公开凭据**（`cloud_name=dkqae7j1m`，`upload_preset=BOHIMG`）。
- 项目有一道 DB 闸 `assert_cloudinary_upload_allowed(p_source)`（grant 给 authenticated，`cloudinary-client.js:261` 调用）——但它是**上传前的客户端预检**，攻击者拿到 preset 名后可**直接向 Cloudinary 传**，完全不经过这道闸。
- ⚠️ **补充（2026-09-21 复核时新发现）**：该预检函数体第一行是 `if (!userId) return;` —— **未登录访客直接跳过整道预检**。所以对匿名上传者而言，这道闸根本不存在。

**风险**：消耗账号配额/存储、把任意内容托管在项目 Cloudinary 交付域名下（内容合规与声誉风险）；`context.uid` 由客户端提供，删除归属校验也可被伪造。

**修法（两条路，代码成本不同）**：

| 路线 | 做法 | 是否需要改代码 |
|---|---|---|
| ① **签名上传**（根治） | preset 设为 Signed；客户端必须带 `signature` + `api_key` + `timestamp`，而签名要用 API secret 计算 → **必须新增一个签名用 Edge Function**，并改 `cloudinary-client.js` | ✅ **需要**（EF + 客户端），约 1 个 EF |
| ② **收紧 preset 约束**（缓解，零代码） | 控制台里限制 allowed formats（jpg/png/webp/gif）、max file size、固定 folder、开启 moderation；并配账号级用量告警 | ❌ 纯控制台 |

> 更正：此前把本项描述为「跟代码仓库无关」**只在路线 ② 成立**。走根治路线 ① 是代码活。

**✅ 实施进展（2026-09-21，路线 ① 代码已就绪）**

| 产出 | 说明 |
|---|---|
| `supabase/functions/_shared/cloudinary-sign.ts` | 纯签名模块：按 Cloudinary 口径排序拼接 + SHA-1(payload+secret)；带 folder 校验（拒 `..`/`//`/大写/超长）。**测试向量由独立实现（Python hashlib）交叉校验**，非自我印证 |
| `supabase/functions/cloudinary-sign-upload/index.ts` | 新 EF：① 必须带有效登录态（anon key 无法通过）② **服务端**执行 `assert_cloudinary_upload_allowed`（用用户 JWT 调，使 `auth.uid()` 生效）——把原本可绕过的客户端预检变成真闸 ③ 按 `CLOUDINARY_UPLOAD_MODE` 下发签名 |
| `src/utils/cloudinary-client.js` | `uploadImageToCloudinary` 增加签名分支：signed 时**参数一律以服务端返回为准**（含 `context` 归属标记，客户端无法伪造）；`resolveCloudinaryUploadSignParams` 区分四态 signed / unsigned / unavailable / **blocked** |
| CI | `scripts/cloudflare-security-headers.mjs`（见 P2-1） |

**切换顺序（与前端部署顺序无关）**：
1. 先部署 EF（`CLOUDINARY_UPLOAD_MODE` 默认 `unsigned`）→ **线上行为零变化**
2. 设置 EF secrets：`CLOUDINARY_API_KEY` / `CLOUDINARY_API_SECRET`（**不要贴进对话，本地执行**）
3. 把 Cloudinary preset 的 Signing Mode 改为 **Signed**
4. `supabase secrets set CLOUDINARY_UPLOAD_MODE=signed` → 立即切换，**无需重新部署**

**关键安全不变量（有测试守）**：EF 明确拒绝（未登录 401 / 额度 429 / 配置缺失 5xx）时**绝不静默降级**为无签名上传——否则等于把刚堵上的洞又打开。反证已验：改成「一律回落 unsigned」→ 4 个测试转红。

**落点**：`supabase/functions/cloudinary-sign-upload/`、`supabase/functions/_shared/cloudinary-sign.ts`、`src/utils/cloudinary-client.js`、`tests/unit/cloudinary-sign.test.js`、`tests/unit/cloudinary-signed-upload.test.js`

---

### P2-1 🟠 线上零安全响应头

**生产实测** `https://www.blockofhome.cn/`：

```
HTTP/2 200
content-type: text/html; charset=utf-8
server: cloudflare
access-control-allow-origin: *
cache-control: max-age=600
```
**缺失**：`Content-Security-Policy`、`Strict-Transport-Security`、`X-Content-Type-Options`、`Referrer-Policy`、`X-Frame-Options`、`Permissions-Policy` —— 一项都没有。

`public/_headers` 里也只有 `Cache-Control`，且**注释自己写明**：GitHub Pages 不支持自定义 HTTP 头，此文件是给迁移到其他平台备用（与记忆「`public/_headers` 线上完全不生效」一致）。

**叠加放大**：与「Session 存 localStorage」（报告 S-6 仍属实）组合 → 一旦出现 XSS，token 无任何缓解层可挡；且站点可被任意第三方 iframe 嵌套（点击劫持）。

**修法（2026-09-21 复核补充）**：
- Cloudflare **Transform Rules → Modify Response Header 免费版即可用**（免费版共 10 条规则、单条最多改 10 个响应头）。域名已在 Cloudflare 之后（实测 `server: cloudflare` + `cf-ray` + `cf-cache-status`），故此为唯一顺路落点。
- **分两步走**：
  - **第一步（零风险，可立即做）**：`X-Content-Type-Options: nosniff`、`Referrer-Policy: strict-origin-when-cross-origin`、`X-Frame-Options: DENY`、`Permissions-Policy: camera=(), microphone=(), geolocation=()`，以及 HSTS（Cloudflare 有独立开关：SSL/TLS → Edge Certificates）。这 4+1 个只收紧默认行为、**不拦截任何现有资源**，能拿到大部分收益。
  - **第二步（需灰度）**：CSP。站点有大量 inline 样式与动态 chunk、且跨源到 `*.supabase.co` / `res.cloudinary.com`，**一次性上强制 CSP 极可能白屏** → 必须先上 `Content-Security-Policy-Report-Only` 观察收敛（响应里已有 `report-to`/`nel` 通道可用），稳定后再改强制。
- 备选路线：写 Cloudflare Worker 注入（仓库已有 `cloudflare/gemini-proxy/` 的 Worker 部署链路可复用），但 Worker 挂到 `www.blockofhome.cn/*` 会接管全部请求，需正确回源，风险高于 Transform Rules。
- 同步更新 `public/_headers` 以保持单一源（该文件线上不生效，仅作迁移备用）。

**✅ 自动化已就绪（2026-09-21）**：`scripts/cloudflare-security-headers.mjs` + CI 步骤（`deploy.yml` 的 "Apply Cloudflare security headers"）。
- 安全设计：只管理带 `[boh-security-headers]` 标记的那一条规则（读-改-写），**不会覆盖**手工加在同 phase 的其他规则；未配 token / 权限不足 / 任何失败一律「告警 + exit 0」，**不阻断发布**（与仓库既有 purge 步骤同策略）。
- 默认下发「零风险四件套 + HSTS」，CSP 以 **Report-Only** 下发；`--csp-enforce` 才转强制。`--remove` 只删自己那条。
- ⚠️ **前置**：现有 `CLOUDFLARE_API_TOKEN` 注释写明只有 **Zone.Cache Purge** 权限，跑该 phase 会 403。需在 Cloudflare → My Profile → API Tokens 给该 token 追加 **Zone → Config（Transform Rules）→ Edit**。补齐后每次发布自动保持。

**落点**：`scripts/cloudflare-security-headers.mjs`、`.github/workflows/deploy.yml`、Cloudflare 控制台（token 权限）+ `public/_headers`（保持单一源，非生效路径）

---

### P2-2 🟠 过滤器转义口径不一致

| 位置 | 现状 |
|---|---|
| `src/utils/api/profile-api.js:384` | `String(safeUsername).replace(/[,.\()']/g,'')` ✅ 已转义 |
| `src/views/user-center/UserSpace/UserSpaceMain.vue:1124` | 用 `safeUsername` ✅ |
| `src/views/Profile/ProfileMain.vue:1367` | `.or(\`author_id.eq.${userId},author_username.eq.${username}\`)` ❌ **未转义** |

PostgREST `.or()` 的 `,` `.` `(` `)` 是语法字符，未转义可注入额外过滤条件。**实际危害低**（posts 本就公开可读，且 `username` 来自被查看者资料而非任意输入），但属于**同仓两套口径**——与项目「真相源单一化」的原则冲突，且是未来被误用的模板。

**修法**：抽出 `escapePostgrestFilterValue()` 单源放进 `src/utils/api/`，三处统一调用。

---

### P3-1 🟡 依赖漏洞 6 项（5 high / 1 moderate）

```
image-size  <=2.0.2   high   ICNS/JXL/HEIF 解析死循环 DoS   ← 经 pptxgenjs
nanoid      <=3.3.17 / 4.0.0-5.1.15  high  负 size 死循环    ← 经 docx + 直接依赖
postcss     <=8.5.22  high   sourceMappingURL 任意 .map 读   ← 构建期
```
均为**客户端 DoS / 构建期**类，**无 RCE、无服务端影响**。`npm audit fix` 可修（`pptxgenjs` 需 `--force` 降级到 2.2.0，属破坏性变更，要先确认导出功能）。

---

## 四、修复优先级

> **执行状态（2026-09-21 当日）**
>
> | 项 | 状态 | 证据 |
> |---|---|---|
> | P0 `resolve_email_for_login` 去邮箱化 | ✅ **EF 已部署并验证**；前端待推送 | 新增 `supabase/functions/_shared/resolve-login-id.ts`；`auth-login/index.ts` 改为引用之；`auth-api.js` 用户名登录改走 EF + `setSession`；删除 `getEmailByUsername`。48 个单测通过（反证 8 红 / 10 红）；全量 122 文件/2008 用例全绿；构建通过。EF 部署后已用「上传清单＝import 图」证明新代码生效（`altcha.ts` 未上传 / `resolve-login-id.ts` 已上传）。**剩余：推送 main 让前端切换**（未切换时自动降级到旧路径，登录不中断）。⚠️ **但 P0 并未关闭**：只要 `LOGIN_LEGACY_RPC_FALLBACK = true`，`resolve_email_for_login` 仍匿名可达且仍返回明文邮箱——必须等前端切换上线并验证后，删掉降级分支并 `drop function public.resolve_email_for_login(text)`，这条 P0 才算真正关闭 |
> | P0 撤 anon 函数 EXECUTE + 表级写权限 | ✅ **已应用** | 迁移 `2026092101_revoke_anon_execute_and_table_writes.sql`（已在库内记录）。anon 可执行函数 **224→160**；anon 可写表 **75→0**；authenticated 表写权限 **75 不变**；service_role 仍可调配额函数。匿名探针差分：公开页所需 RPC **全部不变**，攻击面全部转为 `42501`。⚠️ 本轮复验发现并已修复该迁移的**防复发缺口**：原句只撤 `anon`，而函数在 PostgreSQL 里默认对 `PUBLIC` 授予 EXECUTE、anon 经 PUBLIC 继承 → **此后新建的每个函数都会重新变成匿名可调**，修复会随时间衰减。已把该句改为 `from anon, public`；并因 2026092101 **已在远端执行过**（改文件不会重跑），另加后续迁移 `2026092102_fix_anon_default_privileges.sql` 纠正已生效的默认权限 |
> | P0 轮换 `sb_secret_` 密钥 | ⬜ **待你在 Dashboard 执行** | 需人工操作，见 P0-2 修法 |
> | P1 Cloudinary 签名上传 | 🟡 **代码已就绪，待配置与切换** | 新增 EF `cloudinary-sign-upload` + `_shared/cloudinary-sign.ts`（签名向量经独立实现交叉校验）+ 客户端签名分支；35 个新测试通过（反证 4 红）。待：部署 EF → 设 `CLOUDINARY_API_KEY/SECRET` → preset 改 Signed → `secrets set CLOUDINARY_UPLOAD_MODE=signed` |
> | P1 安全响应头 | 🟡 **自动化已就绪，待 token 补权限** | `scripts/cloudflare-security-headers.mjs` + CI 步骤（读-改-写、只动自己那条规则、失败不阻断发布、CSP 默认 Report-Only）。待：给 token 追加 Zone → Config（Transform Rules）→ Edit |
> | P2 过滤器转义单源 / 依赖升级 | ⬜ 待办 | — |
> | 第二阶段（需匿名流量实测后决定） | ⬜ 待办 | `increment_ad_clicks` 节流、`resolve_email_for_login` 撤权、`execute_lottery_draw` 等 6 项 |

| 级别 | 动作 | 落点 |
|---|---|---|
| **P0** | 轮换 `sb_secret_` 密钥，`cron.job#8` 改从 `app.settings` 取，清历史 command 留痕 | 库内 cron.job + 迁移 |
| **P0** | 撤 anon 函数 EXECUTE 总闸 + 按白名单回授；给 33 个无守卫函数补函数体内门 | 新迁移 `*_revoke_anon_function_execute.sql` |
| **P0** | `resolve_email_for_login` 去邮箱化（挪进 `auth-login` EF 或加 Altcha） | 新迁移 + `auth-api.js` + `auth-login` EF |
| **P1** | `revoke insert,update,delete on all tables in schema public from anon` + `alter default privileges` | 同一迁移 |
| **P1** | Cloudinary preset 改签名（或收紧 folder/格式/大小 + moderation） | Cloudinary 控制台 |
| **P1** | 经 Cloudflare 注入 5 个安全响应头（CSP 先 Report-Only） | Cloudflare + `public/_headers` |
| **P2** | 抽 `escapePostgrestFilterValue()` 单源，修 `ProfileMain.vue:1367` | `src/utils/api/` |
| **P2** | 依赖升级（`pptxgenjs` 破坏性变更需先验证导出） | `package.json` |
| **P3** | Session 迁 httpOnly（报告 P3-31，长期） | `supabase-client.js` |

---

## 五、结论边界（哪些是实测，哪些是推断）

**直接实测（生产环境 HTTP 探针，只读操作）**：
- `resolve_email_for_login` 匿名可达 + 返回邮箱；限流 10/分/IP **且 XFF 不可伪造**（做了 A/B 两组对照实验，初始假设被实验推翻）
- `admin_supabase_project_status` 匿名被守卫拦截（守卫有效）
- `get_user_subscription_tier` / `boh_cloud_image_limit_for_user` / `points_card_preset_capacity_for_user` / `is_avatar_frame_owned_by` 匿名读取**真实用户**数据成功
- 线上响应头缺失 6 项（curl `-I`）
- `dist` 内仅 `role=anon` JWT
- 依赖漏洞 6 项（npm audit）

**远端库目录快照（`pg_policy` / `pg_proc` / `acl` 权威口径，非源码推断）**：
- 88 表 RLS 全开启 / 4 表零策略 / 75 表 anon 有写权限
- 232 anon 可执行函数 / 149 SECURITY DEFINER / 33 无守卫非触发器
- `cron.job#8` 内嵌 41 字符 `sb_secret_`（长度与格式已核，**明文未写入本报告**）
- 全部 S-1/S-3/S-4 修复状态

**未验证 / 需人工确认**：
- `insert_moderation_log`、`increment_ad_clicks`、`reserve_ai_*` 等**写类函数未做活的写入探针**（避免污染生产数据），结论依据是 `has_function_privilege` 目录事实 + 函数体无门 + 同批函数的只读探针已证明匿名可达。**建议在预发/本地 mock 上补一次写入级验证。**
- Cloudinary preset 的实际约束（folder / allowed formats / max size / moderation 是否已在控制台配置）**无法从仓库推断**，需在控制台确认；本报告只确认「客户端无签名上传」。
- CSP 注入后是否会白屏**未验证**（项目 inline 样式与动态 chunk 多），必须先 Report-Only。
- 33 个函数中「DB 内被 cron / service_role 调用」的部分，撤 anon EXECUTE 前需确认**没有依赖 anon 身份的调用路径**（否则会出现 42501）。

---

## 六、实现改动验证（2026-09-21 追加复验）

> 验证原则：**不采信改动自带的注释与说明**。迁移里「无前端调用点」「实测有效」这类断言一律回到代码与目录事实反向核查。

### 6.1 三闸门实测（本机独立复跑）

| 门禁 | 命令 | 结果 |
|---|---|---|
| 单元测试 | `npx vitest run` | ✅ **122 文件 / 2008 通过 + 1 跳过**，0 失败 |
| 类型检查 | `npm run type-check` | ✅ exit 0 |
| 静态检查 | `npm run lint` | ✅ **0 errors** / 376 warnings（全为既有 `no-unused-vars`，非本次改动引入） |

### 6.2 授权判断的反向核查（不查就是盲信）

迁移 `2026092101` 的 Part 1 把 8 个函数**连 `authenticated` 一起撤**——这是最激进一档，若前端有调用点会当场打断功能。逐条 grep `src/` 实测：

| 集合 | 核查结论 |
|---|---|
| Part 1 八函数（`reserve_ai_token_quota` / `reserve_ai_web_search` / `release_ai_token_quota` / `settle_ai_token_quota` / `settle_ai_web_search` / `check_rate_limit` / `increment_rate_limit` / `get_ai_token_usage_since`） | **0 处前端调用点** ✅ 撤到 authenticated 是安全的（它们只由 EF 以 service client 调用） |
| 白名单保留（`resolve_email_for_login` / `assert_cloudinary_upload_allowed` / `increment_ad_clicks` / `cleanup_lottery_scheduler_logs` / `execute_lottery_draw`） | **均有**前端调用点（`auth-api.js:331`、`cloudinary-client.js:261`、`AdSlot.vue:41`、`DataAdmin.vue:4344`、`useDataAdminMutations.js:213,262`）✅ 保留正确，**无漏撤** |

→ 授权判断与代码事实一致，**不会打断线上**。

### 6.3 复验中发现 2 处缺陷（均已修复）

#### 缺陷 1（影响修复的**持久性**）—— 防复发语句只撤 `anon`

`2026092101` 原第 120 行：
```sql
alter default privileges in schema public revoke execute on functions from anon;   -- ❌ 无效
```
函数在 PostgreSQL 里**默认对 `PUBLIC` 授予 EXECUTE**，anon 经 PUBLIC 继承后照样能执行——这正是该文件 `:9-13` 自己实测证明过的机理，只是防复发语句没跟上。

**后果**：存量函数已被收紧，但**此后新建的任何一个 public 函数都会重新变成匿名可调**，修复会随时间衰减。

**已修**：改为 `from anon, public`，并补注说明「必须同时撤 public」以免后人"简化"回去。

> ⚠️ **本节结论已被后续实测部分推翻 —— 请以 [6.7](#67-2026092102-应用与实测结论修正) 为准。** 改正语句本身是对的（`from anon, public` 对**存量**函数有效），但它**无法防住增量**：新建函数仍会被 anon 执行。原因是 `defaclrole=supabase_admin` 那条默认权限我们碰不到（42501），而新函数仍从 PUBLIC 拿到 EXECUTE。

**关键补充动作**：`2026092101` **已在远端库执行过**（并写入 `supabase_migrations.schema_migrations`），改它的文件内容**不会重跑它**。因此另建后续迁移 `2026092102_fix_anon_default_privileges.sql` 来纠正**已生效**的默认权限。→ 这是「改文件救不了已应用的迁移」的典型场景，容易漏。

#### 缺陷 2（影响**事故恢复**）—— 回滚附录 73 行语法错误

回滚附录全部写成：
```sql
-- revoke execute on function public.xxx(...) to anon, authenticated;   -- ❌ 恢复授权要用 grant
```
`revoke ... to` 不是合法语法。这 73 行一旦在事故中被取消注释执行，**每一条都报语法错误**——这份回滚附录在最需要它的时候是失效的。

**已修**：73 行全部改为 `grant execute on function ... to ...`（残留 0 行），并注明「PUBLIC 的隐式 EXECUTE 有意不恢复，避免回滚时把匿名调用面一并还原」。

#### 修复的精准性自证

```
活跃语句（非注释）差异：仅 1 行    ← 只有防复发那行变了，73 条 revoke 一条未动
注释区：73 行 revoke→grant + 防复发注释 + 回滚说明
文件行数：214 → 218
```

### 6.4 逐项状态（对应本报告第三章编号）

| 本报告编号 | 其他报告编号 | 现状 | 剩余动作 |
|---|---|---|---|
| P0-1 匿名枚举邮箱 | SEC-3 | 🟡 **部分修复** | EF 已部署并验证；**前端待推送**；⚠️ **P0 未关闭**——降级分支在，RPC 仍匿名可达且返回邮箱 |
| P0-2 `cron` 明文密钥 | SEC-1 | ⬜ **完全未动** | 需你在 Dashboard 轮换 + 改 `current_setting` 读取 |
| P0-3 匿名函数 EXECUTE | SEC-2 | 🟡 **已应用 + 本轮补缺陷** | 待执行 `2026092102`（纠正已生效的默认权限） |
| P1-1 表级 anon 写权限 | SEC-4 | ✅ 已应用 | — |
| P1-2 Cloudinary 无签名 | SEC-5 | 🟡 代码就绪，默认未启用 | 部署 EF → 设 secrets → preset 改 Signed → 切 mode |
| P2-1 零安全响应头 | SEC-6 | 🟡 自动化就绪 | CF token 补 Zone → Config（Transform Rules）→ Edit |
| P2-2 / P3-1 | — | ⬜ 未动 | — |

### 6.5 本轮未验证项（诚实边界）

- **远端库状态本轮无法复验**：验证过程中外网整体不可达（`api.supabase.com` / Supabase REST / 站点三处 curl 均 `HTTP=000`，SSL EOF）。迁移「已应用」这一判断依据的是**本会话早前实测**（anon 可执行 **224→160**、anon 可写表 **75→0**、authenticated 写权限 75 不变），**不是本轮现测**。
- **缺陷 1 的修复未做活的 DB 探针**：已写好可回滚的探针 SQL（见 `2026092102` 文件末尾注释），待网络恢复后可一键复验「新建函数不再匿名可调」。
- **缺陷 1/2 的修复为文本级验证**：diff 证明改动精准（活跃语句仅 1 行变化、73 行回滚语句全改）；`grant` / `alter default privileges ... from anon, public` 的语法正确性依据 PostgreSQL 语法与 `2026092101` 中同形语句（其 `alter default privileges ... from anon` 变体已在远端执行成功）。
- 依赖漏洞、CSP 白屏风险等边界项见第五章。

### 6.6 报告关系说明

`docs/` 下另有 `2026-09-21-仓库可维护性审查报告.md`（另一路审查产出，用 SEC-x / 批次 编号）。两份报告结论一致，但编号体系不同、且那份的「SEC-2/4 待执行迁移」表述与**本会话早前的实测**不符（见 6.5）。**引用前请以本报告 6.4 的编号映射为准**；若两份需合并，建议保留 SEC-x 编号并把本报告 6.4 的映射表并入。

---

## 七、下一步执行顺序（按「落地成本 × 风险」排序）

> 排序原则：**先收口已投入的工作，再新开工**。当前有 5 项「代码就绪但未启用」，它们已经是沉没成本，不落地的边际损失最大；而且长期停在半途会形成**假安全感**——下次审计会把它们当成已修，而线上实际仍未防护。

### 第 0 步（只有你能做，约 5 分钟，收益最大）

**轮换 `cron.job#8` 里的明文 `sb_secret_`**（P0-2 / SEC-1）。

这是唯一一个**「泄露即全盘失守」**的项：该密钥 service_role 等价、**绕过所有 RLS**，而本报告其余全部修复都建立在「它没被泄露」这个假设之上。目前它明文躺在库里，且 `cron.job_run_details` 已累积 11,296 行会一并进备份。

1. Dashboard → Settings → API keys → 轮换 secret key
2. `alter database postgres set "app.settings.service_role_key" = '<新值>';`
3. 重建 `cron.job#8`，改用 `current_setting('app.settings.service_role_key')` 拼接 header（不再内联）
4. 清理 `cron.job_run_details` 中含旧密钥的历史行

### 第 1 步（1 条 SQL，我可代做，需网络）

**应用 `2026092102_fix_anon_default_privileges.sql`** —— 纠正已生效的默认权限，否则新建函数会重新变成匿名可调（见 6.3 缺陷 1）。幂等、只影响将来创建的对象、不动任何存量授权。

### 第 2 步（1 次 push + 1 条 SQL）

**收口 P0-1**：推送 main 让前端切到 EF 登录 → 确认线上登录正常 → **删除 `LOGIN_LEGACY_RPC_FALLBACK` 降级分支** → `drop function public.resolve_email_for_login(text)`。**走完这四步，「匿名枚举用户名拿邮箱」这条 P0 才算真正关闭**；在此之前它一直可被利用。

### 第 3 步（1 次点击）

**给 Cloudflare token 追加 Zone → Config（Transform Rules）→ Edit** —— 自动化脚本已就绪，补完权限后每次发布自动保持，4 个零风险头 + HSTS 立即生效，CSP 以 Report-Only 开始收数据。

### 第 4 步（需你设密钥，我不能代做）

**切换 Cloudinary 签名上传**：部署 `cloudinary-sign-upload` → 设 `CLOUDINARY_API_KEY` / `CLOUDINARY_API_SECRET` → preset 改 Signed → `secrets set CLOUDINARY_UPLOAD_MODE=signed`。在此之前线上仍是**无签名上传**，任何拿到内联 preset 名的人都能往账号传文件。

### 第 5 步（纯代码，不依赖网络/账号，我建议现在就做）

**① 身份单一源化（本轮复验新识别的残余）**

`2026092101` 只撤了 `anon`，但这三个函数的**身份参数由客户端传入**，登录用户仍可伪造：

| 函数 | 残余风险 | 修法 |
|---|---|---|
| `insert_moderation_log(p_…, p_moderator_id)` | 登录用户可伪造审核日志、把记录**栽给任意 moderator** | `coalesce(auth.uid(), p_moderator_id)` |
| `log_forum_rate_limit_event(p_user_id, …)` | 可伪造他人的限流事件 | `coalesce(auth.uid(), p_user_id)` |
| `record_lottery_join_attempt(p_user_id, …)` | 可伪造他人抽奖记录 | `coalesce(auth.uid(), p_user_id)` |

影响是**审计链完整性**（不是提权），但审计链被污染 = 真出问题时查不出来。`coalesce` 写法的好处：**对 cron / service_role 调用无影响**（此时 `auth.uid()` 为 NULL，回落用入参），所以零破坏风险。

**② `increment_ad_clicks` 内部节流**

白名单项，仍未处理：现在**任何人（含未登录）**可无限刷广告点击数。因公开页游客点击需计数，不宜撤权，改为在函数体内按 `(ip, ad_id)` 限流一次/窗口。

### 第 6 步（需先做匿名流量实测）

**第二阶段 6 项撤权**（`execute_lottery_draw` / `execute_due_lottery_draws` / `assert_cloudinary_upload_allowed` / `cleanup_lottery_scheduler_logs` / `increment_ad_clicks` / `resolve_email_for_login`）。

**为什么不能直接撤**：这些函数前端有调用点，且所在页面可能是**公开路由**（`/lotteries`、注册流程等）。静态分析**推不出「哪些公开页真的会调」**——路由公开 ≠ 页面不调登录态 RPC，组件 mount 时是无条件调用的。必须先跑一次**无登录态浏览器抓包**，拿到真实的匿名 RPC 清单，再按清单撤。否则撤了就是当场打挂游客页面。

### 第 7 步（低危，可与其他批次并行）

- **P2-2 过滤器转义单源**：抽 `escapePostgrestFilterValue()` 统一三处（`profile-api.js:384` 已做、`ProfileMain.vue:1367` 未做）——低危，但违背项目「真相源单一化」原则，且是未来被误用的模板。
- **P3-1 依赖漏洞 6 项**：`npm audit fix`；`pptxgenjs` 那条是破坏性变更（降到 2.2.0），**必须单独一批**并先验证文档导出功能，不要与安全改动混在一批里，否则回归难以归因。

### 一句话总结

**只有第 0 步是「你不动手就没人能做」的真 P0**；第 1～4 步都是「差最后一下」的收口，边际成本极低；第 5 步是纯代码、我可以立刻开工；第 6 步卡在一次匿名抓包上；第 7 步可延后。

### 6.7 `2026092102` 应用与实测（结论修正）

**已推送**：`npx supabase db push --linked --yes` 成功；`supabase migration list` 现显示 `2026092101` / `2026092102` 三列一致。
**顺带证实**：`migration list` 里 `2026092101` 三列一致 → **该迁移确实早已在远端生效**（见 6.6 对另一份报告「待执行」表述的质疑，此处以 CLI 输出为准）。

#### 实测结果：目标只达成一半

| 检查项 | 实测结果 |
|---|---|
| `defaclrole=postgres / public / functions` 默认权限 | `{postgres=X, authenticated=X, service_role=X}` —— **anon 与 PUBLIC 均已移除** ✅ |
| `defaclrole=supabase_admin / public / functions` 默认权限 | `{anon=X, authenticated=X, postgres=X, service_role=X}` —— **anon 仍在** ❌ 且尝试修改报 `42501 permission denied to change default privileges` |
| **新建函数是否仍可被 anon 执行** | **`t`（是）** ❌ —— 新函数 ACL = `{=X/postgres, postgres=X/postgres, authenticated=X/postgres, service_role=X/postgres}`，即 **PUBLIC 的 EXECUTE 仍在**，anon 经 PUBLIC 继承 |

#### 结论修正（重要）

**6.3 缺陷 1 里「已改为 `from anon, public` 即可防复发」的说法不成立。** 准确表述应为：

- ✅ **存量侧有效**：`revoke execute ... from anon, public` 对**已存在**的函数确实有效（本会话早前对 `admin_ban_user` 做过 A/B 实证：只撤 anon 无效、撤 anon+public 后 `anon_can_execute=f`）。这也是 `2026092101` 把 anon 从 224 降到 160 的机制。
- ❌ **增量侧无效**：`alter default privileges` 这条路在本环境**保证不了**「新建函数不被 anon 执行」。已排除「`from public` 没写上」这一解释（该条目里 PUBLIC 确实已消失），真实障碍是**另一条我们碰不到的默认权限条目（`supabase_admin`）+ PUBLIC 的内建 EXECUTE**。

#### 另一个值得记住的推论

全库 **160** 个 anon 可执行函数里，有 **92** 个同时带着 PUBLIC 的 EXECUTE → 对它们**只撤显式 anon 授权是无效的**，必须连 `public` 一起撤。这解释了为什么任何「只 `revoke ... from anon`」的写法都会静默失效——`2026092101` 从一开始就用 `from anon, public`，所以存量侧没有踩这个坑。

#### 增量防护的两条可行路线（默认权限路线已排除）

1. **逐条显式撤权**（与 `2026092101` 同法）：每次新增函数时，在同一迁移里显式 `revoke execute on function ... from anon, public`。可控、无依赖，但要靠人记得。
2. **加一道检测**：定期 / 发布前扫描 `public` 下 anon 可执行函数，超出白名单即告警。项目已有十余个 `scripts/check-*.mjs` 门禁，符合现有工程惯例，且**不依赖任何我们改不动的数据库权限**。

> 建议：新增函数时用 ①，同时用 ② 兜底（防止漏写）。**不要再用 `alter default privileges` 当作防复发手段**。

#### 对第七章执行顺序的影响

第七章「第 1 步」已完成（`2026092102` 已应用）。**第 6 步「第二阶段撤权」的优先级应上调**——因为「新增函数自动不开放」这条护栏实际不存在，每多一个未加固的新函数就是一个新的匿名入口；在护栏补齐前，撤权与检测的价值更高。

### 6.8 Cloudinary 与 Cloudflare 两项的现状与实证（2026-09-21 追加）

#### 6.8.1 Cloudinary：EF 已上线，但**漏洞并未关闭**（无损实证）

`cloudinary-sign-upload` EF 已部署并验证：无凭证 → `401 UNAUTHORIZED_NO_AUTH_HEADER`；
**用 anon key 冒充 Authorization → `401 INVALID_SESSION`**（后者证明是 EF 自身逻辑拦下的，不是网关）。

但**这并不等于漏洞关闭**。实测判定 preset 签名模式（**无损**：不发任何文件、不产生任何资源）：

```bash
# 带 preset、不带 file
curl -s -X POST "https://api.cloudinary.com/v1_1/dkqae7j1m/image/upload" -F "upload_preset=BOHIMG"
# 实测返回 → {"error":{"message":"Missing required parameter - file"}}   ← 已通过 preset 校验

# 对照组：不带 preset
curl -s -X POST "https://api.cloudinary.com/v1_1/dkqae7j1m/image/upload"
# 实测返回 → {"error":{"message":"Upload preset must be specified when using unsigned upload"}}
#            ↑ 这句「when using unsigned upload」证明该端点走的是无签名上传路径
```

第一句说明 `BOHIMG` **仍接受无签名请求** → **preset 仍是 unsigned**。
因此：**EF 只挡住了「经由本项目前端的上传路径」；任何人拿到内联在公开产物里的 `cloud_name` + `preset` 名，
仍可绕过 EF 直接往账号传文件。** 真正的关闭需要两步：preset 改 Signed → `CLOUDINARY_UPLOAD_MODE=signed`。

> ⚠️ **勘误（2026-09-21 当日更正）**：本报告草稿此处原写「需要三步，含配置 `CLOUDINARY_API_KEY/SECRET`」，
> **这是错的**。这两个值**早已存在于项目 secrets 中**（供 `cloudinary-delete` 使用，见 §6.8.4），无需新配。
> 误判成因见 §6.8.4 末尾。

> 这里要明确区分：**「前端路径已加固」与「漏洞已关闭」是两件事**。把前者当作后者是本报告反复提醒的
> 「假安全感」——下次审计会以为该项已修，而实际攻击面未变。

#### 6.8.2 Cloudflare：自动化就绪，卡在 token 权限（本地无可用凭据）

自动化已就绪（脚本 + CI 接线已上线，见 §6.7）。本轮尝试用本地凭据直接完成，结论是**必须由用户操作**：

- `~/Library/Preferences/.wrangler/config/default.toml` 虽有 wrangler OAuth 凭据，但
  **已过期**（`expiration = 2026-09-17T03:12:25Z`）→ 实测调用返回 `9109 Invalid access token`；
  且 scopes 中**只有 `zone:read`**，不含任何 zone 级写权限。
- CI 中 `CLOUDFLARE_API_TOKEN` 报 `9106 Authentication failed (status: 400)`。9106 的常见文案是
  「Missing X-Auth-Key/Email/Authorization」（认证头缺失），而此处带 `status: 400` 的
  「Authentication failed」出现在**权限不足**场景。关键旁证：**同一次 run 里 purge cache 用同一 token
  成功了** → token 本身有效，是缺该端点所需权限。

两条路径（任选其一）：

| 路径 | 操作 | 效果 |
|---|---|---|
| ① 补 token 权限 | 给现有 token 加 `Zone → Config → Edit`（即 Transform Rules） | CI 每次发布自动保持 |
| ② 手动加规则 | 照 `npm run security:headers:dry` 的输出在控制台建规则，**描述必须含 `[boh-security-headers]`** | 立即生效；将来 token 权限补齐后脚本自动接管（幂等），不产生重复规则 |

⚠️ Cloudflare 有 **User API Token**（`/profile/api-tokens`）与 **Account API Token**（`/{account_id}/api-tokens`）
两套入口，权限下拉项不同；Transform Rules 属 **Zone** 维度。

#### 6.8.3 两件事的当前状态一句话

- **CF**：脚本与 CI 都到位、失败不阻断发布（实测告警 + `exit 0`）；**线上仍是零安全响应头**，等你补权限或手动加。
- **Cloudinary**：EF 与非秘密配置已到位，且 **API key/secret 早已存在**（供 `cloudinary-delete` 使用，见 §6.8.4）；
  **preset 仍无签名、mode 仍 unsigned**，攻击面未变 —— 只差两步：`secrets set CLOUDINARY_UPLOAD_MODE=signed` + 把 preset 改为 Signed。

#### 6.8.4 `CLOUDINARY_API_KEY/SECRET` 的来源：**早已存在，无需新配**

排查起因：一度把一个**推断**当成了事实写进待办（「需要你配置 API key/secret」）。核查后更正：

- `secrets list` 中 `CLOUDINARY_CLOUD_NAME` / `CLOUDINARY_UPLOAD_PRESET` / `CLOUDINARY_API_KEY` / `CLOUDINARY_API_SECRET` 四项均在。
- **`CLOUDINARY_API_KEY` / `SECRET` 不是近期配置的**：`supabase/functions/cloudinary-delete/index.ts`
  的 L4–L6 读取它们，**L56 / L91 用 SECRET 做 sha1 签名**（`sha1Hex('public_id=…&timestamp=…' + SECRET)`），
  L61–L62 把 `api_key` + `signature` 发给 Cloudinary 的 `/destroy`；L199 在缺配置时直接返回
  `CLOUDINARY_ENV_MISSING`。该文件最近改动为 **2026-08-16** —— 即一个月前「删除用户上传图片」就已依赖这两个值。
- 仓库内**不存在**任何自动设置它们的脚本或 workflow（已搜 `.github/`、`scripts/`、`package.json`）；
  `.env.example` 只列 `VITE_CLOUDINARY_*`（前端公开变量），**不含** API key/secret 模板。
- **推论**：若站内「删除自己上传的图片」功能正常，即说明这两个凭据有效 → **无需重设**。

**顺带发现（低危）**：`cloudinary-delete` 同样在**模块顶层**读 `Deno.env.get()`（同 §6.13 所述机理）。
它的 secret 不常变，影响远小于需要切换的 `CLOUDINARY_UPLOAD_MODE`；将来若轮换 Cloudinary 凭据，
该 EF 在 isolate 回收前可能短暂失效，届时按同一手法改为按请求读取即可。

**误判成因（值得记下）**：看到 `secrets list` 里存在这两项 → 直接推断为「用户刚配置的」→ 写进待办，
**中间缺了「查来源」这一步**。与本报告反复强调的口径一致：**观察到的事实**（两项存在）与
**推断**（是谁、何时、为何配置）必须分开陈述 —— 把推断当事实写进交付物，正是本报告批评旧报告的那种失分方式。
