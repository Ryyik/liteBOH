BOHLITE 首个正式版 · 全库评估与放行任务清单（V2 更正版）
评估时间：2026-09-15（V2 更正于同日 19:30）
评估对象：/Users/ryyik/Documents/BOHLITEForMacLatest/BOHLITEBeta2.5
版本说明：本文件为唯一有效版本，V1 内容已整体作废。V2 基于三路独立复核回源逐条更正，
          共改 20 处，并新增 1 个此前漏报的真 bug（P0）。全部结论带本次亲自核对的文件行号。

════════════════════════════════════════════════════════════
零、V1 → V2 更正清单（先看这里）
════════════════════════════════════════════════════════════

【撤回·原判不成立】5 条
R1 原 S13「消息列表无虚拟滚动」—— 撤回。实际已实现：Messages/index.vue:537
   `import { useVirtualList } from '@vueuse/core'`、:591 阈值 VIRTUAL_SCROLL_THRESHOLD=100、
   :688-701 useVirtualList(itemHeight 88 / overscan 10)。论坛侧另有
   Forum/composables/useForumVirtualFeed.js。分页 24 条（:589）与哨兵自动加载与虚拟滚动
   并存，是设计，不是缺陷。
R2 原 S14「登录即 2.5s 预载 4.4MB、与列表图片争带宽」—— 撤回。体积描述正确
   （dist: nsfw-weights 3.6MB + tfjs-vendor 1.09MB），触发条件全错：
   useForumImageModerationPreload.js:17-26 shouldPreload() 只放行桌面大屏
   （deviceMemory≤4 / pointer:coarse / 短边≤900 一律 false），:42 幂等一次，
   :55 非即时的 idle 超时是 12s（不是 2.5s），:59 的 2500ms 只是起跑延时。
   调用方全仓只有 ForumMain 5 处：:1538（选图 immediate）、:2104（开编辑器 immediate）、
   :2014/:2036/:2115（非 immediate，且都在 isLoggedIn 分支内）。登录链路无调用，移动端被拦。
   改判：P2 观察项（桌面大屏空闲时段预载，可选优化）。
R3 原 S8「30+ 处 confirm 无保护会静默失效/卡死」—— 撤回。全仓 window.confirm 只有 2 处
   （BlockWall/index.vue:547、BOH8YearsJourney/CopyEditor.vue:70）。其余全部是
   useConfirmDialog.js:32-51 的 Promise 封装，只有「弹窗已被占用」才 reject（:33-35），
   正常路径 resolve(true/false/null)。逐点核过：UnifiedNavbar:740/749/764 传对象语义正确、
   HeroConsole:1323/1582 有 `if (!confirmed) return`。真实瑕疵只剩一处类型错误（见 R9）。
R4 原 S1「无全局兜底、可观测性为零」—— 撤回一半，改判。
   已有：main.js:281-283 `app.config.errorHandler`；main.js:144 `initMonitoring()`；
   monitoring.js:104/117 已挂 window error 与 unhandledrejection 并调 sendMetric。
   确缺：全局 ErrorBoundary UI —— App.vue:282-303 的 RouterView 外无任何边界，
   ErrorBoundary.vue 仅被 src/views/Lab/index.vue:822 局部使用。
   另 .env 未配 VITE_MONITORING_ENDPOINT，monitoring.js:5-6 `if (!MONITORING_ENDPOINT) return`
   → 上报被短路，错误只落本地 logger。
   改判：Boundary UI 缺失 = P1（原 P0 不成立，errorHandler 已存在）；endpoint 未配 = P1，
   准确说法是「链路未接通」，不是「可观测性为零」。
R5 原 M8「BOH8YearsEvent 是占位死页面」—— 撤回。该文件 436 行真实周年页，未完成仅局部一块：
   :146「暂定」徽标 + :151「活动地图即将上线，敬请期待」。改判 P2 局部未完成。
   Gift 页的判断保留（455 行、日期过期文案、mock 中奖名单）。

【下调·夸大或已过时】4 条
R6 原 S4「无回滚、永不复位」—— 夸大。SharedMemoryManagement.vue:286→:301、:347→:364、
   :387→:408 三处都有 previous* 快照 + `if (!result.ok)` 回滚，ok:false 路径会复位。
   真实残留只有两点：① 接口「抛异常」而非返回 ok:false 时无 try/finally（乐观值不回滚、
   按钮不复位）；② :307/:370/:414 三个裸 setTimeout 无句柄、无卸载清理。改判 P1。
R7 原 S12「发版即丢草稿」—— 过时一半。仍真的：vite.config.js:156-157 skipWaiting +
   clientsClaim 为 true；:159-187 runtimeCaching 无通用 /static/js 规则；
   main.js:117-128 任意一次 SW error 即 unregister 全部 SW 并清空全部 Cache。
   已不成立：「自动 reload 导致丢草稿」—— main.js:217-220 明确 controllerchange 不再自动
   reload，只打日志，刷新由用户在 PWAUpdateToast 弹窗确认（PWAUpdateToast/index.vue:4/37）。
   残留真实路径：旧标签页访问已删除的 hash chunk → router/index.ts:69-81 捕获动态模块加载失败
   后调 forceCleanAndReload（version-checker.js:164），带冷却 + sessionStorage 去重，
   且论坛草稿有 useForumPostDraftStorage 本地存档。改判 P2（含 SW error 误清全库的频控加固）。
R8 原 M7「权益散落四处」（选择性引用）—— 更正。plans/007-beta6-plan.md:18 自己已复核纠正：
   「P0-2b 权益层统一 🟢 核实后基本已是现状（09-08 复核）：useUserTier/subscription-benefits
   已被 9+ 处采用，ProfileMain 已接 composable、Shop 已无本地判断」。
   本次实测 useUserTier 在 11 个文件被引用（useLabQuota、FollowListModal、CommentThread、
   PostDetailMain、ProfileHomePanel、AvatarFrameGrid、CommunityTab、AssetsHubPanel 等）。
   V1 只引 :117 的规划建议、未引 :18 的复核结论，属选择性引用。
   TIER_RANK 残留仅 2 个文件且各有明确理由：useAvatarFrame.js:38（头像框解锁的本地档位推导）、
   SubscriptionPlans.vue:294（购买页升级排序，非权益判断）。改判 P2。
R9 原 S8 位置错（新增真瑕疵）—— UnifiedNavbar/index.vue:818 `alert("您已经在使用BOH桌面版应用了！")`
   传字符串，而 :312 `const { alert, confirm } = useConfirmDialog()`，封装期望对象
   （useConfirmDialog.js:65 `{...options}` + `options.title`）→ 展开字符串得到字符下标对象，
   标题回落默认「请确认」、正文为空，内容整段丢失。同型共 6 处：:818/:823/:827/:833/:835/:840
   （全是 PWA 安装引导）。另有 4 处原生 alert（Login:307、Download:257、Join:414/591）
   能正常弹出，仅违反「不使用原生弹窗」的约定。

【新增·此前漏报的真 bug】1 条（本版最高优先级）
N1 P0 SharedMemoryManagement.vue 的 dialog 从未定义，点编辑/删除必抛 ReferenceError
   证据：:118-130 是完整 import 段（无 useConfirmDialog 导入）；全文件 grep "dialog" 只有两处
   使用：:324 `await dialog.prompt({...})`（editMemory）、:378 `await dialog.confirm({...})`
   （removeMemory）。模板确实挂着入口：:76 `@click="editMemory(item)"`、
   :99 `@click="removeMemory(item)"`。
   后果：点「编辑」/「删除」→ 同步抛 ReferenceError → 变成被吞掉的 rejected promise，
   UI 完全无反应，并产生 unhandledrejection（会被 monitoring 记为运行时错误）。
   即公共记忆的编辑与删除功能整体不可用。修 S4 时必须一起修（补 `const dialog = useConfirmDialog()`
   + 互斥兜底）。

【更正·定位与口径】8 处
P1 PostCard 定时器作用域：PostCard.vue:1 是唯一 `<script setup>` 块（全文件无第二个 script 标签），
   故 :178 `let stripLockTimer = null` 是逐实例变量，不是模块级共享。
   该句柄只在 :182-186 的 lockStripIndex 内 clear，组件卸载不清理 → 「每张卡最多挂一个
   未清理的 900ms 定时器」成立，等级 P2 轻微泄漏。
   （这是本次唯一不采纳的复核意见，已附反证。）
P2 ContentDetailIsland 路径与调用面：真实路径 src/components/UnifiedNavbar/ContentDetailIsland.vue，
   v-html 在 :115（:114 为其 eslint-disable 注释），prop html 在 :25 注释即标注「已消毒 HTML」。
   调用方只有 Newsroom（:141 import，:386 传 DOMPurify.sanitize(news.content, NEWS_SANITIZE_OPTIONS)）
   与 activities/ActivitiesList.vue（:178 传 paragraphs 纯文本分支，:183，不走 html）。
   孤立把 v-html 标为风险属误判 → 改判为硬化建议（组件内加一层 sanitize），非缺陷。
P3 Lab/index.vue:1599 的 e.message 未转义，行号正确，但消费方是 :717 `:html="previewHtml"` →
   Lab/components/DocPreview.vue:78 `DOMPurify.sanitize(props.html)` 统一兜底；
   成功分支 :1597 自己也先 sanitize。不可利用 → P2 代码整洁项。
P4 BohaiSidebar 完整路径为 src/views/BOHAI/BOHAI/components/BohaiSidebar.vue（双层嵌套）；
   其模板深层解引用的判断保留（P2）。
P5 计数更正：Edge Function 14 个（另有 _shared 目录，V1 写 15）；.vue 190 个（V1 写 189）；
   测试 105 个 .test.js（91 unit + 14 integration）+ 1 个 e2e .spec.js = 106 文件，
   vitest 实跑 105 文件。
P6 visibilitychange 为 5 处独立注册（auth.ts:333、version-checker.js:368、main.js:184、
   AnniversaryCafe:1006、CommunityTab:385），属优化项 P2。
P7 「37 处 confirm」口径不准：useConfirmDialog 覆盖 24 个文件、约 69 处调用点
   （useDataAdminMutations.js 17、FreemodelsConfig 12、DataAdmin 9、PityGrantConsole 5 等）。
P8 互斥 reject 确需调用方兜底，且已被部分覆盖：PWAUpdateToast/index.vue:42 注释显示它显式处理
   「弹窗被占用」并暂存重试 → 统一 safeConfirm 属 P2 加固，不是 P0/P1。

════════════════════════════════════════════════════════════
一、总体结论
════════════════════════════════════════════════════════════

规模：src 158372 行、190 个 .vue、45 条懒加载路由、238 个数据库迁移、14 个 Edge Function。
门禁实测（本次运行）：
  eslint        0 error / 2216 warning（几乎全是 no-unused-vars，不阻断）
  vitest        105 文件全通过，1710 passed / 1 skipped，耗时 20s
  vite build    通过，20.78s，PWA precache 110 项 3588 KB
  npm audit     critical 0 / high 11 / moderate 5 / low 1（直接依赖 2 个）
  CI            deploy.yml: build:ci（views / structure / liquid-glass / important-budget /
                first-paint / shell-precache / bundle）+ 并行 checks job（lint + type-check），
                deploy needs [checks, build]；仅 test 被注释跳过

判定：功能完成度约 85%，工程质量属上游。发正式版前要清四类：
  1) 一个「功能整体不可用」的真 bug（N1）+ 缺全局 ErrorBoundary UI（错误已捕获但用户只见白屏）。
  2) 商业化半截：支付是二维码 + 人工入账；积分现金兑现零代码。
  3) 三个 HIGH 级越权/隐私漏洞（安全章复核后全对，本版保留）。
  4) 发布工程三处细节：监控 endpoint 未配、PWA 无 /static/js 运行时缓存 + SW error 清全库、
     无 CSP（GitHub Pages 限制）。
建议：先发 1.0 RC，P0 清零 + 连续 7 天无 P0 工单再转正。当前版本号 6.2.1，需单独定名。

════════════════════════════════════════════════════════════
二、已实现且可用（可对外宣称）
════════════════════════════════════════════════════════════

1. 路由与壳：45 条懒加载路由零漂移；守卫含登录/管理员/封禁拦截 + 3s 超时兜底
   （router/index.ts:96-161）；chunk 丢失自愈（:69-81）。
2. PWA：真实 sw.js + manifest，更新检测（main.js:174-221）+ 用户确认式更新弹窗
   （PWAUpdateToast + App.vue:334）。
3. 论坛闭环：发帖含多图 + 异步审核入队（post-api.js:864/:172）、评论楼中楼
   （comment-api.js:195/266/303）、点赞/转帖/官方卡/举报（forum-interaction-api.js:15/119/146/205）、
   周签到（post-api.js:1585）、服务端类型筛选、#标签语法、虚拟滚动信息流。
4. AI：BOH AI 主体 + agents + 自动路由、AI 额度表与后台（2026062601）、AI 岛、联网检索、论坛摘要。
5. 私信 IM：2482 行，Realtime 订阅 + 指数退避重连（Messages/index.vue:1105/1159-1182）；
   消息列表虚拟滚动（:537/:688-701，阈值 100）。
6. 商城/订单/地址：user_addresses（2026080601）、下单原子 RPC 且服务端取价（2026033104）。
7. 订阅：服务端权威定价（2026091001，忽略前端传参）、试用锁 Pro/3 天。
8. 积分：账本 + 对账 RPC、签到/发帖/管理员发放三进两出闭环。
9. 用户空间：5 tab + 资产 7 分区 + 数据导出（ZIP + 离线网页，7 天一次）。
10. 管理后台：43 tab，审核/封禁/禁言/跨表搜索/导出/备份/变更日志。
11. 运维底座（V1 漏算）：全局 errorHandler + window error / unhandledrejection 监听 + Web Vitals
    （monitoring.js）、日志分级 logger、5 道构建门禁 + CI 并行 checks job。

════════════════════════════════════════════════════════════
三、缺失与半成品
════════════════════════════════════════════════════════════

M1 支付闭环缺失（半成品，用户可见）
   SubscriptionPlans.vue:210「扫描收款码并备注 UID，管理员确认后会为你补充积分」、
   :211/:230 硬编码本地二维码；全仓无支付 API 调用。
M2 积分现金兑现零代码（2026-09-14 已拍板，实现 0 行）
   全仓 cash_redemption 零命中；无 2026091401 迁移；无 PointsRedeemConsole.vue；
   plans/points-redeem-plan.md:69-134 仍为设计稿。
M3 论坛图片后端审核未落地
   forum_image_moderation_cache 全仓零命中；forum-async-worker/index.ts 无 image 分支；
   当前只有前端 nsfwjs，绕过前端直传即可发违规图。
M4 ALTCHA 服务端从未校验（.env:5 开着；auth-login 全文件无校验调用；
   2026042001 已 drop auth_altcha_proofs）→ 有图标不校验比没有更糟。
M5 头像框解锁仍是前端真相源（useAvatarFrame.js:2/6/37/47；仅佩戴写入
   profiles.avatar_frame_url）。:7 注释说明这是有意设计（跟 useUserTier 档位走、
   不新增权益真相源），风险点在「解锁清单是前端常量」可被改。
M6 导航栏全局搜索被硬编码关闭（UnifiedNavbar/index.vue:15-17 `'has-nav-search-panel': false`），
   配套 20+ 处 CSS 完整残留（style.scoped.css:546-683）；用户空间「搜索」只聚焦论坛搜索框。
M7（已按 R8 下调）权益层非「四处分裂」：useUserTier 已被 11 处采用，TIER_RANK 残留 2 处
   且各有理由 → P2。
M8（已按 R5 收窄）死内容：Gift 页（455 行、日期过期、mock 中奖名单）P1；
   BOH8YearsEvent/index.vue:146/:151 局部「暂定 / 即将上线」P2；
   /tutorial、/forum、/creator-studio 已退化为 redirect；
   PROJECT_MANUAL.md:254/:296/:733/:141/:142 多处路径与表结构说明过时。

════════════════════════════════════════════════════════════
四、稳定性与流畅度（按更正后等级）
════════════════════════════════════════════════════════════

【P0】
S1 公共记忆编辑/删除抛 ReferenceError（dialog 未定义）= N1，见第零章。
   落点：:118-130 补 useConfirmDialog 导入与实例；:324/:378 补互斥兜底。
S2 消息中心卸载竞态：Messages/index.vue onMounted 为 async，:1414/:1417 await 之后才在
   :1427/:1429/:1431 注册 window/document 监听、:1432 建立 Realtime 订阅；而 onUnmounted
   在 :1332 起，快速进出时它可能已先跑完 → 监听器与订阅再无回收路径（重复通知 + 通道累积）。
S3 重连定时器未跟踪：同文件 :1169-1179 的退避 setTimeout 最长 30s，卸载后仍会调
   startRealtimeChannels 建一条永不移除的通道；removeRealtimeChannels 只清了 realtimeRefreshTimer。
S4 可编辑列表用数组下标做 key：EditDrawer.vue:365/376、ShopConsole/index.vue:202、
   TagsImpressions.vue:59 → 删除中间项后输入框/开关状态错位（静默写错数据）。
S5 首屏关键路径 loading 卡死：Home/index.vue:344-374 / 432-462 / 464+ 三个 async 无
   try/catch，抛错后 isXxxLoading 永久 true；:471 的短路会让八周年礼物再也领不了。

【P1】
S6 全局 ErrorBoundary UI 缺失（App.vue:282-303 RouterView 外无边界）→ 渲染期抛错仍表现白屏。
S7 监控上报链路未接通：.env 未配 VITE_MONITORING_ENDPOINT → monitoring.js:5-6 直接 return，
   错误只落本地 logger。
S8 SharedMemoryManagement 的 throw 路径无 try/finally + 三个裸 setTimeout（:307/:370/:414）。
S9 清理函数挂 window：SubscriptionPlans.vue:487-536 把 scroll / 每卡 tilt / IntersectionObserver
   写进 window.__subCleanup，:537 onUnmounted 只调一次 → 多实例互相覆盖、旧实例残留。
S10 孤儿 setTimeout（组件卸载后仍写 ref，20+ 处）：AiEdgeTrigger.vue:82-89、
   NotificationToast.vue:38/47、PostCard.vue:178-186（逐实例、卸载不清理）、
   UnifiedNavbar/index.vue:1008-1013、ResetPassword:238、Download:432、AccountSecurity:419 等。
S11 Pinia 持久化串号：stores/health.ts:659-663 健康数据固定 key 持久化且登出不清
   （auth.ts:1083-1087 logout 不触碰）→ 同设备换号读到上一位用户的体检/体重档案。
S12 管理员身份本地优先：auth.ts:174-177 + :1085 + router/index.ts:151-152 本地短路。
S13 首屏依赖未按需：BOHAIMain 静态引入 marked + hljs 8 语言（≈450KB）、Birthday 静态引入
   html2canvas + confetti（204KB）、Profile 静态引入 browser-image-compression（51KB）、
   Login 静态引入 AltchaWidget（109KB）。

【P2】
S14 PWA 缓存策略残留（按 R7 下调）：vite.config.js:156-157 skipWaiting + clientsClaim；
   :159-187 runtimeCaching 只有 supabase-storage / cloudinary / nsfw 三条，无通用 /static/js 规则；
   main.js:117-128 单次 SW error 即注销全部 SW + 清空全部 Cache；chunk 404 时
   router/index.ts:69-81 触发 forceCleanAndReload（有冷却 + 草稿有本地存档）。
S15 桌面大屏空闲预载图片模型（nsfw-weights 3.6MB + tfjs 1.09MB），由
   useForumImageModerationPreload.js:17-26 设备档位 + :55 idle 12s 控制（非「登录即预载」）；
   可选换 wasm 后端或进一步延后。
S16 模板深层解引用无兜底：Lab/index.vue:259/283/356、
   views/BOHAI/BOHAI/components/BohaiSidebar.vue:56-69、Health/index.vue:82、
   Birthday/index.vue:179 等（这些页面无 ErrorBoundary，会落到 S6 的白屏）。
S17 三处硬化建议（非缺陷）：ContentDetailIsland.vue:115 组件内加 sanitize 兜底；
   Lab/index.vue:1599 错误分支改插值/转义（消费方 DocPreview.vue:78 已 sanitize）；
   UnifiedNavbar/index.vue:818/823/827/833/835/840 的 alert 改传对象。
S18 互斥弹窗统一 safeConfirm 包装（useConfirmDialog.js:33-35 会 reject；24 个文件约 69 处调用点；
   PWAUpdateToast:42 已自行兜底，其余多为裸 await）。
S19 重复的 visibilitychange 注册 5 处 → 建议收敛为单例服务。

已排除（不必再投人）：setInterval 全部有 clearInterval；三类 Observer 全部 disconnect；
无事件总线；JSON.parse 全部有 try；ForumMain 与 UserSpaceMain 的请求竞态已用
requestSeq/AbortController 与 fetchToken 处理；未发现 watch 自触发死循环。

════════════════════════════════════════════════════════════
五、安全扫描（复核基本全对，本版保留）
════════════════════════════════════════════════════════════

基线（已确认安全）：.env 从未入库（.gitignore:44）；git 历史无 service_role JWT / sk- /
ghp_ / AKIA / 私钥；VITE_* 全为必须前端可见类；89 张业务表全部启用 RLS；
订阅价格与商城下单均服务端权威（2026091001、2026033104）；profiles.points 禁客户端直改；
管理 RPC 有 current_user_is_admin() 守卫；Edge Function CORS 精确白名单；
SW 不缓存 /rest/v1 与 /auth/v1（vite.config.js:161-170）；v-html 全部经 DOMPurify
（src/utils/dompurify.js:13-21，未放开 style/onerror）。

A1 HIGH 匿名可把用户名反查成登录邮箱
   2026090911_resolve_email_login_from_auth_users.sql:55-61（security definer 跨 auth.users
   查询）、:65（grant to anon）；限流 10 次/分钟且 IP 取 XFF 首段（:19-29）。
   前端登录主链路在用：src/utils/api/auth-api.js:209/615。
A2 HIGH 任意登录用户可读全站 profiles 全部列
   2026090803_security_hardening.sql:60-70 只对 anon 做列级 revoke（文件 :18-20 自认残留）
   → 可互读收货人、手机号、收货地址、pushplus_token。
A3 HIGH 登录/注册/邮件限流键可伪造
   auth-login/index.ts:61-64、auth-register/index.ts:33-34、email-send/index.ts:143-145
   均取 x-forwarded-for 首段；修复样板已在 api-key-vault/index.ts:225-236。
A4 HIGH api-key-vault 的 runtime-chat 允许未登录访客直调付费 LLM
   api-key-vault/index.ts:230-241（无 token 返回 guest）、:1910-1913（按 IP 计量，代理池可绕）。
A5 MEDIUM Cloudinary 未签名 preset 暴露（cloudinary-client.js:12-17 默认 BOHIMG；
   类型/大小校验全在客户端 cloud-upload-guard.js:1-14）。
A6 MEDIUM 其他越权残留：refund_lab_usage 授 anon 且无归属断言（2026070802:246-277）；
   activity_entries 对 anon 全表可读（2026090802:126-127/156）；
   current_user_is_admin() 只认 role='admin'（2026051002:3-16）与函数侧
   ['admin','superadmin']（generate-forum-weekly-report/index.ts:103）口径不一致；
   缺 supabase/config.toml，14 个函数 verify_jwt 未在仓库声明。
A7 MEDIUM 依赖：sharp（需 >=0.35.4，构建期）、pptxgenjs（经 image-size 引入解析死循环）
   为直接依赖；其余 9 项 high 为传递依赖。
A8 MEDIUM 无 CSP / X-Frame-Options / Referrer-Policy（GitHub Pages，public/_headers:1-2 自述不支持）。
A9 LOW supabase/.env:6 明文 ALTCHA_HMAC_KEY（未入库，建议轮换）；output/ 与 debug-screenshots/
   已被 git 跟踪（含后台页面截图）；auth-login/index.ts:20-24 仍查已 drop 的 profiles.email
   （2026090803:34）且前端零调用 → 僵尸函数，建议删除。

════════════════════════════════════════════════════════════
六、通向首个正式版的任务清单
════════════════════════════════════════════════════════════

批次 0（P0，放行前必做，预计 3~5 个工作日）
T0-1 修 SharedMemoryManagement 的 dialog 未定义（N1/S1）
     范围：:118-130 补导入与实例、:324/:378 加互斥兜底；顺带补 try/catch/finally 与
     :307/:370/:414 定时器句柄化（一并清掉 S8）。
     验收：编辑与删除各成功执行一次；把接口 mock 成抛异常后乐观值回滚、按钮可再点。
T0-2 修消息中心卸载竞态与重连泄漏（S2/S3）
     范围：Messages/index.vue 加 disposed 标志、监听器提到 await 之前、重连定时器句柄化。
     验收：快速进出消息页 10 次，Realtime 通道数不增长、通知不重复弹。
T0-3 可编辑列表改稳定 key（S4）：EditDrawer:365/376、ShopConsole:202、TagsImpressions:59。
     验收：删除中间一项后其余项内容与开关不错位。
T0-4 首屏关键路径 loading 兜底（S5）：Home/index.vue:344/432/464 改 try/catch/finally。
     验收：接口 mock 500 时按钮可再点且有失败提示。
T0-5 三个 HIGH 安全项（A1/A2/A3）
     新迁移：resolve_email_for_login 取消 anon 授权并限为 auth.uid() 自查/管理员跨查；
     profiles 对 authenticated 列级 revoke 后按公开列重新 grant（隐私列迁 profile_private）；
     三处限流键改用 _shared 的 getClientIp。
     验收：anon key 直连被拒；普通用户 select=shipping_phone 返回 400；伪造 XFF 不能绕限流。
T0-6 验证码去留（M4）+ guest 付费通道关闭（A4）：二选一（推荐先关 VITE_ALTCHA_ENABLED）；
     api-key-vault 无 token 直接 401。验收：登录页无假验证码；无 Authorization 调
     runtime-chat 返回 401。

批次 1（P1，正式版窗口内，2~3 周）
T1-1 全局 ErrorBoundary UI（S6）：ErrorBoundary.vue 提到 src/components，包住 App.vue:282 的
     RouterView/KeepAlive，提供「刷新 / 回首页 / 丢弃缓存重载」。
T1-2 接通监控上报（S7）：配置 VITE_MONITORING_ENDPOINT，验证 runtime_error /
     unhandled_rejection / Web Vitals 三类事件可上报。
T1-3 商业化定性（M1/M2）：接真实支付，或把文案明确改为「内测赞助、人工到账」；
     积分现金兑现若进 1.0，按 plans/points-redeem-plan.md 落 2026091401 迁移 + 用户面板 +
     管理端审核面板。
T1-4 论坛图片后端审核（M3）：建缓存表 + forum-async-worker 加 image 分支 + 前端消费。
T1-5 会话与权限治理（S11/S12）：health persist key 按 userId 分片 + 登出清理；
     router:151 去掉本地 isAdmin 短路。
T1-6 清理函数与孤儿定时器（S9/S10）：SubscriptionPlans 改闭包 cleanup；20+ 处统一句柄化。
T1-7 依赖与响应头（A7/A8）：sharp 升 ^0.35.4；评估 pptxgenjs；index.html 加 CSP meta；
     output/ 与 debug-screenshots/ 加入 .gitignore。
T1-8 管理员口径与函数声明（A6）：current_user_is_admin() 补 superadmin；refund_lab_usage
     加归属断言并收窄授权；activity_entries 收窄匿名读；补 supabase/config.toml。
T1-9 Gift 过期活动页下线或改造（M8 的 P1 部分）。

批次 2（P2，正式版后持续）
T2-1 首屏包体按需化（S13）：marked/hljs、html2canvas+confetti、压缩库动态化，
     AltchaWidget 改 defineAsyncComponent。
T2-2 互斥弹窗统一 safeConfirm（S18）+ alert 传参修正（S17，6 处）。
T2-3 模板深层解引用兜底（S16）+ ContentDetailIsland 内部 sanitize 兜底 + Lab:1599 转义。
T2-4 PWA 细节收敛（S14）：runtimeCaching 增 /static/js 的 StaleWhileRevalidate；
     SW error 清理加频控。
T2-5 图片模型预载策略微调（S15）+ 统一 visibility 服务（S19）+ 重复 v-for index key 清理。
T2-6 文档与死页面清理（PROJECT_MANUAL 漂移、三个 redirect 去留、八周年局部占位）。

批次 3（发布工程，贯穿）
T3-1 版本治理：建 CHANGELOG.md；VERSION / package.json / 更新弹窗版本号单一源；定名正式版。
T3-2 CI 补 test 门禁（准确说法：build:ci + checks 已覆盖 lint/type-check/五道门禁，
     仅 tests 被注释跳过）。
T3-3 预发验收：用 anon key 实测 A1/A2/A5 三条（直连 RPC / select 隐私列 / 直传 Cloudinary）。
T3-4 迁移纪律保持：YYYYMMDDNN + 先 dry-run + push 必 --yes。

════════════════════════════════════════════════════════════
七、最小放行门槛（1.0 前必须全绿）
════════════════════════════════════════════════════════════

1. T0-1 公共记忆编辑/删除可用且失败可回滚（N1）
2. T0-2 消息中心无订阅泄漏、无重复通知
3. T0-3 可编辑列表 key 正确
4. T0-4 首屏关键路径无 loading 卡死
5. T0-5 匿名邮箱反查已关闭 + profiles 隐私列对 authenticated 不可读
6. T0-5 限流键不可伪造
7. T0-6 验证码去留明确 + guest 付费通道关闭
8. T1-1 全局 ErrorBoundary UI 就位
9. T1-2 错误上报链路已接通（有 endpoint）

其中 5、6 必须用 anon key 在预发实测，不能只看迁移文件。
